# 验收标准追踪表

> 规格书 19.2 定义了 52 条验收标准。本文逐条追踪实现状态。
> 附录A#7 要求「每个阶段完成后运行对应验收用例，并列出已完成、未完成和已知偏差；
> **不能用伪代码或占位图冒充完成**」—— 所以这张表只有三种状态，没有「基本完成」。

## 状态图例

| 标记 | 含义 |
|---|---|
| ✅ | **已完成且已验证**。有自动化测试或已人工驱动实际游戏确认 |
| 🔨 | **进行中**。所属里程碑已开工 |
| ⬜ | **未开始** |
| ⚠️ | **已知偏差**。实现与规格书不一致，理由记录在下方「已知偏差」一节 |

> ⚠️ 重要：只有当你**实际跑过游戏或跑过测试看到它成立**时才标 ✅。
> 「代码写完了应该没问题」不算，那是 🔨。

## 汇总

> **M10（联网对战）新增 13 项端到端判据**，见 `scripts/verify-m10.ts` 与
> [13-roadmap-post-m9.md](13-roadmap-post-m9.md) A6。它们不属于规格书的 52 条，
> 而是「跨进程之后规则是否仍然成立」的工程判据。当前 13/13 通过。

| 状态 | 数量 |
|---|---|
| ✅ 已完成 | 52 |
| 🔨 进行中 | 0 |
| ⬜ 未开始 | 0 |
| ⚠️ 已知偏差 | 4（#2 / #6 / #7 / #8，见下方「已知偏差」，均不影响任何一条验收标准成立；#1 / #3 / #4 / #5 已解决）|
| **合计** | **52** |

> 汇总数与下表必须一致。核对命令：
> `python -c "import re;rows=[l for l in open('docs/10-acceptance-tracking.md',encoding='utf-8') if re.match(r'^\| \d+ \|',l)];print(len(rows),[r.split('|')[4].strip() for r in rows].count('✅'))"`

---

## 逐条追踪

| # | 验收标准 | 里程碑 | 状态 | 验证方式 |
|---|---|---|---|---|
| 1 | 游戏为完整 3D 场景，镜头可在第一人称与远距离第三人称之间连续缩放 | M1 | ✅ | 人工 + `pnpm verify:m1` #1a/#1b（第一人称 ↔ 18m 连续缩放） |
| 2 | 左键环绕不改变角色朝向，右键镜头与角色朝向联动 | M1 | ✅ | `CameraController.test.ts` + `pnpm verify:m1` #2a/#2b/#2c |
| 3 | 镜头不能穿墙获取敌人信息，靠墙时会拉近并平滑恢复 | M1 | ✅ | `CameraController.test.ts`「墙挡在中间时镜头被拉到墙前」+ 非对称插值断言 |
| 4 | 玩家可以通过点击、姓名板、Tab、队伍框和焦点选择目标 | M2 | ✅ | `targeting.test.ts`（34 项）+ `pnpm verify:m2` #4a–#4d |
| 5 | 未被发现的潜行目标不能被点击、Tab 或小地图选中 | M9 | ✅ | `net/visibility.test.ts` 30 条 + `pnpm verify:m9` #5a–#5e。★ 做法是**整个实体不进快照**，不是发个隐藏标记 |
| 6 | 硬目标在超距或被墙遮挡后仍可保留，但技能明确显示不可用原因 | M2 | ✅ | `targeting.test.ts`「超距/被墙遮挡后硬目标仍保留」 |
| 7 | 直接目标、地面、直线、锥形、自身中心和碰撞投射物六类瞄准均可区分 | M3 | ✅ | `aiming.test.ts`（六类分流 + 技能栏覆盖）+ `pnpm verify:m3` #7a/#7b |
| 8 | 地面技能非法位置不能确认，真实范围边界与实际判定一致 | M3 | ✅ | `aiming.test.ts`「预览边界 == 判定边界」+ `verify:m3` #8a–#8e（含非法落点拦截） |
| 9 | 近战、短程、中程、标准远程和长程技能遵守统一距离基准 | M0/M3 | ✅ | `geometry.test.ts` + `data.test.ts`（距离上限断言） |
| 10 | 角色视觉大小、帽子和武器不会改变战斗碰撞体与被选中难度 | M0/M1/**M12** | ✅ | `geometry.test.ts`「模型再大也不改变碰撞体」+ **`verify:m12` #10-m12：引入真实模型后重验** —— 八个职业源模型比例各异，实测视觉身高全部归一化到 `HITBOX_HEIGHT`（偏差 < 0.01 m）|
| 11 | 墙体、柱子和封闭门正确阻挡视线，低矮物体不会频繁误判 | M0/M3 | ✅ | `geometry.test.ts`「低矮栏杆不造成无视线」 |
| 12 | 锁定投射物与碰撞投射物具有不同命中和躲避规则 | M3 | ✅ | `projectile.test.ts`（17 项）：同一套走位下锁定命中、碰撞落空 |
| 13 | 读条、引导、瞄准射击和普通自动攻击拥有不同中断规则 | M2 | ✅ | `casting.test.ts` 中断矩阵（硬控制/移动/沉默/缴械各一条） |
| 14 | 普通伤害默认不会取消或延长施法 | M2 | ✅ | `casting.test.ts`「普通伤害不取消也不延长施法」（同时断言 endsAt 不变） |
| 15 | 专用打断能停止可打断法术并锁定对应学派 | M2 | ✅ | `casting.test.ts` + `verify:m2` #15a/#15b（法师反制 4 秒特例） |
| 16 | 专用打断可以取消物理射击准备，但不会产生魔法学派锁定 | M2 | ✅ | `casting.test.ts`「打断物理射击只取消本次，不产生学派锁定」 |
| 17 | 沉默不能阻止物理射击，缴械不能阻止纯魔法施法 | M2 | ✅ | `casting.test.ts` 四条：沉默↔物理射击、缴械↔纯魔法 各正反各一 |
| 18 | 玩家可以主动取消读条进行假施法，取消不消耗技能资源和冷却 | M2 | ✅ | `casting.test.ts` + `verify:m2` #18 / 7.5（骗打断闭环） |
| 19 | 施法开始和完成时都会检查目标、距离、视线和朝向 | M2 | ✅ | `casting.test.ts`「开始合法、完成瞬间超距/目标死亡 → 失败且无副作用」 |
| 20 | 技能失败不会产生重复伤害、资源扣除或冷却异常 | M2 | ✅ | `casting.test.ts`「连续五次失败不累积任何副作用」 |
| 21 | 八职业均拥有明确定位、资源、弱点、专用打断或等价沉默 | M0 | ✅ | `data.test.ts`（八个职业逐个断言） |
| 22 | 所有职业可以自由重复选择，系统不强制阵容 | M5 | ✅ | `room.test.ts`（全队同职业/无治疗/全近战均可开始）+ `pnpm verify:m5` #22a/#22b |
| 23 | 控制递减、通用解控、驱散、免疫和治疗抑制可正确结算 | M4 | ✅ | `effects.test.ts`（46 项：递减梯度/解控/驱散/免疫/抑制/叠加规则）+ `pnpm verify:m4` |
| 24 | 2v2、3v3、5v5 地图尺寸和掩体数量符合对应人数 | M5 | ✅ | `maps.test.ts`（三张图逐条断言尺寸、掩体数、公平约束）+ `verify:m5` #24 |
| 25 | 竞技场当前回合死亡后不能普通复活，宠物不计入存活人数 | M5 | ✅ | `arena.test.ts`（宠物不计存活、死后不复活）+ `verify:m5` #25a/#25b |
| 26 | 双方最后玩家同时死亡时可以正确判定平局 | M5 | ✅ | `arena.test.ts` 结算窗口三种情形 + `verify:m5` #26 |
| 27 | 决胜阶段可以结束多治疗、多潜行和高机动拖延 | M5 | ✅ | `arena.test.ts` + `verify:m5` #27a/#27b/#27c（压迫伤害穿透完全免疫） |
| 28 | 经典竞技场不生成临时武装，武装竞技场生成职业武器、护甲和增益 | M6 | ✅ | `loadout.test.ts` + `pnpm verify:m6` #28（经典 0 军械点 / 武装 3 点） |
| 29 | 不符合职业的玩家不能拾取武器或护甲，错误交互不会使物品消失 | M6 | ✅ | `loadout.test.ts` + `verify:m6` #29 —— **失败路径不删除地面物品** |
| 30 | 战场装备栏始终保留默认装备，并可携带规定数量的备用装备和道具 | M6 | ✅ | `loadout.test.ts`（默认装备不可删、备用槽 2/2 上限）+ `verify:m6` #30 |
| 31 | 双手武器高单击低攻速，双持低单击高攻速，盾牌高防御低输出 | M0 | ✅ | `data.test.ts`「武器取舍成立」 |
| 32 | 装备均存在清晰优势与代价，没有全面上位装备 | M0 | ✅ | `data.test.ts`「没有全面上位装备」 |
| 33 | 切换武器和护甲具有时间、动作与中断窗口 | M6 | ✅ | `loadout.test.ts`（武器 0.8s 可移动 / 护甲 2s 必须原地）+ `verify:m6` #33 |
| 34 | 换装不能刷新普通攻击、取消后摇、重置技能、恢复资源或清除负面 | M6 | ✅ | `loadout.test.ts` 五项逐条断言 + `verify:m6` #34。`completeSwap()` 只有两行赋值 |
| 35 | 当前武器模型、挂点和攻击动作在切换完成后正确更新 | M6/M8 | ✅ | `hud.test.ts`（新旧对比只列差异且带方向箭头）+ `verify:m8` #35a/#35b（分区显示/当前高亮/优势代价/换装进度条）。⚠️ 「武器**模型**」仍是程序化几何体，无外部素材（docs/09 §5 的显式决定） |
| 36 | 敌人能识别当前武器和护甲，但不能查看备用装备 | M6 | ✅ | `loadout.test.ts`「敌方视图字段列表恒为三项」+ `verify:m6` #36 |
| 37 | 回合结束后临时装备和增益全部清除 | M6 | ✅ | `arena.test.ts`（光环/递减/地面区域）+ `loadout.test.ts`（临时装备）+ `verify:m6` #37 |
| 38 | 夺旗战场支持拔旗、携旗、掉旗、归还、交旗和重置完整流程 | M7 | ✅ | `flag.test.ts`（拔旗读条/跟随/掉落/归还/交旗）+ `ctf.test.ts`（地图路线与门洞）+ `verify:m7` #38a/#38b/#38c（真实跑图跑完整局） |
| 39 | 己方旗帜不在基地时无法交旗 | M7 | ✅ | `flag.test.ts`「交旗**途中**己方旗帜被拔走立即中断」+ `verify:m7` #39a/#39b |
| 40 | 潜行、消失和完全无敌会先掉旗，旗帜不会随角色隐藏 | M7 | ✅ | `flag.test.ts`「旗帜不随隐身角色消失」+ `verify:m7` #40a/#40b/#40c |
| 41 | 双方同时持旗时可继续比赛，并通过旗手聚焦打破僵局 | M7 | ✅ | `flag.test.ts`（叠层节奏 + 逐步清除按时间走）+ `verify:m7` #41a/#41b/#41c/#41d |
| 42 | 旗手断线、退出或进入非法区域时旗帜回到最后合法位置 | M7 | ✅ | `flag.test.ts`（断线/非法区域/实体消失三路）+ `verify:m7` #42a/#42b |
| 43 | 复活波次、复活保护和基地出口不会造成永久堵门 | M7 | ✅ | `flag.test.ts` + `ctf.test.ts`（复活区出口分散、后墙挡视线）+ `verify:m7` #43a–#43f |
| 44 | 角色可沿墙滑动、自动跨越低障碍、正常上下楼梯，不能斜向爬陡坡 | M1 | ✅ | `movement.test.ts`（滑墙/跨障/楼梯/陡坡）+ `verify:m1` #44a/#44b/#44c |
| 45 | 跳跃允许有限空中调整但不能增速或瞬间反向 | M1 | ✅ | `movement.test.ts`「空中画圈不能累积速度」+ `verify:m1` #45a/#45b |
| 46 | 冲锋、闪现、后跃、拉拽和队友拉回都不能穿墙或到达非法位置 | M1/M4 | ✅ | `effects.test.ts`「冲锋撞墙停在墙前」「拉拽不能穿墙」+ `geometry.test.ts` |
| 47 | 动作状态不会因单帧抖动反复切换，传送不会播放高速跑步 | M1 | ✅ | `AnimationController.test.ts`（双阈值迟滞、传送不误判为跑步） |
| 48 | 所有关键地面区域、控制、护盾和完全免疫在低画质下仍可见 | M8/**M12** | ✅ | `quality.test.ts`（关键八项逐条、三档全可见、两表不相交 —— `hiddenAtQuality()` **只接受装饰角色**，隐藏关键元素是类型错误）+ `verify:m8` #48a/#48b（最低画质下控制标记/旗帜/队伍框/小地图逐项确认）+ **`verify:m12` #48a/#48b/#48c：引入美术后重验** —— 最低画质卸掉 IBL（14.4 允许的「非关键光照」）但角色模型与 HUD 五块一块不少 |
| 49 | 第一人称下技能特效不会遮满屏幕，最远镜头下旗手和范围仍清晰 | M8 | ✅ | `vfx.test.ts`（近身透明度 ≤0.25 且 >0 —— 压透明度而非隐藏；远距关键标记放大）+ `verify:m8` #49a/#49b（18m 最远镜头下旗帜信息仍在） |
| 50 | 战后统计包含打断、驱散、控制、换装、装备争夺和夺旗贡献 | M9 | ✅ | `stats.test.ts` 57 条 + `pnpm verify:m9` 六类逐条断言。★ 含 16.4「不能只按伤害或击杀评选」的行为测试 |
| 51 | 外部素材有来源登记；GitHub 已公开分发媒体允许入库与发布（GH）；无暴雪资产 | M9/**M12** | ✅ | [09-asset-license.md](09-asset-license.md) §0.1/§7.1：GH 档可 commit/发布；`assets/art`、`assets/music` 已登记。**M12 起由 `verify:m12` §1 自动校验**：顶层包逐个对照 §4 登记表、`assets/local/` 未被 git 跟踪、禁止项路径扫描 |
| 52 | 不存在账号等级、永久装备、付费属性或外观稀有度带来的战斗优势 | M9 | ✅ | [12-fairness-review.md](12-fairness-review.md) 逐条判据 + `verify:m9` #52/#52b 用**数据层**复核 |

> #46 在 M4 转为 ✅：位移技能已接入效果系统，所有位移统一走
> `effects/displacement.ts` 的 `moveTo()`，它是 `clampDisplacement` 的唯一出口。
>
> #37 在 M6 补完：M5 已清空光环/递减/地面区域，M6 补上临时装备与换装状态。
> 它在 M5 阶段刻意标的是 🔨 —— 半条规则成立不算成立（附录A#7）。

---

## 已知偏差

> 实现与规格书 [00-design-spec.md](00-design-spec.md) 不一致的地方，必须登记在此。
> 格式：偏差内容 / 涉及条款 / 原因 / 是否计划修正。

### 1. 抗法型护甲的「魔法控制承受降低」只实现了一半 —— ✅ **M11 已解决**

| 项 | 内容 |
|---|---|
| **偏差** | ~~10.8 抗法型护甲的优势写作「法术伤害与**魔法控制**承受降低」，当前只实现了法术伤害那一半~~ |
| **涉及条款** | 10.8 / 17.1 |
| **原因（历史）** | schema 只有全局的 `ccDurationTaken`，写成全局 0.8 会顺带削减物理控制 —— 那是抗控型护甲的身份，两件护甲会互相踩线。**宁可少表达一半优势，也不要表达错** |
| **解决** | M11 给 `AuraModifiers` 加了 `ccDurationTakenBySchool`，`applyControl()` 从 `ctx.skillId` 反查 `SkillDef.school`，抗法护甲已接上，advantage 文案改回「法术伤害与魔法控制时长降低」。★ 测试的重点是**物理学派必须是 undefined** —— 那才是它与抗控型的分界线 |

### 2. 16.2「增益期间击杀」恒为 0

| 项 | 内容 |
|---|---|
| **偏差** | `ArenaStats.killsDuringBuff` 目前恒为 0 |
| **涉及条款** | 16.2 |
| **原因** | 消耗品能被拾取（`DropKind` 含 `'consumable'`），但 sim 里没有任何地方**使用**它们产生增益 —— 所以「增益期间」这个状态从不存在。★ 按附录A#7，如实记为缺口，而不是用一个恒为 0 的字段冒充完成 |
| **是否修正** | 计划修正。`recordItemBuff()` 已留好唯一入口，消耗品接上使用路径后这一项自然有值 |
| **⚠️ M11 核查更正** | 缺口比原记载更大：`data/schema.ts` 里**根本没有 `ConsumableDef`**（没有名字、效果表、持续时间、冷却），也**从没有代码创建过 `kind:'consumable'` 的掉落物**，`Loadout.consumables` 至今只被赋值过 `[]`。所以修正它的工作量是「新增数据类型 + 注册表 + 拾取 + 使用 + 统计接线」，不是补一个函数 |

### 3.「减弱镜头震动」没有接线对象 —— ✅ **打击感改造已解决**

| 项 | 内容 |
|---|---|
| **偏差** | ~~17.2 列出的「减弱镜头震动」设置项已实现规则（`shakeAmplitude()`）并被测试覆盖，但本项目**根本没有镜头震动效果**，所以它没有作用对象~~ |
| **涉及条款** | 17.2 |
| **原因（历史）** | 震动不是任何一条验收标准要求的效果。保留这个函数是为了在引入震动时有一个**唯一入口** —— 震动一旦分散在多处实现，这个设置就会只减弱其中一部分 |
| **解决** | 打击感改造引入 `camera/CameraShake.ts`（创伤模型，docs/07 §1.7）。当初留唯一入口的用意兑现了：`sample()` 的 yaw/pitch/roll/pullIn 四通道**各自**过 `shakeAmplitude()`，`CameraShake.test.ts` 断言 `cameraShake=0` 时四通道全零、`CameraController.test.ts` 断言满创伤 30 帧位置与朝向和无震动逐帧相同。防穿墙是**结构性**的：角度通道不动位置、位移通道只从碰撞探测后的距离里做**减法**（只拉近永不推远） |

### 4.（M10）事件消息按可见性整条丢弃，命中反馈可能缺失 —— ✅ **M11 已解决**

| 项 | 内容 |
|---|---|
| **偏差** | ~~服务器广播战斗事件时，只要引用了不可见实体就整条不发 → 被潜行者打了一下收不到伤害事件~~ |
| **涉及条款** | 14.1 / 验收 #5 |
| **解决** | `Damage` / `Heal` 的 `sourceId` 改为**可空**，服务器 `redactFor()` 优先「抹掉来源」而不是「整条丢弃」：目标看不见才整条不发（那时这条与他无关）。★ `verify:m10` 第 1c 条钉住它，并**先断言真的收到了 ≥1 条 Damage** —— 否则「没泄露」会因为一条都没发而平凡成立 |

### 5.（M10）服务器端 TabTarget 用角色朝向而非镜头朝向 —— ✅ **M11 已解决**

| 项 | 内容 |
|---|---|
| **偏差** | ~~5.3 规定 Tab 取**镜头**前方 140°，而服务器端的 `TabTarget` 用的是角色朝向~~ |
| **涉及条款** | 5.3 |
| **解决** | `NetworkScene` 在**客户端**算 Tab（它有镜头 yaw）再发 `SetTarget` —— 服务器对 `SetTarget` 校验可见集合，所以既符合 5.3 又不放宽安全边界。★ 5.3 的优先级排序与「下一个」的循环语义**复用 shared 的 `sortTabCandidates()` / `nextTabPick()`**，客户端只负责把快照喂成那个形状；配了一条**一致性测试**断言两条路径在同一场景挑出同一个人。★ 验收 #5 在这条路径上是自动成立的 —— 未被发现的潜行者根本不在快照里。服务器端那份保留为无客户端时的兜底 |

### 6. 美术基调由「风格化奇幻」调整为「可爱卡通（Q 版）」

| 项 | 内容 |
|---|---|
| **偏差** | 13.2 首版人物风格的**基调形容词**由「风格化低/中低多边形奇幻角色」改为「可爱卡通（Q 版）：大头、圆润、明亮饱和」；14.2 八属性特效按此基调实现（高饱和双色、先胀后消的 Q 弹粒子，而非写实火焰烟雾）|
| **涉及条款** | 13.2 / 14.2 |
| **原因** | 项目所有者的创作决定（基调偏好）。★ 这是**纯表现基调**的调整 —— 13.2 其余每一条玩法约束原文不动：碰撞体仍大体一致（验收 #10）、职业/阵营识别方式不变、14.3 可读性与 14.4 低画质公平的关键元素清单一字未改。所以它不影响任何一条验收标准的成立 |
| **是否修正** | 不修正，这是目标基调 |

### 7. 暴击机制（规格书没有这条机制 —— **玩法新增**）

| 项 | 内容 |
|---|---|
| **偏差** | sim 新增暴击：全职业 10% 几率、1.5 倍伤害/治疗（`CRIT`，constants/combat.ts）。规格书 8.x 的结算流程里**没有暴击** |
| **涉及条款** | 8.x / 14.1 / 17.2 |
| **原因** | 项目所有者对实测反馈「打击效果不强烈」的拍板：真暴击机制 + 表现层强调都要。⚠️ 与 #6 的纯基调调整**性质不同** —— 这真的改变了伤害数值分布（均值 ≈ +5%） |
| **影响分析** | · 随机性由既有的**按实体 PRNG** 承载（暴击掷攻击者的流、免疫/被规避不消耗随机数，`crit.test.ts` 钉死），回放与配平仍可复现<br>· 结算顺序：免疫→规避→**暴击**→修正→背刺→取整→吸收（docs/03「暴击」小节）；周期跳（DoT/HoT/地面 tick）与 8.5 压迫伤害不暴击<br>· #14（伤害不打断施法）/ #23 / #27 / #48 / #49 逐条未变 —— 暴击只是乘了个系数，结构上碰不到打断/免疫/画质<br>· **balance 基线已重出**（2026-08-01，168 场种子=1：胜率极差 95.2pp，hunter 95.2% 居首 —— 与暴击前同为回归基线不是平衡结论）<br>· 协议 `Damage`/`Heal` 新增 `crit?`/`overkill`；`'crit'` **仍在** `FORBIDDEN_CLIENT_FIELDS`（服务器下发 ≠ 客户端可上报），扫描窗口自检测试防误删 |
| **是否修正** | 不修正，这是目标机制。刻意**不引入**暴击属性系统（那是配平工作不是手感工作，`rollCrit` 预留了 chance 参数）；16.x 无暴击统计要求，stats 刻意不加 |

### 8. 渲染顿帧（hit-stop，规格书没有这个效果）

| 项 | 内容 |
|---|---|
| **偏差** | 打击瞬间的渲染时间缩放（重击 55ms / 暴击 75ms / 击杀 90ms 冻结在 0.06×，`render/HitStop.ts`）|
| **涉及条款** | 无对应条款（新表现）；边界与 17.2 / 验收 #47 相关 |
| **原因** | 打击感改造的一环。安全边界只有一条：**只缩放渲染 dt** —— GameLoop 的模拟步、输入采样、`serverTime`/插值、AnimationController 全部走真实 dt（`GameLoop.test.ts` 断言 timeScale=0.06 时模拟步数不变），预测确定性（docs/08 §5）与 #47 反抖动不受影响 |
| **是否修正** | 不修正。有独立开关 `accessibility.hitStop`（默认开）—— 它不隐藏任何信息通道，不违反 17.2 第二句的反模式；也**不进** 17.2 第三句点名的四项（表长度被测试钉在 4）|

### 9.（M14）普通攻击的开火判据：目标驱动，而非 4.x 的右键开关

| 项 | 内容 |
|---|---|
| **偏差** | 4.x 写「右键点击敌方目标：开始或停止普通攻击」（手动开关）。联网对局的实现是**目标驱动**：`MatchLoop.syncSwings()` 每 tick 同步——敌方硬目标存活即开火，失去目标/换友方/死亡即收手 |
| **涉及条款** | 4.x / 7.6 / 8.1 |
| **原因** | M14 接线普攻时（在此之前 `SwingStore` 只在测试与 balance-report 里被登记，**真实对局里普攻不存在**，战士因此没有怒气来源），协议里没有「开始/停止普攻」消息；目标制战斗里「选中敌人却不想打他」的场景极少，而漏开火的代价（零怒气、零白字）是结构性的。零新协议消息 |
| **是否修正** | 待定。若实测需要「选中不打」（如仅观察目标框），加一条协议消息即可切回开关语义；`beginSwing` 幂等（换目标不刷新挥击计时，7.6）这一层不受影响。试验场（141 项验收载体）没有 SwingStore，不受影响 |

### 10.（M14）旋刃斩：瞬发固定落区，而非移动旋风

| 项 | 内容 |
|---|---|
| **偏差** | 9.1 旋刃斩的意象是「持续旋转攻击（引导，可移动）」。实现为**瞬发 + 固定在起手位置的 5 米圆区**（凛冬领域同款），战士自身的免疫减速定身光环照常 |
| **涉及条款** | 9.1 / 7.1 |
| **原因** | 两个实现层事实：① 光环周期效果结算在**持有者**身上——旋刃原实现把带周期伤害的光环挂在战士自己身上，**8 跳全打在自己头上**（自残键，配平 bot 用上它的那一轮战士胜率应声掉到 4.8%）；② 引导在本实现里只在结束时结算一次，无逐跳出口。schema 尚无「跟随实体的持续区域」 |
| **是否修正** | 计划修正：给 `spawnGroundArea` 加「跟随施法者」选项后恢复移动旋风（M16 表现补齐的候选）。counters/description 已按现状如实改写 |

### 11.（M14）数值配平批：与 9.x 表格数字的系统性偏离

| 项 | 内容 |
|---|---|
| **偏差** | 19 处 `flat` 伤害/治疗、各武器 `swingPercent`、资源 `regenPerSecond`、`RAGE_PER_SWING` 等按 M14 配平结果改值，与规格书 9.x 表格里的示例数字（如重创斩以外的武器百分比）不再逐一相等；「自动射击」不再是按钮技能（9.5 该行的「移动中自动进行｜武器间隔」由 7.6 挥击系统承载 —— 这一条是**向规格书靠拢**而非偏离） |
| **涉及条款** | 9.x 全部数值行 |
| **原因** | 规格书 9. 前言明文：「以下数值用于首轮可玩原型和规则验收，**不代表最终平衡结果**」。配平判据（docs/14 §M14）：极差 ≤30pp、无职业 <15%/>80%、无场均 <6s —— 已达成（28.6pp / 33.3–61.9% / ≥13.7s，种子 1）。每处改动的理由写在数值旁的 `// M14` 注释里（技术债 §2 的教训） |
| **是否修正** | 不修正（这就是配平的产出）。瞄准类型、反制窗口、职业定位全部未动（9. 前言的红线）；技能**描述文字**已与新数值同步 |

---

## 待设计侧确认的问题

实现过程中发现的规格书留白或潜在矛盾，需要设计侧拍板：

| # | 问题 | 涉及条款 | 当前处理 |
|---|---|---|---|
| Q1 | 规格书未给出绝大多数技能的具体伤害/治疗数值（只有「造成基础神圣伤害」「恢复大量生命」等描述） | 9.x | 按各职业生命/资源基线估了占位值，代码中逐条注释标明。**数值配平需要一轮专门的工作** |
| Q2 | 牧师武器方案提到「群体治疗 +12%」「范围治疗 -10%」，但 9.7 技能表里没有任何群体治疗技能 | 9.7 | 武器描述保留，暂无对应技能。需确认是否漏了一个技能 |
| Q3 | 法师「寒冰护盾」未说明能否被驱散 | 9.6 | 暂按不可驱散（`DispelType.None`）处理 |
| Q4 | 肾击「消耗连击点造成 1 至 3 秒昏迷」的具体每点映射未给出 | 9.4 | 按 5 点上限 3 秒线性处理，1 点为 0.6 秒。若应为 1 点=1 秒需改用 `byPoints` 显式列表 |
| Q5 | 圣骑士武器表提到「剑盾保留保护型盾击技能」，但 9.2 技能表未列出该盾击 | 9.2 | 按 warrior 的盾击范式补了「正义盾击」，需确认是否符合设计意图 |
| Q6 | 德鲁伊长柄战刃 reach 文档写 3.6 米，而 6.1 距离基准的「长柄近战」是 3.8 米 | 6.1 / 9.8 | 按 9.8 的 3.6 米实现（就近原则），需确认 |
| Q7 | 11.2 的「横穿 12–16 秒」按 7 米/秒换算得到 84–112 米的场地，与同条的「出生点到中央 5–7 秒」（35–49 米）自相矛盾 | 11.2 | 以**出生点距离**为准建图（它才是战斗节奏的决定因素），场宽约等于两倍出生点距离。见 `data/maps/arena.ts` 文件头 |
| Q8 | 8.2 只说「15 秒内连续同类控制」，未说明这 15 秒从**施加**还是从**结束**算起 | 8.2 | 按「从控制结束时算起」实现。从施加算起会让 8 秒的变形术在结束时窗口只剩 7 秒，递减形同虚设。理由见 `sim/dr.ts` 文件头 |
| Q9 | 4.1 的「角色占屏 4%–8%」与「不能一次看到整张地图」在 60° FOV 下互相冲突：8% 需要镜头距离 21.6 米，而那样 2v2 小地图会被一眼看完 | 4.1 | 当前 `MAX_DISTANCE = 18m`（占屏约 9.6%），优先满足「不能看完整张图」。推导见 [07-client-render-camera.md](07-client-render-camera.md) §1.2。改一个常量即可切换 |
| Q10 | 12.4 说战场聚焦「逐步清除」，但没给清除速率 | 12.4 | 按与叠加同速处理：每 30 秒掉一层（`FOCUS_STACK_INTERVAL`）。★ 关键是**不能按 tick 掉层** —— 20Hz 下 5 层 0.25 秒就没了，那是「立即清除」不是「逐步」。见 `sim/match/flag.ts` 的 `updateBattlefieldFocus` |
| Q11 | 12.5 只说「地下或室内路线狭窄」，没说地道口开在哪 | 12.5 | 井口偏到 x=±22，**不在中线上**。开在 x=0 会在「中央开阔路线」正中间留一个 8 米宽的洞，顺着中央跑的人会直接掉进去（M7 验收时真的掉了）。见 `data/maps/ctf.ts` 的 `TRENCH_X0` |
| Q13 | 15.2 要求目标框显示「主要控制递减」，但没列出具体是哪几种控制 | 15.2 | 显示全部五种（定身/昏迷/沉默/缴械/恐惧），字形复用 `vfx/status.ts` 的同一张表 —— 目标框、队伍框和 3D 场景用同一个字符，玩家不用学三套符号。M8 接线时原本只列了三种，**漏了定身和恐惧** |
| Q12 | 14.2 的视觉语言表有**八**行（含「毒素」），但规格书从未把毒素列为伤害学派 —— 7.2 学派锁定、8.4 免疫都只谈魔法/物理，毒素只出现在驱散类型里 | 14.2 / 7.2 | 分成两层：`School`（7 项，规则层，决定学派锁定与免疫）与 `VisualAttribute`（8 项，表现层，只决定长相）。毒刃学派是 physical 但视觉走毒素。★ **不要**为凑齐 14.2 往 `School` 加 Poison —— 那会凭空产生一条「毒素学派锁定」。见 `client/src/vfx/schools.ts` 文件头 |

| Q13 | 16.x 列出了要统计**什么**，但没给「多近算护送」「多久内算助攻」「多久内的打断落空算被假读条骗到」这类判定口径 | 16.1 / 16.3 | 逐条取值并写明理由，能复用 6.1 距离基准的就复用：护送/防守/通道控制统一取 `RANGE.SHORT` = 12 米（「近距离支援」档）；助攻窗口 10 秒（短于 8.2 的 15 秒递减窗口、长于一次读条）；假读条窗口 2 秒（被骗的打断必须在对方还以为你在读条时按下）。全部集中在 `sim/stats.ts` 的 `STATS` 常量里 |
| Q14 | 17.3 / docs/08 §6 说重连要「限时内」，但没给时长 | 17.3 / 11.5 | 取 90 秒：短于一局竞技场常规时长（6 分钟）不让对手干等半场；长于一次波次复活间隔（12.6 最长 30 秒）所以夺旗里断线回来还赶得上下一波；覆盖家用网络一次重连（DHCP 续约、Wi-Fi 漫游）。★ 这段时间里角色**站在原地被打**（11.5），所以不能太长，否则「断线」会变成送人头的惩罚而不是容错。见 `server/src/room/reconnect.ts` |

---

## 如何更新这张表

1. 完成某条验收后，**先实际验证**（跑测试 / 驱动游戏看到它成立）
2. 把状态改成 ✅，在「验证方式」列写清楚是哪个测试文件或怎么人工验的
3. 更新顶部「汇总」的计数
4. 同步更新 [PROGRESS.md](PROGRESS.md)
