# 技术债总账（唯一活账本）

> **这是全仓库技术债的唯一登记处。** 此前欠账散在四处：PROGRESS 技术债一节、
> 各里程碑章节的「已知不足」、docs/14 的「继承的技术债」、以及约 150 处
> 中文散文注释（「占位值」「欠条」「如实不画」「保守实现」）——
> **散文注释 grep 不出来，散账无法判断优先级**。2026-08-04 的全仓审计
> （四路：客户端 UX / 性能与债 / 服务器公平性 / 表现层对齐）把它们归并到这里。
>
> 与 [10-acceptance-tracking.md](10-acceptance-tracking.md) 的分工：
> **规格偏离**进它的「已知偏差」表、**设计留白**进它的 Q 表；「做了但欠着」的账进本表。
> 一件事最多在两边各有一行，互相引用，不重复叙述。

**创建**：2026-08-04（M16 完成后）
**行号口径**：证据列的行号是审计时点的快照，会漂移；**以文件 + 符号名为准**。

---

## 登记规矩（约束，随 [11-contributing.md](11-contributing.md) §8 检查清单执行）

1. **新债必须落账**：引入任何「先欠着」的取舍（占位值、保守实现、只做一半、
   写了没接线、协议缺字段…）时，在本表对应分类里加一行，领一个 ID。
2. **代码注释挂 ID**：新债在代码注释里写 `// DEBT(A1): 一句话`，从此可 grep。
   存量注释**不回改**（无行为收益、污染 blame）；触碰旧文件时顺手补标。
3. **还清改状态、不删行**：状态改 ✅ 并注明还债的轮次/提交，与 docs/10 偏差表同规矩。
   把已完成记成未完成、把未完成记成已完成同样有害（M16 军械箱之鉴：三处文档
   写着「数据类型都不存在」，实际 M11-6 就做完了）。
4. **决定 ≠ 债**：有意的边界标 🔵 并写理由（或链接），防止后人当成待办去「修」。
5. **PROGRESS 章节照常写叙事**，但每条「已知不足」必须同步在本表落一行 ——
   判断「还欠多少」只看这里。
6. **每个里程碑/批次收尾过一遍表**：状态列刷新，新增的债补登。

**状态图例**：⛔ 未处理 · 🔧 进行中 · ✅ 已清（注明轮次）· 🔵 决定（有意边界，非欠账）· ⚠️ 待复核

**执行计划**见 [16-roadmap-post-m16.md](16-roadmap-post-m16.md) —— 本表管「欠了什么」，计划管「何时还」。

---

## A. 正确性（实现与规则 bug）

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| A1 | **`LeaveMatch` 淘汰绕过死亡漏斗**：`eliminate()` 直改 `alive/health` 字段，不产生 death 事件 → `deaths` 统计不加（**违反 11.5**，且函数上方注释声称在执行 11.5）、`settleDeaths()` 不跑（10.10 临时装备不清）、无 `Death` 广播。零测试覆盖 | `packages/server/src/room/RoomServer.ts` `eliminate()`；修法见 `sim/tick.ts` 第 0 步 `forfeits` | 统计失真 + 规则静默失效 | ✅ 批次一 1.1（2026-08-04，含变异测试） |
| A2 | **无输入实体不做物理积分**：`tickWorld` 第 2 步 `if (!input) continue` → 不积分重力、不软推开。停发 Input 即可空中悬停 + 单向卡位；正确性寄托在「客户端每 tick 都发」的自觉上 | `packages/shared/src/sim/tick.ts` 第 2 步（缺输入现按全零输入积分；「无 movement 条目不参与移动」边界不变） | 可被主动利用（悬空/卡位）；高丢包玩家持续微顿 | ✅ 批次一 1.2（2026-08-04，含变异测试；verify-m10 白盒摆位随之改用 teleportTo） |
| A3 | **无心跳/空闲超时**：半开连接（断电、NAT 超时）永远不被识别为断线 → **不触发人机接管**。偏差 #14「断线瞬间接管」只对优雅关闭的连接成立 | `packages/server/src/index.ts` 心跳（默认 30s ping × 2 次落空 → terminate，走既有 close→disconnect→接管路径） | 最常见的断线形态下队友是十几分钟的活靶 | ✅ 批次一 1.3（2026-08-04，含变异测试） |
| A4 | **人机决策拿全局信息**：`nearestFoe()` 遍历全部实体，不过可见性/视线 → 能感知潜行者精确坐标并据此走位。「故意断线换 AI 代打」附带信息优势 | `nearestFoe` 按 `isVisibleTo`（与快照裁剪/SetTarget 校验同一判定）过滤：人机看得见的 = 真人看得见的；全场只剩潜行者时原地待机（真人同处境，非瞎子）；被发现瞬间照常进候选 | 偏差 #14 论证了不规避统计，未论证不规避信息面 | ✅ P1a（2026-08-05，BotDriver 单测 ×4） |
| A5 | **朝向无转身速率限制**：`intent.facing` 在 `validateCast` 之前被无条件采信，移动的 `characterYaw` 同 → 脚本客户端永远满足朝向门禁（spinbot）；背刺「背后 120°」的转身博弈对作弊者不成立 | `packages/shared/src/sim/tick.ts`（≈:319）；`FORBIDDEN_CLIENT_FIELDS` 不含 yaw/facing | 反作弊边界的口子 | ⛔ |
| A6 | **跨房间 `Reconnect` 造成房间永久泄漏**：`onReconnect` 不查 `session.roomId/phase` 直接覆写 → 旧房间的旧 playerId 永不清理，`dropIfEmpty()` 恒 false | `packages/server/src/room/RoomServer.ts` `onReconnect()` 入口守卫（在房会话诚实拒绝，与 JoinRoom 同规矩） | 资源泄漏（非信息泄漏，playerId 全局唯一） | ✅ 批次一 1.4（2026-08-04，含阳性对照与变异测试） |
| A7 | **`referencedEntities()` fail-open**：`default: return []` → 新增带 EntityId 的服务器消息忘登记时，`redactFor` 原样放行。与 codec 侧 `satisfies never` 的穷尽保证是同一问题的相反做法；零测试 | `packages/server/src/MatchLoop.ts` 穷尽 switch（`satisfies never`，新消息不归类编译不过）+ `MatchLoop.test.ts` 逐类断言 | 防线本身没有防线 | ✅ 批次一 1.5（2026-08-04，编译期变异验证过） |
| A8 | **阶段白名单漏两条 + 房间函数漏守卫**：`MATCH_ONLY` 不含 `OpenArmory`/`ChooseArsenal`（当前被下游 `!sr?.loop` 兜住）；`setReady()` 是唯一没有 `room.started` 守卫的房间变更函数 | `Session.ts` MATCH_ONLY 补两条；`room.ts` `setReady()` 加 started 守卫 | 纵深防御各少一层 | ✅ 批次一 1.6（2026-08-04，断言钉住「阶段」层拒绝理由，双变异验证） |
| A9 | **`Death` 事件对「杀手不可见」的接收者整条丢弃** → 全队收不到死亡反馈，只能靠快照 `alive:false` 推断。偏差 #4 家族残留：`Damage` 已改「发但抹 sourceId」，`Death` 没跟 | `packages/server/src/MatchLoop.ts` `redactFor` default 分支 | 命中/死亡反馈缺失（14.1 同源） | ✅ 用户拍板批 2026-08-10（任务 D）：`redactFor` 给 `Death` 补专门分支 —— 死者可见就发（`killerId` 按凶手可见性抹/留），死者不可见才整条丢。`referencedEntities` 的 killerId 登记降级为兜底、fail-closed 不放宽。新测 `MatchLoop.redact.test.ts` 3 条（潜行 DoT 磨死队友 → 收到死讯且不带凶手 id、字节级无泄露；凶手在场 → 原样带 id；**弃权判死**的潜行者 → 敌方整条收不到、己方照收）。变异验证（去掉分支 → 第 1 条红）；`verify:m10` 16/16 不掉 |
| A10 | **近战 `inRange` 把碰撞体重叠判为超距**（原 PROGRESS 技术债 §8 迁入）：负边距 + `min=0` 检查 → 站进模型里拳击永远 OutOfRange。修时先补「重叠时可施放」单测 | `packages/shared/src/math/geometry.ts` `edgeDistance` 注释 + `inRange()` | 需主动贴脸才触发，低频 | ✅ 用户拍板批 2026-08-10（任务 D）：`inRange` 的下界改成 `minRange <= 0 \|\| d >= minRange` —— 只对**真的有最小距离**的技能生效。冲锋的「太近不能用」语义零改动（靠 `minRange > 0` 表达，重叠时负边距照样被拒）。先红后修：`geometry.test.ts` +4 条（负边距前提／min=0 重叠可施放／冲锋 25-8 与近战 min=1 仍拒／`inMeleeRange` 对照组）、`casting.test.ts` +1 条消费侧（`validateCast` 贴脸重叠 → `Ok` 而非 `OutOfRange`）；变异验证两条都随之变红。**balance 归因（主循环 2026-08-10 实跑）**：胜率八职业逐位不变（极差 81.0pp），二阶指标微动（盗贼场均 16.7→16.3s、DPS 59→60 等 ±1 量级）—— 贴脸重叠可命中让近战缠斗略提速，方向与机制自洽，非回归 |
| A11 | **死亡后客户端照常发输入、技能栏可点**（服务器静默拒绝）：`simulate` 只判 `started` 不判 `alive` | `packages/client/src/input/deathGate.ts`（新）；`NetworkScene.selfAlive()` / `readInput()` / `simulate()` | 无害但脏流量 + 死亡体验混乱（与 W5 联动） | ✅ **用户拍板批 2026-08-10 Wave1.5**。落地：新建 `input/deathGate.ts` 的 `gateInputWhenDead(input, alive)` —— **允许清单**而不是禁止清单（将来新增的动作默认被挡住；写成禁止清单的话，新动作默认是「死人也能发」且没有任何测试会红）。清单里只有观察类：观战 V／镜头复位／Esc 关面板／记分商店／记分板／设置／静音／画质／色盲／界面缩放；移动量与 `selfCastHeld` 归零，其余按键（技能 1–9、Tab 切目标、F 焦点、R 解控、G 交互、B 换武器、Z/X 道具）一律不过闸。`simulate()` 死亡期间**直接不发指令帧**（此前 20Hz 白发一整段死亡时间）—— 安全性来自 `Predictor`：pending 被 ackSeq 清空后每份快照直接重置成权威态，复活那一下位置跳变超 `CORRECTION.SNAP_ABOVE`，走的正是 13.4 瞬移分支。判据是**快照里的 alive**（`selfAlive()`，收不到自己那份时按活着 —— 开局前默认路径逐字节不变），所以复活不需要任何「解除」逻辑。技能栏置灰是**免费的**：`SnapshotCombatView.gateBlocker` 对 `!alive` 早就返回 `CastFailure.Dead`，缺的只是「按下去不发」。三条配套：死后技能格点击一并作废 + `aim.reset()` 收掉举着的落点预览；Esc 仍过闸（要关面板）但不再发 `CancelCast`；数字键读**未过闸**的原始输入，等复活那几秒照常在大乱斗商店买货（P13）。与 W5 死亡遮罩/观战不打架（那边管显示，这边管发不发）。测试 `input/deathGate.test.ts` 8 条（含「活着返回同一个对象」与「复活立刻恢复：闸门无状态」）。**余账**：死亡期间 `seqSentAt` 没有新条目 → W6 的延迟读数停在最后一次测量值上；为了让它继续跳而每帧发一条空指令等于没修这条债，如实留着 |
| A12 | **`antialias` 是死设置且有假绿测试**：档位表声明、`QualityController.apply()` 不消费、两个场景渲染器硬编码 `true`，而 `quality.test.ts` 断言这个无效字段 —— 「测试通过 ≠ 功能存在」的实例 | 字段与断言已删（quality.ts 留注释说明；真按档切 MSAA 随 P9 重建 renderer 时再加回并真消费） | 低端机 low 档拿不到承诺的收益；绿灯说谎 | ✅ 批次一 1.7（2026-08-04） |
| A13 | **切画质丢昼夜 preset**：两处 `env.apply(tier)` 不带 preset 参数，回落 day | `Environment.apply` 记住 `lastOpts`，不带 opts 沿用上次（两处调用点零改动） | 一行级；W15 做完后会显形 | ✅ 批次一 1.7（2026-08-04） |
| A14 | **`SWING_CLIPS` 含不存在的片段名** `Unarmed_Melee_Attack_Punch_A`（任何模型里都没有）；[1] 之后的条目全是死分支 | 死条目已删；按武器类型选片段归 W14 | 死数据误导后人 | ✅ 批次一 1.7（2026-08-04） |
| A15 | **粒子池容量注释过期互相矛盾**：注释写 32 格/40 格，实际事件池 40×48、细流池 48×32 | `ParticleBurst.ts` 活注释改正并指向构造参数为准；PROGRESS 二期章节的 32 格是**当时真实值**，按「历史数字不回填」规矩保留 | 文档说谎 | ✅ 批次一 1.7（2026-08-04） |
| A16 | **施法失败提示优先级**（原 PROGRESS 技术债 §3）：资源检查排在距离前 → 怒气 0 的战士在 30 米外收到「资源不足」。正解是**另加** `describeCastBlockers()` 全量提示，不改门禁顺序 —— 该函数 M11 写完后**零生产消费方**晾了五个里程碑 | `HudSkillSlot.blockers[]`：试验场由 `CombatDirector.skillSlots()` 调 `describeCastBlockers` 填全量，HUD 按「位置→视线→朝向→资源→冷却→状态」取首个 + 四类分级样式（颜色+边框字形双通道）；联网侧补距离/朝向判定（视线/学派锁定要服务器数据，如实注释） | 提示质量（15.2） | ✅ P10（2026-08-07，hud 单测钉六级顺序） |
| A17 | **夺旗时限/加时整条未实现**（W12 探路发现）：`CTF.DURATION`（12/15 分钟）与 `setOvertime()`（加时波次 16s）**零消费方**；规格 6.x「时间到比分高者胜、同分突然死亡加时」没有任何一层在跑 → 双方都不碰旗的联网夺旗**没有自然终点**（得分胜负已通，见 W12）。联网 HUD 因此如实不显示「剩余时间」（15.4 右列的比赛时间一栏空缺 —— 不画到零也不会发生任何事的倒计时，附录A#7）；试验场的 720s 是本地演示钟 | `packages/shared/src/constants/combat.ts` `CTF.DURATION`；`sim/match/respawn.ts` `setOvertime`；胜负出口在 `flag.ts`（照 arena.outcome 的结构补，MatchLoop 只消费） | 拖延战术无终点；15.4 比赛时间缺席 | ✅ **四层全通**（sim + 服务器 + 快照：用户拍板批 2026-08-10 任务 D；HUD 面 + 场景接线：Wave1.5）。落地：`CtfState` 补 `duration/startedAt/overtimeSince/outcome`（`CtfOutcome` 形状照抄 `arena.RoundOutcome`），`flag.ts` 新增 `resolveCtfOutcome`（tickFlags 末尾调，顺序在状态机之后 —— 本 tick 的交旗要先记账）+ `ctfInOvertime` / `ctfTimeRemaining`；`setup.ts` 把 `CTF.DURATION[mode]` 喂进去（查不到落 0 = 不限时）；`tick.ts` 成为 `setOvertime()` 的**第一个消费方**（在 tickRespawn 之前，幂等）；新常量 `CTF.OVERTIME_HARD_CAP=180`（理由同 arena 的 `SUDDEN_DEATH_HARD_CAP`，到点判平局）；`MatchLoop.checkEnd` 改读 `ctf.state.outcome`（不再直接问 `ctfWinner`，否则绕过时限）。**快照新增 `match.timeRemaining` / `match.overtime`**（server→client、无 EntityId，codec 无需改；刻意不复用 `suddenDeath` —— 那个字段会带出 `suddenDeathBlips` 位置揭示，挂到夺旗上等于给潜行旗手点名）。测试：`flag.test.ts` +10 条、`MatchLoop.ctfClock.test.ts` +4 条接线；`verify:m7` 23/23、`verify:m10` 16/16 不掉。**HUD 面**（Wave1.5）：`CtfHudView` 加 `overtime?`，`ctfClockHtml(timeRemaining, overtime)` + `CTF_CLOCK_URGENT_SECONDS=60` 单独导出（加时把同一个数字的含义从「比赛还剩多久」换成「距硬上限还剩多久」，不显示旗子的话玩家会看到倒计时归零后又跳回一个新数，只能理解成 HUD 坏了）。**场景接线**（Wave1.5）：新建 `scenes/sceneViews.ts` 的 `ctfHudViewFromMatch(m, flags, teams)`，`NetworkScene.renderModeHud` 改调它 —— 那段「⚠️ 刻意**不传** timeRemaining（sim 里没有夺旗时限）」的注释**连同判断一起删掉**：它在任务 D 之后就过期了，而注释没有过期机制，留着只会劝阻下一个来接线的人。不限时的一局服务器仍不发这两个字段 → 整行不画，W12 口径一字未改。测试 `scenes/sceneViews.test.ts` 4 条 |
| A18 | **死亡不清光环 —— 形态类 3600 秒光环跨越死亡**（W26 收口批对抗校验发现的既有缺陷，maxHealth 接线让它第一次可见）：`sim/death.ts` 全文无 clearAuras（全仓只有 boss 摘尸与 arena 回合重置清），熊形态光环 duration 3600 跨死 —— 实测置死后光环仍在、maxHealth 仍 1260，波次复活 `e.health = e.maxHealth` 直接给满 1260/1260。W26 之前只泄漏隐形的 damageTaken 0.85，现在连血池一起。WoW 口径死亡掉形态。修法：AuraDef 加 `clearOnDeath` 默认 true，出生保护/复活护盾显式 false；动 sim 死亡结算须先补测试 + balance 归因 | `sim/death.ts` `settleDeaths`；实测记录见 W26 行收口批段 | 带复活的模式（夺旗/FFA）里形态与增益跨死存活 | ⛔ |

## S. 安全与公网部署

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| S1 | **无任何消息速率限制**：`inputQueue` 在 tick 间无上限；`pendingCommands` 无界且逐条执行（1000 条 TabTarget = 一 tick 内 1000 次全实体排序）。单客户端可拖慢同机全部房间 | 入站令牌桶（`Session.handleRaw` 解析**前**判、静默丢弃、持续滥用 terminate）+ 每玩家每 tick 命令数上限（`MatchLoop.enqueue`）。参数集中在 `limits.ts`，人机会话跳过 | **不需要游戏知识即可打穿**的路径 | ✅ 批次三 3.6（2026-08-05，Session.rate 单测 8 + verify:hardening 压测「灌 4000 条/s 他房仍 100% 20Hz」） |
| S2 | **无 `maxPayload`（ws 默认 100MiB）/ 无出站背压（不看 `bufferedAmount`）/ `roomId` 无长度上限**（会被当 Map 键长期持有） | `maxPayload` 16KB（ws 以 1009 关）+ **背压巡检**（`backpressureStrike` 纯函数：连续两个巡检间隔都超阈值才断 —— 瞬时冲高不误杀，白盒同步快进那一幕正是被它救的）+ codec `roomId ≤ 32` | 内存放大 / 慢读者拖爆出站缓冲 | ✅ 批次三 3.6（2026-08-05，背压判定 4 单测 + hardening maxPayload e2e） |
| S3 | **无 origin 校验、无连接/房间/房内人数上限**（观战席无限，每人每 tick 一份完整快照） | Origin 白名单（`verifyClient` 握手层拒绝，env `WOWPVP_ORIGINS`）+ 连接数（1013）/房间数（拒新建）/房内成员数（拒加入）三档上限 | 跨站连接 + 资源放大 | ✅ 批次三 3.6（2026-08-05，hardening e2e 5 条） |
| S4 | **无 TLS/WSS**：客户端硬编码 `ws://`；重连令牌明文过网且**整局 24h 有效** → 同网段嗅探一次即可在受害者断线时接管。令牌本身是 UUIDv4（够强），弱点全在传输层 | `packages/client/src/main.ts`（≈:139）；`room/RoomServer.ts` `TAKEOVER_GRACE_SECONDS`（≈:112） | 会话劫持面；HTTPS 页面下混合内容直接连不上 | ⛔ **归发布前 F3**（要配反向代理，与部署形态绑定） |
| S5 | **tick 循环无异常防护**：`setInterval(pump)` 无 try/catch、无 `uncaughtException`/SIGTERM 处理，而 `assertNoHiddenEntities` 设计上会抛 → 一个 bug = 全服进程崩、带走所有房间 | `MatchLoop.pump` try/catch（**爆炸半径 = 单房间**：出错房间判平局收场，其余照跑）+ 主入口 `uncaughtException`/`unhandledRejection`/SIGTERM/SIGINT（**只在直接运行时装**，import 时不装以免吞测试失败） | 单点全崩 | ✅ 批次三 3.6（2026-08-05，containment 4 单测：advance 抛 → onEnd(draw)、不二次抛、日志可见、onEnd 自身也抛的兜底） |
| S6 | **无监控/结构化日志/健康检查**：全部日志 6 行 console；追帧丢弃（超 5 tick 丢）是静默的，过载不可见 | JSON 行日志 `log.ts`（`onLog` 测试钩子）+ `/healthz`（聚合读数、**不带房间码**）+ tick 耗时/慢 tick/丢帧计数（`MatchLoop.stats`，过载告警 5s 节流）+ 半开/限流/背压/异常关闭全部留痕 | 运维盲区 | ✅ 批次三 3.6（2026-08-05，/healthz e2e + 各防线日志被压测断言） |
| S7 | **光环 id 全量下发泄露隐身攻击者的职业**：`Damage.sourceId` 抹了，但目标身上的 `auraId`（形如 `rogue.rupture`）与 `AuraApplied` 广播直接说出攻击者职业。「抹来源」防线的语义漏点，需拍板口径（与 X3 的 skillId 同题） | **口径：施加者不可见 → auraId 掩成 `HIDDEN_AURA_ID`（"hidden"）、连学派一起藏。** 两条通道都改：`snapshotEntity`（持续快照，按 aura sourceId 判可见性）+ `AuraApplied`（加可空 sourceId，`redactFor` 同款掩码，`referencedEntities` 兜底登记 fail-closed）。客户端所有按 auraId 分派（护盾/控制/化形/复活保护）都不匹配 "hidden"，自然回落中性显示 | 不违反任何验收，但未记录过 | ✅ 批次四（2026-08-05，visibility 掩码单测 ×3 + m10 #1e 字节级） |
| S8 | **`PeerDisconnected`/`PeerEliminated` 全房广播** → 敌方准确知道哪个对手正由 AI 操作（战术信息）。需设计拍板：是否只发给己方 | `room/RoomServer.ts`（≈:176-180、:509） | 与偏差 #14 耦合 | ⛔ |
| S9 | **文档债**：无延迟补偿（判定用服务器当前 tick，高延迟纯吃亏）是合理取舍但 docs/08 **没写**；「20Hz 够用」的论证只覆盖控制时长分辨率，没覆盖输入 50ms 量化对打断窗口的影响 | `docs/08-network-protocol.md` §5 | 取舍未记档 = 将来被当 bug 反复排查 | ⛔ |
| S10 | **W24 观战席是全知席位，而本仓库没有账号系统** —— 观战段给的是两队可见集的**交集**（未被任何敌人发现的潜行者不进，装备一律敌人视图，`self` 段一个字节都不发；★ 「装备一律敌人视图」这半句在 **2026-08-11 W24 收口之前是假的** ——服务器传的是被跟随者而不是 `SPECTATOR`，观战席拿到的是那一队的**队友视图**，按 V 换边还能把双方收齐；修好之后这条缓解论证才成立，S10 的严重度在此之前被低估了），已经比任何一名选手看到的都少；但「场上双方的位置/血量/光环」本身就是转播要给的东西，而**任何人都能开第二个浏览器窗口坐进观战席** ——于是一名选手可以给己方多一个信息源（对手在哪、还剩多少血）。这不是实现漏洞，是「运行中房间可观战」这条产品需求的固有代价 | `shared/net/visibility.ts` `isVisibleToSpectator`；`server/room/RoomServer.ts` `enterAsSpectator` | 竞技强度下的信息不对称（休闲局无感） | ⛔ **本批不做，如实记账**。三条候选缓解，按代价排序：① **转播延迟**（观战段延后 N 秒发 —— 最标准的答案，但要给共享段加一条历史缓冲）；② **只给单队视角的观战**（跟谁就用谁那一队的可见集，把「交集」换成「跟随对象所在队的可见集」，反而更宽 —— 需要重新裁决）；③ 房主开关「本房不可观战」（最小实现，但把功能关掉不算解决）。做之前不要先加协议字段 |

## W. 接线缺口（组件写完了、没人喂 ——「写对了没人调」家族存量）

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| W1 | **联网无队伍框**：`PartyFrame` 已构造、试验场在喂，`NetworkScene` 零调用 → 队友血量看不见，**治疗职业没法玩** | 投影收进 `PartyFrame.ts`（`partyViewOf`/`partyViewFromSnapshot` 共用一份 `partyMemberView`；控制优先级 `controlKindsOf` 顺带把目标框副本收拢）；`NetworkScene.renderParty()` 喂快照 | 极高感知 | ✅ 批次二 2.1（2026-08-04，双源一致性单测 + m13 #11 变异验证） |
| W2 | **联网无小地图**：`Minimap` 零调用；`'supply'`/`'objective'` blip 类型无生产者（M16 已记的军械点图标属此） | `NetworkScene.renderMinimap()` 喂快照（潜行免费继承验收 #5）+ **`suddenDeathBlips` 首个消费方**（8.5 模糊位置，距离启发式压一人两点噪音）。余账：旗帜 blip 已随 W12 清（批次三 3.5）；`supply`/`objective` blip 随军械图标 | 战场态势不可知 | ✅ 批次二 2.3（2026-08-05，m13 #13 像素级变异验证） |
| W3 | **联网无模式 HUD**：`ModeHud.renderCtf` 全仓唯一调用点在试验场；`respawnIn`/`supplyRespawnIn`（12.6 复活波次倒计时）**从未被任何一边喂过** | 竞技场侧批次二 2.2；夺旗侧（renderCtf + respawnIn，快照新字段驱动）批次三 3.5。**余账**：`supplyRespawnIn`（10.4 补给刷新倒计时，数据其实已在快照 `armories.availableAt`，差 renderArena 喂一口） | 比分/旗帜/复活已可见；补给倒计时仍欠 | 🔧 夺旗侧清（批次三 3.5，2026-08-05）；余 supplyRespawnIn |
| W4 | **`ModeHud.renderArena()` 两个场景都零调用** → 8.5 战斗抑制百分比与「决胜阶段」**从未显示给任何玩家** | `NetworkScene.renderModeHud()` 喂快照 `match.dampening/suddenDeath`（M10 起就在每份快照里）；存活数按可见口径（与记分板同规矩，如实不编全知计数）。⚠️ `suddenDeathBlips` 的消费**挪到 W2**（小地图才是它的归宿） | 核心节奏机制不可见 | ✅ 批次二 2.2（2026-08-05，m13 #12 变异验证 + 15.4 否定式断言） |
| W5 | **死亡后无去向 + 观战未接镜头**：无「你已阵亡」遮罩、无复活倒计时；`SpectateController` 规则/测试齐全但 V 键只 `console.info`，镜头从未切换（其 `:90` 注释指向一个不存在的死亡界面）。观战席玩家开局停在房间页（M13 边界）也归此 | 联网侧：死亡遮罩（textContent 防注入）+ V 键轮换（`nextSpectateTarget` 纯函数，服务器 `spectatableFor` 复核）+ 镜头改指被跟随者（`lastRemotePos` 插值同源）+ 被跟随者死亡自动换人。**余账**：观战镜头切换的 E2E 断言待 3 人局景（可用 fillWithBots）；CTF 复活倒计时已随 W12 清（批次三 3.5，verify:w12 #5）；试验场 V 键与观战席开局去向未动 | 「死后 20 秒」是最易流失时刻 | ✅ 联网侧清（批次二 2.4，2026-08-05，m13 #14 rAF 监视器变异验证） |
| W6 | **断线全程静默**：`NetworkScene` 自建连接的 `onClose` 是空实现；大厅只在放弃重试后 toast 一次；约 7.75s 退避重连零提示；全客户端无延迟/连接状态指示 | 断线横幅（纯轮询 `NetLink.connected`，两条入网路径同一份逻辑）+ 延迟指示（Input seq→快照 ackSeq 往返，EMA，含 50ms tick 批处理如实不减）。**顺带修掉 E2E 抓到的真 bug**：重连后服务器不告知恢复的 playerId → 大厅身份错位、赛后重开对重连过的人整个是坏的（重发 Welcome，零新协议） | 网络问题被误判成游戏 bug | ✅ 批次二 2.5（2026-08-05，m13 #15 服务器侧掐线闭环验证） |
| W7 | **键位事实上不可重绑**：`rebind()`/`getBindings()` 全仓零调用方、无持久化；技能栏 `<kbd>` 写死 1-8（接了重绑也会撒谎）。17.2 明文要求可重绑 | `settings/keybindings.ts`（存档 `wowpvp.keybindings.v1` + `rebindWithSwap` 冲突交换/拒绝 + `makeRebindController` 两场景共用）；`SceneShell` 开局 `loadBindings` 进 InputManager；`SettingsPanel` 键位表点行进捕获态、按键即改（含技能九格）；`CombatHud.skillKeyLabel` 读实时绑定不再写死 | 无障碍 + 非 QWERTY 玩家被挡 | ✅ 批次三 3.9（2026-08-05，keybindings 单测 ×14 + m13 #20/#21/#22：重绑往返/冲突检测/技能栏键号同步） |
| W8 | **三个已定义动作不可达**：`Trinket`（R 通用解控，8.3 —— **PVP 核心反制**，且 sim 侧 `UseTrinket` 结算也未接，M10 已知不足迁入）、`SelfCast`（Alt，5.6）、`Skill9`（两处 `SKILL_SLOT_COUNT=8` 截断第 9 格护盾） | R：tick 第 1c 步（`useTrinket` M9 零调用至今首条真实调用链；按下即进冷却保博弈；昏迷中可用）+ 协议路由 + 双场景 R 键与冷却预检 + `AuraRemoved` reason 加 'trinket'。Alt：`FrameInput.selfCastHeld`，双场景只对 Ally/Any 技能改写目标。**Skill9 系误报**：试验场四期已是 9 格，联网 8 格是快照技能栏设计口径 —— 登记时未核实，如实更正 | 被控只能干看着 | ✅ 批次二 2.7（2026-08-05，tick 四断言 + m13 #17 昏迷中解控 + 变异验证；Alt 的 E2E 待奶职业局景） |
| W9 | **无设置面板**：音量四通道（`setVolumes()` 零调用）、画质（F2 盲切、反馈在 console）、9 项无障碍（F3/F4 仅试验场响应，6 项在联网完全无法触达）——**数据层与持久化全部就绪，只差一个面板** | `settings/SettingsPanel.ts`（F10/大厅按钮；音量四通道 `setVolumes` 首个调用方、画质走 F2 同链、九项无障碍走各场景 `setAccessibility` 唯一入口、键位只读表 `getBindings()` 首个消费方）。面板不持状态。**余账**：键位重绑 UI 归 W7 | 投入产出比最高的单项 | ✅ 批次二 2.6（2026-08-05，m13 #16 落盘往返 + 变异验证） |
| W10 | **大厅路径的对局不装配键位帮助/状态面板**：`#help`/`#stats` 只在无参路径挂；`?net=` 老路帮助文案停在「M1 试验场」且 `#stats` 无人绘制 | 场景自带 `#net-hint` 一行提示（指向 F10 完整键位表，W9 是骨干）+ 延迟标签补帧率显示；`?net=` 撤掉误导的试验场帮助与空 #stats —— 两条入网路径同一份体验 | 新手主路径恰好是唯一没提示的 | ✅ 批次二 2.8（2026-08-05，m13 #18）；P10 起 `#net-hint` 迁至两场景共用的 SceneShell `#hint-bar`（m13 #18 断言随迁） |
| W11 | **教学↔联网断链**：毕业文案「去大厅找真人过招」但无回大厅按钮；大厅从不读 `wowpvp.tutorial.v1` | 毕业页「去大厅」按钮；大厅标题页按存档（done 含 graduate）区分「推荐先玩·尚未完成 / 已完成✓可重温」；存储键挪 steps.ts（轻模块）防大厅 chunk 拖重 | 转化漏斗断口 | ✅ 批次二 2.12（2026-08-05，m15 23/23 回归） |
| W12 | **夺旗联网线未通**：大厅无模式选择（预设仅经典/武装竞技场）→ 联网夺旗**无入口**；即便进入，`NetworkScene` 无 `FlagMarkers` import、无旗手 blip、无 CTF 面板 —— M7 整套规则 + M9 35 条验收只活在 sim 与试验场演示里 | 协议 `SetRoomMode` + sim `setMode()`（换图/换档/超编拒绝）+ 大厅模式行 + `NetworkScene` 接 FlagMarkers/renderCtf/旗帜 blip/死亡遮罩倒计时/FlagEvent 日志 + 快照补 `scoreToWin/focusStacks/respawnIn`。消费侧前夜抓出三真 bug（ctfWinner 零调用、enqueueRespawn 零调用、复活不写 movement）+ 墓地 yaw 反向，详见 PROGRESS 3.5 章 | 一整个游戏模式联网不可玩 | ✅ 批次三 3.5（2026-08-05，`verify:w12` 11/11 + 13 条新单测；时限/加时缺口另立 A17） |
| W13 | **音频接线欠账**：BGM 战斗切换未做（`lastCombatAt` 数据在、audio 层零引用；19 首曲子只播 `combat_1`）；10 个 `amb_*` 环境音零使用；脚步只有 `foot_stone` 单材质；联网无脚步/跳跃/落地/驱散/位移音 | **BGM 半已清**（批次二 2.9）：`MusicDirector`（试验场读 sim 权威 `lastCombatAt`、联网按可见 Damage/Heal，脱战 8 秒滞后）+ 每图氛围曲表 + `playMusic` 交叉淡化。**余账**：`amb_*` 环境音（需 AudioManager 加环境循环通道）、脚步材质、联网脚步/跳落/驱散/位移音 | 氛围与反馈缺层（速赢清单项已销） | 🔧 BGM 清（2026-08-05，+4 单测）；余账在册 |
| W14 | **8 个动画片段零调用**（`Spellcast_Raise`/`Spellcast_Shoot`/`2H_Ranged_Shoot`/`Block`/`Dualwield_Melee_Attack_Chop`/`Lie_Idle`/`Sit×2`）；**跑动中施法无上半身表现**（`applyClip` 单片段全身淡化，只有 Idle 才播施法姿态，无骨骼分层/additive） | **上半身叠加分层已交付**（`entity/animLayer.ts`：脊柱子树遮罩 + `makeClipAdditive`；`CharacterView.buildCastLayer`/`setCasting` 叠加权重淡入淡出，腿照跑手施法；骨架无脊柱时安全回落旧行为）。核心算法 6+2 单测（用**真骨架名** hips/spine/chest/upperarm.r/upperleg.l，从 GLB 逐一核对）；art=on 无运行时错误（m12 #12d）。**X10 真机轮（2026-08-09）抓出叠加层参考帧 bug 并已修**：`makeClipAdditive` 不传参考 clip 时以片段自己的第 0 帧为参考，而 Spellcasting 是循环片段、第 0 帧就是施法姿态 —— 增量 ≈ 0，层在播但**什么都看不见**（用户实测「施法没动作」的根因；旧单测的合成 clip 恰好第 0 帧是单位四元数，测不出）。现以遮罩后的 Idle 为参考，补「第 0 帧非静止」回归用例 ×2。`Block` 已在 P6 接进招架/格挡（本行此前漏记）。**Wave1.5 任务 E（2026-08-10，用户实测「近战只会用手攻击」「法系施法没有任何动作」）再接四个片段**：① `entity/weaponAnim.ts` 按武器选出手片段 —— 双持→`Dualwield_Melee_Attack_Chop`、双手/长柄→`2H_Melee_Attack_Chop`、弓弩→`2H_Ranged_Shoot`、法杖魔杖→`Spellcast_Shoot`、单手→1H 家族奇偶交替。★ 根因不是「没写候选表」而是**旧表的第一项永远命中**（八个模型 22 片段齐全），所有武器共用一个单手斜劈；★ `handedness` 一个字段不够用（弓与魔杖同为 `'ranged'`），判据改成「远程 + 只挂左手 = 弓弩」。挥砍倍速按 `swingInterval` 归一（双持匕首 0.7 秒一刀而片段长 1.27 秒，此前每刀都被下一刀 reset，只看得见抬手）。② `setCasting(true)` → `Spellcast_Raise` 起手（站立时；跑动仍交叠加层，不还回 W14 的「腿照跑」），新 API `playCastRelease()` → `Spellcast_Shoot` 释放。③ **站立读条改回全身 `Spellcasting`**：X10 修好参考帧后叠加层确实在生效，但 `Spellcasting` 相对 `Idle` 的增量本就细微，站着不动时叠出来只是微微前倾 —— 用户说的「没有任何动作」是这个，站立时腿无事可做，全身播才看得见（叠加层同时收掉，免得同一段姿态叠两遍）。顺带：四份逐字重复的「一次性覆盖动作」合并成 `playOverride/endOverride`；`WEAPON_MODEL` 补上唯一漏配的 `boss.molten_maul`，全覆盖门禁从 `ALL_WEAPONS`（漏 BOSS）改扫 `WEAPON_BY_ID`。新增 `weaponAnim.test.ts`（40，含**逐件武器**的风格断言 —— 分支覆盖证明不了「匕首这件武器被认成双持」）+ `CharacterView.test.ts`（23，CharacterView 此前零单测）。**余账**：`Lie_Idle`/`Sit×2` 三个片段仍零调用（没有坐/躺的游戏状态可接，不是接线问题）；`playCastRelease()` 的场景触发点**已接**（Wave1.5 收口：两场景的「施法结算」分支各加一条 `else if` —— 与近战挥砍**互斥**，两者共用同一条一次性覆盖通道，都调的话后者会取消前者，近战再补一记推掌就是抽搐；瞬发技能不走 `setCasting`，这一下是它们唯一的施法表现）；真机观感截图仍待 | 每分钟都发生的表现缺失；工作量大 | 🔧 批次三 3.8 + X10 真机轮修参考帧（2026-08-09）+ Wave1.5 任务 E 武器/施法片段接活（2026-08-10）；余 Lie/Sit 与截图 |
| W15 | **昼夜 preset 全闲置**：5 个 HDR preset 只用 `day`，`MapDef` 无 preset 字段（速赢清单「每张图配一个」连数据入口都没开）；`EnvironmentOptions.sky` 从未被传 false | `MapDef.envPreset`（纯表现字段）+ 每图配档（试验场 day 红线不动 / 教学 dawn / 竞技场 dusk·day·overcast / 夺旗 dawn）+ `presetOf` 校验回落 + 双端消费；防拼错测试钉住每图的值。`sky:false` 仍无消费者（无室内图，如实留） | 四张图长得一样 | ✅ 批次二 2.10（2026-08-05，速赢清单销账） |
| W16 | **复活保护无渲染器**：`spawnProtection` 是 14.4 essential 八项里唯一「保证不被隐藏、但从来没被画过」的角色 | `StatusMarkers` 金色地环+柔光柱（纯程序化，`?art=off` 同构造）；双端按光环 id `system.spawnProtection` 检测（快照 auras 全公开，与化形同通道）。organically 要等 W12 的夺旗复活波次，白盒断言先钉住 | 保护期不可见 → 玩家误判 | ✅ 批次二 2.11（2026-08-05，单测 ×2 + m13 #19 白盒变异验证） |
| W17 | **协议缺 `Damage.avoided`**：联网侧闪避/招架/格挡无区分 → 规避三态特效与音全缺（M12 已知不足迁入，「如实地少一层」） | `Damage` 加 `avoided?`（sim 早有，只差下发）；`pushEvent` 转发；`NetworkScene` 传给 `HitFeedback.onHit`（闪避/招架/格挡浮字 + 音，其消费早有单测）。规避是被攻击者信息，无泄露争议 | 一笔协议债，照 M10 规矩还 | ✅ 批次四（2026-08-05，codec 往返 + HitFeedback avoided 单测） |
| W18 | **待复核**：他人姓名板施法条在联网侧是否有数据源（M10 已知不足记「快照无他人施法状态」；特效二期接了 `CastStarted` 事件流后 HUD 侧是否跟上未核实） | **复核结论：数据源已在，销账。** `CastStarted` → `SnapshotCombatView.beginCast()` 注册表 → `castOf(e)`，被 `renderUnitFrame`（目标/焦点框）**与** `renderNameplates`（`.np-cast`）双双消费。M10 缺口早由特效二期事件流补上 | 复核后要么销账要么转正 | ✅ 批次四复核销账（2026-08-05，m13 #23：B 通过事件流看到 A 施法） |
| W19 | **鼠标指向（mouseover）施法整条链路是死的**（P10 审计）：`targets.mouseover` 全仓声明三处、**零赋值**；`allowMouseover` 生产代码零调用 —— 5.6 写明的「治疗/驱散/保护支持鼠标指向施法」在游戏里不存在，悬停只换光标图案 | `sim/entity.ts:93` 声明；`targeting.ts:275` 读；两场景 mousemove 均不写。修法：hover 命中写 `targets.mouseover` + 治疗/驱散/保护类技能标 `allowMouseover` | 治疗手感（队伍框可点后优先级降低） | ⛔ |
| W20 | **联网硬目标不从快照回读**：服务器拒绝 `SetTarget`（不可选/超距）后本地仍显示选中态 —— P10 给超距补了 `Rejected` 回话，但目标框的乐观显示没有回滚。focusId 已走「快照回读」口径（P10），hardTargetId 没跟 | `net/SnapshotCombatView.ts`；修法与 `EntitySnapshot.focusId` 同手法加 `hardTargetId` | 拒绝后 UI 与服务器不一致（低频：姓名板 45m 剔除后常规路径撞不到） | ⛔ |
| W21 | **协议无 Swing 消息 → 联网白字没有挥砍动画**：服务器 `tickSwings` 的结果只进伤害结算，无 Swing 广播 | 用户拍板走**便宜路**（2026-08-09）：客户端从 `skillId === 'autoAttack'` 的 Damage 消息反推播挥砍 + 破空声（NetworkScene Damage 分支），零协议改动。**口径如实记**：落空的挥击没有动作（协议里没有那一拍）—— 命中率高时几乎无感；将来若要完全忠实再立正式 Swing 广播（须过 S7 抹除口径 + P11 带宽评估） | 联网白字打得响看不见 | ✅ X10 真机轮（2026-08-09，便宜路） |
| W22 | **TestbedScene 实体渲染循环无惰性 view 创建**：假人 view 只在构造函数里建一次，中途新增的实体永远没有模型 | 渲染循环改为与 NetworkScene 同写法的惰性创建（CharacterView + AnimationController + StatusMarkers 一次补齐） | 现有模式撞不到，埋雷 | ✅ X10 真机轮（2026-08-09） |
| W23 | **直接目标法术的伤害在读条结束瞬间落账，弹道只是表现**：霜矢等全部 Direct 法术走即时 `damage`，客户端的弹道飞行是纯装饰 —— 用户实测点名「法术还没到，伤害就出来了，应该命中后才出伤害」。sim 的**锁定投射物**模块（`projectile.ts`：创建即确认命中资格、到达才结算、走位躲不掉 —— 恰是 6.6 的语义）一直存在但**没有 effect kind 能表达它**（`spawnHoming()` 自 M4 起零生产调用方） | **已交付**（用户拍板 2026-08-10，Wave2）。**schema**：`EffectDef` 加 `{ kind: 'lockedProjectile'; speed; onHit }`，处理器落在 `sim/effects/displacement.ts`（与 `spawnProjectile` 同一处），结算走 `tickWorld` 第 5 步的**同一条** `resolve()` —— 暴击、`damage.skillId`（X3）、S7 来源抹除、统计折叠、击杀归账、`applyControl` 从 skillId 反查学派，一条都不旁路。**迁移口径**（拍板原则「客户端画了弹道的技能必须到达才结算」）：Direct + `TargetFilter.Enemy` + `range.max ≥ 8` + 学派非 physical + 载荷含伤害或控制，**共 21 个技能**（法师 5、牧师 4、德鲁伊 5、圣骑士 3、死骑 2、猎人 2）。速度统一 **55 m/s**，住在 `constants/combat.ts` 的 `SPELL_PROJECTILE.SPEED` —— 客户端 `BOLT_SPEED` 直接 import 它，两边不可能各写一个数。**★ W25 收尾（2026-08-11）：这句话的后半句仍然成立，前半句不再成立。**「统一 55」是 W23 只迁法术时的巧合，W25 迁物理远程后速度按**投出去的是什么**分两档（`SPELL_PROJECTILE.ARROW_SPEED` = 75 的弓弩箭矢 / `SPEED` = 55 的法术与投掷物），每技能写在各自的 `lockedProjectile.speed` 里。**单一来源的纪律靠换一种方式保住**：客户端不再 import 一个常量，而是从**技能数据**读 `lockedProjectile.speed`（`SpellVfx.boltSpeedOf`，按技能 id memo 一次，查不到回落 `BOLT_SPEED`）—— 数字仍然只有 `constants/combat.ts` 一处定义，sim 与视觉仍是同公式同参。由此产生的**同屏两种时序**（猎人的箭 35 米 0.47 秒到、法师的火球同距离 0.64 秒到）自此是**设计**不是 bug：sim 与视觉一起早到，玩家看到的仍然是「箭到了才掉血」。**目标指向的效果全进 `onHit`**（伤害、DoT、减速、CC）；**施法者自身的效果留在弹体外**（圣光弹的圣能射出即入池）。**逐类排除，如实记档**：① 打断技能（含 `interrupt`）—— 断法迟到 0.5 秒等于没断，打断的是「正在读的那条」；② 治疗/友方法术 —— WoW 口径瞬时，客户端也不画治疗弹道，迁了只是凭空给奶量加延迟；③ 近战（<8 米）；④ SelfCenter/地面（后者已是 `delayedImpact`）；⑤ **物理学派远程**（猎人瞄准射击/震慑箭/反制射击/穿透重弩箭、盗贼投掷）—— 猎人是全花名册最脆的一档，箭矢延迟单独一批，**已立 W25**（★ **W25 已于 2026-08-11 交付：4 迁 3 不迁**；这条排除随之作废，口径里的「学派非 physical」已删 —— 详见 W25 行）；⑥ **目标指向的位移**（死骑的死亡之握）—— 载荷是 `pullTarget`，既不是伤害也不是控制 kind，广度锁的 `isCombatPayload` 把它**静默**放行了（对抗校验查出，补记）。它是暗影学派、20 米、单体 Direct，`flies()` 三条全中 → 客户端**正在画**一条飞过去的锁链，而拉拽在读条结束那一瞬就完成 —— 与用户原话是同一个割裂，只是载荷是位移不是伤害。WoW 口径本身是「链子飞到才拽」，所以这条**该迁**、只是不在本批；同族的暗影步/冲锋是**自身**位移（飞的是自己），与它不同族。**bot 必修（结构红线）**：`botController` 的 `hasDamage` / `burstDamageOf` / `ccCategoryOf` 全是顶层扫描出身，不下探 `onHit` 的话法系 bot 直接哑火、控制键全不认 —— 三处都补了递归，`botController.test.ts` 新增「迁移前后估值逐位不变」组（把 `onHit` 摊回顶层做等价比对）钉死。同族的顶层扫描一并修：`skillIcon.flattenEffects`（否则纯输出技能显示成盾牌图标）、`phases.isProjectile/appliesAura`、`schools.isPoisonSkill`（毒蛇钉刺会掉回自然色）、`scripts/skill-audit`（四条瞬发控制会从★★掉档）、`scripts/gen-docs`。**表现层**：装饰弹道保留为视觉载体（它带着冰矛形态与 P3 技能签名），寿命改成**释放瞬间距离 / 速度**、与 sim 的 `impactAt` 同公式 —— 用户抱怨的那个错拍在这里收口；快照路径 `syncProjectiles` 对 `kind === 'homing'` 让位给装饰弹道，两条路二选一，不双重渲染（试验场本地 sim 走同一函数，同样让位）。**协议零改动** —— `ProjectileSnapshot` 自 M12 就带 homing 形状且不含实体引用。**测试**：`sim/lockedProjectile.test.ts` 24 条（广度锁 + 结算同路 + 六个飞行边界）、`vfx/lockedProjectileVfx.test.ts` 14 条（不双重渲染 + 抵达时刻 + 快照兜底 + 命中表现不翻倍，均先做变异验证）、bot 等价 6 条；`skillSmoke` 夹具补有界排空循环。**对抗校验收口（同批，2026-08-10）**：① `SpellVfx` 的 `auraApplied` 分支是**第六处**同族顶层扫描，漏改会让霜矢/裁决/月火在命中帧画两遍爆发（damage 一次 + auraApplied 一次）—— 抽 `dealsDamage()` 递归，`onCast` 那处同款扫描一并换；② 装饰弹道的抵达判据此前是「几何追上**或**到寿命」两者取先，只钳迟到不钳早到 —— 目标被冲锋/死亡之握拉近时视觉早于结算最多 8 个 tick，改成**只看时间**、飞行段按剩余时间配速（顺带消掉「到点还差 0.46 米被强制抵达」的位置跳变）；③ 抵达计时从「累计 dt」改成**绝对钟 `ctx.now`** —— `frame()` 收到的是被顿帧缩放的渲染 dt，一次暴击顿帧就让弹体迟到 88ms，而伤害在真实钟上准时；④ homing 快照从**无条件**跳过改成「有装饰弹道才让位，没有就兜底渲染」—— 施法者不可见时服务器会抹掉 `casterId`，重连/中途加入/观战根本收不到 CastResolved，无条件跳过等于场上一个像素都没有，重新踩回 14.4「不能隐藏投射物主体」（W24 观战开工前必须已经是这个形状）；⑤ `resetRound` 补清 `ProjectileStore`（`ArenaDeps` 加 `projectiles`）—— 上一回合在飞的弹体会打到新回合满血的人，原测试「弹体随对局作废」只断言了新建的空仓，是同义反复；`nextId` 刻意**不**归零（客户端按 id 认弹体）；⑥ bot 等价夹具改从全花名册筛 —— 手写的六个职业名漏掉圣骑士与死骑，21 个迁移技能只覆盖 16 个，漏的正是制裁之锤/扼喉。全量 2289 → 2324 全绿；收口一批实跑 shared 侧 6 文件 315 条 + client 侧 `vfx/` 与 `hud/skillIcon` 10 文件 237 条，**全绿**（全量重算待主循环）。★ 顺手修掉 `lockedProjectile.test.ts` 的三处 typecheck 错（`createArsenalStore()` 缺参、`p.impactAt` 未收窄）—— 这批交付时 `tsc -p packages/shared/tsconfig.test.json` 是红的 | **balance 已归因（主循环 2026-08-10 实跑，种子 1 前后对比）**：极差 81.0→**78.6pp**（收窄）。逐职业：priest 90.5→92.9 / paladin 78.6→**85.7**（+7.1）/ druid 71.4→71.4 / warrior 42.9→42.9 / deathknight 50.0→**40.5**（−9.5）/ rogue 42.9→**35.7**（−7.2）/ mage 14.3→16.7 / hunter 9.5→14.3。方向自洽：掉得最狠的两个正是**远程控制被迁移或依赖对抗被迁法术的近战** —— 死骑的扼喉/寒缚链现在要飞 0.36~0.55s，替补打断到得比读条结束还晚（正是下条「第二嫌疑人」的机械后果）；两个最弱职业（法师/猎人）反而上行，极差收窄。按 M14 规矩：先修根因（bot 时机窗算飞行时间，单独一批归因）再谈数值。已知的方向性风险：法系职业的爆发窗口整体后移 ~0.2–0.6 秒，近战贴脸期收益相对上升。**第二嫌疑人已实验并证伪（2026-08-10 同日，实现→实测→回滚，P1b 风筝同款结局）**：按「替补打断要算飞行时间」实现了 `remaining > flight + 0.1` 的门，种子 1 实测**法师 16.7→9.5pp、极差 78.6→83.3，死骑（原嫌疑对象）分毫未动** —— 假设错在「迟到的控制不是白丢」：晚半秒落地的变形照样吃满持续时间，拦掉它 bot 只会改放伤害填充，净减控制覆盖率。已回滚，`botController.test.ts` 的 W23b 反向测试钉住「不拦」；实验全文落在 CC 步骤的代码注释。**死骑 −9.5 的真根因仍待诊断**（候选：扼喉/寒缚链到达延迟压缩了「先控再退」窗口与 DoT 起跳 —— 属 B1 家族，与真人平衡轮 F5 合并看）。原嫌疑描述存档：bot 拿 CC 当替补打断的时机窗**没算飞行时间** —— `botController` 判 `remaining > 0.1` 就丢控制，而 poly 0.55s / 扼喉 0.36s / 纠缠 0.55s / 制裁之锤 0.18s 现在都要飞，`remaining` 落在 (0.1, 飞行时间) 区间时 CC 到得比读条结束还晚，白丢一个 GCD 还白吃一次 DR 计数；同族第二处是 kiting 门读 `stunned/rooted/feared` 旗子，这些旗子比施法晚 0.2–0.6 秒才亮，「先控再退」的窗口相应缩短。修法（未做）：判据改成 `remaining > 距离 / lockedProjectile.speed + 0.1`，无弹体的技能 flight=0 行为逐位不变。**另三条 note 如实记档**：① `ProjectileSnapshot` 注释自称「不带实体引用所以天然不泄露」对 **homing 不成立** —— homing 每 tick 推向目标当前位置，其位置序列**就是**目标的位置序列，目标飞行途中进潜行时会经共享段广播出去（窗口 ≤0.64s，抵达时 `dealDamage` 会破隐，`verify:m10` 16/16 仍过，因为它查的是实体 id）；② 施法者被**移出世界**（不是死亡）时整组 onHit 静默丢弃，与「施法者死了照样结算」是两条不同的边界（当前全仓只有 BOSS 死亡会 `entities.delete`，而 BOSS 两个技能都没迁移）；③ 攻击破隐挂在 `dealDamage` 上，迁移技能的施法者在整个飞行段仍隐身（纯控制/纯 DoT 的迁移技能则永远不破自己的隐 —— 这条 W23 之前就有）；盗贼无迁移技能，`borrowClassKit` 是唯一可能的组合。**排除理由②的事实修正**：「客户端也不画治疗弹道」**与代码不符** —— `flies()` 判据里没有 targetFilter 这一项，14 个友方技能（闪电治疗、真言术盾、自由祝福…）今天就在画弹道，30 米的那发 0.545 秒才到。排除治疗**仍然成立**，但真实理由是「给奶量加半秒延迟比视觉早到更伤玩法」，不是「没有观感矛盾」 | 施法反馈的真实感 | ✅ Wave2（2026-08-10，21 技能迁移；balance 归因待主循环） |
| W24 | **运行中的房间不可观战、不可中途加入**：对局开始后房间从列表可见但进不去，观战席玩家也收不到 MatchStart（既有账）。用户拍板（2026-08-10）：「运行中房间可以观战，也可以中途加入（根据房间当前队伍情况，比如红队满了，那就只能加入蓝队，可以自己选择职业）」 | **已交付 —— 服务器/共享层半边**（2026-08-11，Wave5）。**① 观战可见性的口径裁决（本批最重要的一条）**：主循环给的说法是「两队可见集的**并集**」，实装取的是**交集** —— 字面并集会把**潜行者自己那一队**的可见性算进来（红队永远看得见自己的盗贼），于是任何人开第二个窗口坐进观战席就能给全场潜行者点名，那是换了个入口的 docs/08 §4.1 透视。判据落成一行：`isVisibleToSpectator = 不是「尚未被任何敌人发现的潜行者」`（12.2 旗手照旧优先可见）。它等价于「至少一个敌方观察者看得见他」，因为 `stealthRevealed` 是**全局**标志而不是按队记录（`isHiddenFromViewer` 的注释原话：己方永远看得见自己人潜行，「被发现」就等于「被敌方那队发现」）—— 所以不需要 O(N²) 两两扫，一个布尔就是那个并集。**安全侧的副产品：观战段 ⊆ 任意一队的可见集**（严格更少），`assertNoHiddenEntities` 因此对观战段照抛不误。**② 架构**：观战段是 P11 共享段的**第三个键**（`sharedByTeam: Map<TeamId | SPECTATOR>`），**惰性构建** —— 零观战者的房间里那个键从不创建，既有对局零成本。`SnapshotAudience = CombatEntity | SPECTATOR` 这个可辨识联合贯穿 `buildSnapshot` / `snapshotEntity`（光环掩码）/ `equipmentViewFor` / `assertNoHiddenEntities` / `MatchLoop.redactFor`（事件流裁剪 —— 没有它观战画面上零伤害数字零死讯）。**③ 观战席拿到什么**：实体段（交集）+ 事件流（同一套 redact）+ EntityMeta；**装备一律敌人视图**（他不是任何人的队友，10.6 对他没有豁免）；**`self` 段一个字节都不发** —— 里面是冷却/GCD/焦点，而 §4.3「敌方技能冷却不发」是一条**裁剪规则**，用产品功能把它挖穿（且是开第二个窗口就能用的那种）不划算；观战者没有身体、按不出技能，本来也不需要冷却条。`you` = 当前跟随对象，一个可跟的都没有时是 `NO_ENTITY`（0 哨兵，实体 id 从 1 起）。跟随判据**另起一条** `isLegalSpectateFollow`（没有「己方」，换成「进得了观战段」）—— 与 11.4 死亡观战那条刻意不复用。**④ 中途加入两条路，顺序有语义**：队伍**还坐得下** → `admitToMatch` 新建实体，职业**当场生效**（新角色，没有便宜可占；大乱斗永远走这条，独立队号 + 新队号登记复活出口 —— 不登记的话他一死就在 (0,0,0) 复活，而零向量在任何图上都不报错）；队伍满了但坐着**补位人机** → 顶替（接过那个席位的 playerId，与 `onReconnect` 换回原 id 逐字同构，实体/统计/令牌/名单条目一张表都不用搬），当场沿用被顶替者的职业，选的那个登记进 `pendingRespec`，在**「死 → 活」的跳变**那一刻兑现（`respecCombatant`）；两条都不成 → **诚实拒绝并点名另一队还有几个**（「红队满了那就只能加入蓝队」这句话得由服务器说得出来）。**⚠️ 只顶替 `fill` 人机，绝不顶替 `disconnect` 接管的席位** —— 那是某个真人的角色，他攥着重连令牌随时回来；BOSS 席位靠「不在 `room.players` 里」被同一道筛排除（不用 `boss#` 前缀那种字符串约定）。**⑤ 为什么换职业要延后**：活着换 = 满血 + 满资源 + 冷却清空 + 光环全清，而这条路径**可以被反复触发**（下一个观战者再顶一次）—— 一个免费复活的经济漏洞。兑现点用**状态跳变**而不是监听复活事件：复活有两个出口（夺旗/大乱斗的 `tickRespawn`、竞技场的 `resetRound`），监听事件要监听两处，漏一处的表现是「换了职业的人永远换不过来」；额外用本 tick 的 `death` 事件补一次 arm，堵住「同一 tick 内死亡又复活」那一拍。**⑥ 协议（最小改动面，全部加法）**：新增 `JoinOngoing{team:'red'\|'blue', classId}`（**SPECTATE_ONLY** 阶段白名单；刻意**没有** `seat:'spectator'` 取值 —— 进观战席就是对 `started` 房发 `JoinRoom`，留一个什么都不做的取值等于留一条死分支；反方向也不给：中途退出走 `LeaveMatch` 按 11.5 淘汰，不能靠改席位规避死亡统计）；`MatchStart += spectating?`（客户端凭它知道 `you` 不是自己操控的角色 —— 不区分的话观战者会对着别人的角色做本地预测并发 Input）；`RoomPlayerView += bot?`（判据是「有没有一条人机会话」，不是名字前缀）；`RoomList` 行 `+= joinableSeats`（**与 `capacity - players` 不等价**：开局后队伍被人机补满，差值恒 0，真正坐得下的正是那些人机席位 —— 没有这个字段大厅只能画出「已开始·满员」，中途加入在 UI 上不存在）。`Session` 加**第四个阶段** `Spectate` + 三张白名单（`MATCH_ONLY` / `MATCH_OR_SPECTATE`（只有 `SpectateFollow`）/ `SPECTATE_ONLY`），MATCH_ONLY 逐条过完的结论：需要身体的一条都不给，`SetTarget` 尤其（它是验收 #5 的探测通道，放行等于白送一个不需要身体的可见性探针）。**⑦ 观战席的生命周期口径**：断线**不登记人机接管**（没有席位可开）、**不广播 PeerDisconnected**（他不是任何人的队友），但**登记重连宽限**（同一个令牌回来仍是观战席）；宽限到期走 `eliminate`，那里对没有身体的人是「从名单里删掉」而不是弃权判死（他没有可规避的死亡统计）。`humanSessionCount` **照数观战者** —— 「有观众就不是无人房间」，P6 的回收计时器因此不会在观战期间开火（有意，非疏漏）。**⑧ 顺带修的一处静默**：P11 的 EntityMeta 补发判据从「只盯 `maxHealth`」扩成**整块静态块的指纹**（`staticsFingerprint`）—— 顶替后改名、换职业后 classId/maxResources 全变，只比血量的话「换成同样血量的职业」一个字节都不重发，客户端会顶着旧职业图标和旧名字画一个新职业的人（与 W26 那次同一种静默）。**⑨ 如实记档的三笔**：**(a) 竞技场默认单回合制**（`roundsToWin: 1`，服务器从不调 `resetRound`）→「下一次复活」在一局竞技场里**不会到来**，顶替人机的玩家整局用被顶替者的职业 —— 客户端文案必须照实说，不能写「下回合生效」然后没有下回合；**(b) 统计行保留累计值**（统计按实体记、实体没换），所以被顶替席位在结算面板上的伤害含人机打的那部分，只有 classId/name 跟着换；**(c) 观战席是全知席位**（两队位置/血量/光环都给），本仓库没有账号系统，**开第二个窗口坐观战席 = 己方多一个信息源**——真正的缓解手段是转播延迟或「只给单队视角」的观战模式，登记为 W27，本批不做。**⑩ 客户端半边（2026-08-11 同日交付）**：**(a) 观战场景**——`NetworkScene` 新增 `spectating` 态（构造参数 + `MatchStart.spectating` 双份，前者只为「不要把一具站在原点的胶囊加进场景」，消息仍是权威）：不建 `Predictor`、不发 `Input`（`simulate` 第一句显式闸，**不靠**「没有预测器所以自然返回」那种碰巧无害）、`selfId = null`（于是记分板高亮/死亡回顾/队伍框/最近敌人**一致地**答「没有我」，而不是错答成被跟随者）。**(b) 跟随目标以快照为准**：`Snapshot.you` 每 tick 都可能换人（服务器在被跟随者死亡/遁形时自动退回 `spectatableForSpectator` 的第一个），客户端因此**不维护**自己的跟随记账 —— 与 A11「复活只有服务器说了算」同一条哲学；`you === NO_ENTITY`（0）时**保持上一帧镜头**并显示「暂无可观战目标」，不去查实体 0。**(c) 镜头不分叉（G4）**：死亡观战与席位观战共用 `spectateCameraTarget()` + `updateCamera()`，全场只剩**一处** `cam.update(`，并由一条计数断言钉住 —— 写第二份的那一半迟早会先长出一个 `position` 参数，而那就是 11.4 禁止的自由镜头。**(d) HUD 观战面**：技能栏/自身施法条/自身光环行三块收在 `CombatHud.setSpectating()` 一道闸后（⚠️ 观战席的 `dir.player` 是**他正在看的人**，照常渲染不是少信息而是**错**信息）；比分/模式 HUD/记分板/击杀播报/小地图照常，小地图的锚换成被跟随者（转播视角，与脚下阵营标记同源）；死亡遮罩对他从头到尾不出现 —— `renderDeathState` **必须先返回**，否则那段第一句「找不到自己 → 清跟随目标」会把跟随 id 每帧抹一次，镜头当场退回原点。**(e) 观战输入是白名单**（与服务器三张阶段白名单同构）：转镜头/V 换视角/O 记分板/F10 设置/M 静音/F2 画质/面板里的离开对局 —— 需要身体的一条都不给，`view.onSelect` 的 `SetTarget` 也在闸内（发上去只换来 20Hz 的拒绝刷屏）。**(f) 大厅**：房间列表对 `started` 房画「观战」+「加入（N 个空席/人机席位）」两颗键，判据是 **`joinableSeats`**（一旦退回 `capacity - players`，中途加入的按钮会在所有开局房间上静默消失，而界面只是少一颗按钮）；两颗键发**同一条** `JoinRoom`，区别只在 `data-intent`（点「加入」的人入场即弹席位面板，点「观战」的只拿右上角一颗小按钮）。席位面板浮在观战画面之上（样式内联 —— 新 class 在真机上是一块没有布局的裸 DOM，`.lb-end[hidden]` 那次的同款教训），余量**每条 RoomState 现算**，`JoinOngoing` 被拒时 toast 服务器给的那句话并留在面板上换一边。**(g) 顶替后的身份切换**：`Welcome` 覆盖 `playerId` 是既有路径，但建场景时**名单还是旧的**（服务器顺序是 Welcome → MatchStart → RoomState）—— 顶替人机时旧名单里那一行**正是被顶替的人机**（队伍与当场生效的职业都在里面，读它就对），坐空席位时自己还是观战席条目（没队伍没职业）→ 退回刚发出的那条 `JoinOngoing`。两条路拿到的都是**这具身体真正的职业**，自己的角色模型因此不会挂错（`CharacterView.setClass` 幂等首调生效，挂错一次就是整局错）。**(h) 文案不许撒谎**：「你选的职业还没生效」由纯函数 `midJoinClassNotice` 产出，竞技场那一支说的是「**本局将沿用**【战士】—— 竞技场只有一个回合」，**绝不出现「下回合」**（单回合制下那一刻不会到来）；席位面板上的事前提示 `midJoinTakeoverHint` 与它口径一致；大乱斗两处都不说话（新建实体，当场生效）。**测试**：`client/spectate/spectateView.test.ts`（新，25 条：提示条两态、观战轮换与「死亡观战那条没被顺手放宽」的反向锁、席位余量四情形、文案六条、**接线锁 6 条**）、`client/lobby/logic.test.ts` +4（`roomRowActions`）、`server/room/RoomServer.midjoin.test.ts` +4（客户端契约：观战快照无 `self` 段且 `you` 必在实体段里、`ackSeq` 恒 0、`RoomState.bot` 只标人机、`Welcome` 先于第二条 `MatchStart`、竞技场顶替后实体职业仍是被顶替者的且 `roundsToWin === 1`）。⚠️ **顺手修掉同文件一处真实的 flake**：「顶替一个补位人机」那条只等 `MatchStart` 就去读 `all('RoomState').at(-1)` —— 而 `finishSeating` 的 MatchStart 与 `broadcastRoomState` 是两条独立 ws 帧，轮询有机会在 RoomState 还在路上时往下走，读到**入场观战时**那一份（席位当然还标着 bot），于是 `bot` 断言随机变红（4 次全量跑里复现 2 次）。改成「记下条数、等一条新的」，随后 4×14 与 3×1231 全量连跑零红 | `shared/net/visibility.ts`（`SPECTATOR`/`SnapshotAudience`/`isVisibleToSpectator`/`isVisibleToAudience`/`isLegalSpectateFollow`/`spectatableForSpectator`/`NO_ENTITY`）、`net/protocol.ts` + `net/codec.ts`（`JoinOngoing` 等四处加法）、`sim/match/room.ts`（`freeSeatsOn`/`seatSpectator`/`takeOverSeat`/`unseatToSpectator`/`leaveSpectator`）、`sim/match/setup.ts`（`RosterDeps`/`spawnCombatant`/`admitToMatch`/`respecCombatant`）、`server/room/Session.ts`（`Spectate` 阶段 + 三张白名单）、`server/room/RoomServer.ts`（`onJoin` 的 started 分支、`enterAsSpectator`、`onJoinOngoing`、`takeableBotSeats`、断线/退出/重连/淘汰四处观战分支、`RoomState.bot`、`RoomList.joinableSeats`）、`server/MatchLoop.ts`（观战共享段、`audienceOf`、`applyPendingRespecs`、`staticsFingerprint`）；测试 `net/visibility.test.ts` +9、`sim/match/room.test.ts` +10、`sim/match/joinInProgress.test.ts`（新，12）、`server/room/RoomServer.midjoin.test.ts`（新，10 条真 ws）；**客户端半边**：`client/scenes/NetworkScene.ts`（`spectating` 态 / `enterSpectatorSeat` / `readSpectateInput` 白名单 / `spectateCameraTarget`+`updateCamera` 合流 / `noteMidJoinClass` / 小地图锚）、`client/hud/CombatHud.ts`（`setSpectating`）、`client/spectate/spectateView.ts`（新：`spectateBannerText` / `SPECTATE_HINT_TEXT` / `midJoinSeats` / `midJoinTakeoverHint` / `midJoinClassNotice`）、`client/spectate/SpectateController.ts`（`nextSpectateSeatTarget`）、`client/lobby/logic.ts`（`roomRowActions`）、`client/lobby/LobbyShell.ts`（房间列表两颗键 / 席位面板 / `JoinOngoing` 收发 / `onMatchStart` 的身份与职业兜底） | 单机房不再只有开局那批人；观战与中途加入都进得去 | ✅ **两半都已交付**：服务器/共享层（2026-08-11，全量 shared 1445 + server 142 全绿，verify:m10 16/16、m16 29/29、m9 35/35、m5 11/11、m6 13/13、m7 23/23、hardening 6/6 实跑）／**客户端半边（2026-08-11 同日）**：client 全量 1085 + server 146 全绿、typecheck/lint 零告警；两条源码接线锁做过变异验证（去掉 `simulate` 那道闸、往观战输入分支里塞一条 `SetTarget` —— 各当场变红）。**浏览器全链路（verify:m13/w12/p5）未跑**（需要 dev server + playwright，留给主循环）—— 手测四步：① 开一局带人机补位的 3v3 → 第二个窗口在房间列表上该看到「观战 / 加入（N 个空席/人机席位）」两颗键；② 点「观战」→ 进 3D 场景，**没有技能栏**、顶部一条「观战中 · 正在看 X · 按 V 切换视角」，按 V 能换人、按数字键什么都不发生（战斗日志里不该有任何 Rejected）；③ 点「加入」→ 席位面板，选边选职业 → 上场后能正常放技能，且中部提示与战斗日志各有一条「本局将沿用【被顶替人机的职业】」（竞技场口径）；④ 把场上所有人打死/全部潜行的那一帧，观战镜头**不掉回原点**、提示条改说「暂无可观战目标」／**观战席的信息不对称已另立 S10**／balance 不受影响（未开观战/中途加入的对局逐位不变）／**★★ W24 收口（2026-08-11，对抗校验驱动）：交付时有 5 条被实测打穿，已逐条独立复现后修复。**① **blocker 观战席拿到的是队友视图装备**（`MatchLoop` 那一行传的是 `effectiveViewer ?? audience`，而观战席的 `effectiveViewer` 是他正在看的那个**真实实体** → `isFriendly` 对那一队判真 → 全队发 `allyEquipment`；按 V 换一遍视角就能把双方的备用武器/护甲/消耗品收齐）。改传 `audience`（参战者逐位不变：死亡观战跟的是队友，而装备视图只按阵营分岔）—— 本行注释、`CULLING_RULES` 4.4-spectator、docs/08 §6b 这三处白纸黑字此前全是假的，`visibility.test.ts` 那条纯函数用例是绿的而**服务器从来不那样调**。② **blocker 中途加入者带着观战期的装备视图缓存上场**：`snapAccounts` 以 **Session 对象**为键，而顶替是同一个 Session 从观战席坐到战斗席 → `seen`/`equipFp` 原样命中、EntityMeta 一条不重发，新队友整局停在敌人视图（换装/消耗品面板空着）。新增 `MatchLoop.resetSnapshotAccount(session)`，`finishSeating` 在发 MatchStart **之前**调（与客户端 MatchStart 清缓存对齐）。③ **major 中途加入可以顶替一具尸体**：全灭守卫只在 `admitToMatch`（有空位）那条路上，而「队伍被人机补满」恰恰走顶替路，`takeableBotSeats` 三道筛一条都不看存活。补第四道筛（实体必须 `alive`，`RoomList.joinableSeats` 因此也变诚实）+ 顶替路开头补一次 `teamWiped`，两条路给**同一句** reason。④ **major 客户端一处都没跟上换职业**（唯一会兑现 respec 的夺旗）：`SnapshotCombatView.ingest` 的判据是「栏是空的」改成「职业变了**或**栏是空的」（后半句不能省：同职业顶替会留下一个空技能栏）；`CharacterView.setClass` 从「幂等首调生效」改成「职业没变才不做」并支持换模型（旧 root/武器/actions/叠加层/侧闪与摇头基准全摘干净 + `classSeq` 防旧请求后到），自己与远端两条路各接一句。⑤ **major 补位房赛后再也开不出第二局**：人机 `connected` 恒真，`resetForRematch` 的断线筛筛不掉，而没有人会替它们 `setReady` → `canStart` 永不满足、`fillBotSeats` 要等 `beginMatch` 才跑（死锁）；观战者赛后还会发现两队都「已满」一个席位都选不到。`resetForRematch(room, botIds?)` 加一个**可选**参数（不传 = 老行为逐字不变），服务器按 `sr.botSessions` 的键传（与 `RoomPlayerView.bot` 同源，不按名字前缀猜）。**锁**：`RoomServer.midjoin.test.ts` +6 条真 ws（装备视图形状 / 上场后全量重发 / 全灭两条路同一句话 / 尸体席位不可顶 / 赛后名单无人机且能开第二局 / 观战者赛后选得到阵营）、`room.test.ts` +2、`CharacterView.test.ts` +4、`SnapshotCombatView.respec.test.ts`（新，3）—— 5 条修复各自做过**变异验证**（把修复改回去，对应用例当场变红）。顺带修掉既有用例 `顶替一个补位人机` 的一条真 flake（满负载全量跑里抓到）：房间广播是扇出的，`waitForNth('RoomState', n+1)` 等到的可能是「他自己入场观战」那条正好晚落地的陈旧帧 ——名单类断言改成按**内容**等（`waitForRoomState(pred)`），不再按条数等。**minor/note 不修，如实记在这里**：(a) `SetRoomPreset`/`SetFillWithBots`/`SetRoomBotDifficulty` 没进 `Session.ROOM_ONLY`，观战席发它们会穿过阶段白名单、靠 sim 的 `started`+`hostId` 守卫兜住 —— 结果对（都被拒）但纵深只剩一层，且 api 清单那句「其余一律『当前阶段（spectate）不接受这条消息』」对这三条是错的；(b) 对局结束瞬间的 `JoinOngoing` 被拒时，toast 出来的是内部措辞「当前阶段（room）不接受这条消息」；(c) 大厅两处「可加入席位」口径不一致：`RoomList.joinableSeats` 只数 `fill` 人机，客户端 `midJoinSeats` 数所有 `bot`（含掉线接管）→ 只剩接管席位的房间会把一颗必然被拒的按钮画成可点；(d) `LobbyStatus.spectating` 赛后不复位（`MatchEnd` 只切 `page`），verify 脚本会读到上一局的席位；(e) 大乱斗满员时会掉进组队模式的顶替兜底，说出「红方/蓝方没有可加入的席位」，而 P12 没有两队（`teamSizeOf(ffa)=100` vs `FILL_TARGET=20`，今天是死分支）；(f) 两处文档漂移：`snapAccounts` 注释里的 `hpFp` 早已改名 `staticsFp` 且覆盖整块静态块。**攻过没打穿的（供下一轮不重复攻）**：潜行三态口径（`stealthRevealed` 全仓库三处写入，是全局标志不是按队记录）、`assertNoHiddenEntities` 对观战段既不误报也不漏报、观战会话 23 种消息里 20 条被阶段白名单拒、私信类（`CastFailed`/`ArsenalOffer`/`PickupResult`/`FfaShop`）经 `sessionOfEntity` 对观战席结构上不可达、限流/背压与 `MAX_ROOM_MEMBERS`/`MAX_CONNECTIONS` 对观战会话照常生效、`self` 段一个字节不发且 `ackSeq` 恒 0、抢席位竞态不双卖、死亡拔锚与 `resetRound` 清锚两笔都真的做了、观战者掉线不触发人机接管 |
| W25 | **物理学派远程的箭矢/投掷仍在瞬间落账**（W23 迁移的**显式余账**）：客户端画着一支箭飞过去，伤害却在按下的那一刻就结算完了 —— 与 W23 修掉的是**同一个**割裂，只是这一批被刻意留到了第二批。**清单是 7 条不是 5 条**（对抗校验枚举全花名册后更正）：**猎人 5** —— 瞄准射击（35m，当前最大错拍 0.64s）、震慑箭（30m，0.55s）、反制射击（30m，`interrupt` 另见下）、穿透重弩箭（32m，已是碰撞型）、大乱斗回旋镖（24m，已是碰撞型）；**战士盗贼 2** —— **风暴之锤**（20m，`[damage, stun]`）与**致盲**（10m，`[incapacitate]`），两条都满足 `flies()`、客户端都在画弹道，而 `projectile.ts` 的模块头注释本来就把「风暴之锤」和火球、审判并列当作锁定投射物的**范例**。**★ 实际交付是「4 迁 3 不迁」**（2026-08-11）：7 条里有 3 条**本来就不该迁** —— 反制射击是打断（排除①），穿透重弩箭与飞去来斧已经是碰撞型（另一类，不是锁定的慢速版本）。真正动了数据的是**瞄准射击 / 震慑箭 / 掷锤 / 致盲**这 4 条 | **已交付**（用户拍板 2026-08-11「一起做」，W25）。**迁移 4 条**：猎人 `aimed_shot`（35m，0.64→**0.47s**）、`concussive_shot`（30m，0.40s）；战士 `storm_bolt`（20m，0.36s，载荷 `[damage, stun]` 整组进 `onHit`）；盗贼 `blind`（10m，0.18s）。**维持排除 3 条**：`counter_shot`（`interrupt` —— **排除①优先于「同组一起迁」**，断法迟到 0.4 秒等于没断）、`piercing_bolt` 与 `ffa.boomerang_throw`（已是 `spawnProjectile` 碰撞型）。**引擎侧一行未改**，纯数据迁移 + 口径放宽。**速度口径（用户方向「箭该有箭的样子」）**：`SPELL_PROJECTILE` 加 `ARROW_SPEED = 75`，法术档 55 一位不动；**分档的边界是「投出去的是什么」而不是学派** —— 掷锤（钝器）与致盲粉（粉末）都走 55，因为它们不该比箭快。**客户端按技能读速度**：`SpellVfx` 的 `BOLT_SPEED` 从「唯一速度」退居**兜底**，装饰弹道寿命改用 `boltSpeedOf(skill)`（读技能数据的 `lockedProjectile.speed`，按 id memo 一次，查不到回落 `BOLT_SPEED`）—— 继续用 55 算的话猎人**每一发箭都晚到 0.17 秒**，正是 W23 修掉的那个错拍在物理远程上原样复发。**★★ 广度锁放宽的代价（本批最大的一条收获，是被测试当场红出来的、不是事先想到的）**：`shouldMigrate` 删掉「学派非 `physical`」之后，**战士的突进**（20m、`Direct`、`Enemy`、载荷 `[chargeTo, stun, gainResource]`）立刻满足全部判据 —— 它此前纯靠学派那一条被顺手挡在门外。迁了它就是「人已经撞到脸上、0.36 秒后才晕」。于是口径把原来隐含在学派里的东西**逐条显式化**：① 打断；② 已是碰撞型（`spawnProjectile`）—— 今天这两条都是 `Targeting.Projectile` 因而已被`Direct` 挡下，但那是巧合不是不变式；③ **施法者自身位移**（`chargeTo`/`chargeToAlly`/`blinkForward`/`teleportBehindTarget`/`leapBackward`）—— 飞过去的是人不是弹体。三条各配一条**反向断言**（点名 `counter_shot` / `piercing_bolt`+`boomerang_throw` / `charge`），外加一条正面断言「physical 迁的就是这 4 条」（只留「physical 一个都没迁」的空数组断言，谁把物理挡回去它照样绿）。**同族顶层扫描第 7 处（`sim/tick.test.ts`）**：约束 6+7 的探针用 `sk.effects.some(e => e.kind === 'stun')` 在 1 米处抓昏迷，抓到的正是掷锤 —— 迁移后战士只剩**突进**这一条顶层昏迷（最小射程 8m），夹具当场红在「找不到一个此刻能放的瞬发技能」。⚠️ 这一处的取舍与 `skillSmoke`/`botController` 那批**相反**：本用例测的是**顺序**，锁定投射物的昏迷天然下一 tick 才落账，所以探针**刻意只认顶层**，站位改到 10 米走突进。**bot**：`MIGRATED_SKILLS` 从全花名册筛，4 条自动进等价断言（`hasDamage`/`burstDamageOf`/`ccCategoryOf` 逐位不变），下限 21→**25**、四条逐条点名；新增一条**正面**断言 —— 等价断言在「两边同时坏掉」时会一起绿，而**瞄准射击是猎人 bot 的主力输出**（认不出来就从 300% 武器伤害退化成随便按一个填充键），所以另钉 `hasDamage(aimed_shot)`、`ccCategoryOf(storm_bolt) === Stun`、`ccCategoryOf(blind) === Incapacitate`。**测试**：`sim/lockedProjectile.test.ts` 24→**33**（新增 W25 行为组：箭速按技能读、掷锤的晕在抵达才亮、致盲 0.18s、掷锤走位躲不掉但免疫挡得住）、`vfx/lockedProjectileVfx.test.ts` 14→**18**（35m 箭 0.47s / 同距离法术仍 0.64s 的**对照组** —— 少了它「全局改成 75」也会绿、掷锤 55、没迁移的技能回落 `BOLT_SPEED`）、bot +1。**变异验证**：把箭速改回 55 → 红 2 条；把客户端寿命改回 `BOLT_SPEED` → 红 1 条；把回落值改成 `ARROW_SPEED` → 红 1 条；删掉排除③ → 广度锁点名 `warrior.charge` 红。shared 39 文件 1455 条 + client 70 文件 1089 条**全绿**，`tsc -b` 与两个 `tsconfig.test.json` 干净，`docs/04` 已重生成（4 行，`lockedProjectile` 入列）。**⚠️ 三条如实余账**：① **秘法箭与毒蛇钉刺也是箭，却仍走 55** —— 它们属于 W23 那批（奥术/自然学派，沉默与学派锁定挡得住，机制上确实是法术），W25 拍板「法术 55 不动」；于是同一张弓射出去的三支箭有两种速度。要统一就把这两条抬到 `ARROW_SPEED` 并单独归因，别当成没人看见。② **W27 接 `skillModifiers` 时必须下探 `onHit`** —— 长弓的 `hunter.aimed_shot: damageMultiplier=1.15` 今天零消费方（W27 债），接线时若只扫顶层 `damage`，这条会**静默失效**：载荷已经在 `lockedProjectile.onHit` 里了。这是 W23 那串「同族顶层扫描」的**第 8 处，且尚未发生** —— 提前记在这里。③ W23 note ③「盗贼无迁移技能」**已过期**：致盲现在是一条**纯控制**的迁移技能，因此它既不破对手的隐（本来就不），也永远不破**自己**的隐（破隐挂在 `dealDamage` 上）—— 与 W23 那批纯 CC/纯 DoT 技能同性质，不是本批引入的新行为。同理 W23 note ① 的 homing 快照位置泄露窗口从 21 条扩到 25 条（新增窗口 0.18~0.47 秒，均短于原有的最长 0.64 秒，无新类别）。**★★ W25 收口（2026-08-11，审计驱动）：物理规避（闪避/招架/格挡）与背后判定的时点。**本批是**第一批把物理伤害送进锁定投射物**的，此前无用例、未入册。W23 迁的 21 条全是魔法学派，`rollAvoidance` 第一行「学派非 `physical` 直接放行」让闪避/招架/格挡整段**压根不执行**；W25 把物理送进这条路之后，那两条**现读实时状态**的判据第一次暴露。**独立复现**（临时探针，跑完已删；靶子挂 `dodgeFront:1`）：① 全程正面 → 箭被闪掉、血一点没掉；② 释放时正面、**弹体在飞时把 `yaw` 转 180°** → `fromBehind` 变真、规避整条跳过、300% 武器伤害照落；③ **弹体在飞时被（队友的）控制挂上 `stunned`** → 同样跳过规避。三者都是「**走位/转身改变了一发已经锁定的箭的结果**」，与本批赖以成立的 6.6 口径正面冲突；而现有的两条「走位躲不掉」用例都绕开了这一面（一条是魔法学派、一条被 `immuneAll` 在规避之前就短路）。**裁决：6.6 的「命中资格」包含规避** —— 6.6 给锁定投射物列的反制方式是**免疫、吸收、反射**三样，闪避不在其中，而「飞行途中转个身」正是它明文禁掉的那件事。**实现**：`HomingProjectile` 加**必填** `hitSnapshot: { fromBehind, canAvoid }`，`spawnHoming` 在释放瞬间冻结这两个**事实**；经 `EffectContext.hitSnapshot` 送到 `rollAvoidance` 与背刺加成（`tickWorld` 第 5 步**只给 `kind === 'homing'` 传** —— 碰撞型是实时轨迹实时命中、延迟落点是落地重新圈人，两者的空间事实就该现读）。**★ 冻结的是「事实」不是「几率」**：`mods` 仍取抵达那一刻的光环聚合值，所以盗贼在箭飞行途中开闪避（「5 秒内正面闪避提高 50%」）**照样有用** —— 分界线是「**按了一个键**」（与免疫/吸收同族，6.6 明文允许）与「**转了个身**」。没有快照的一切路径（近战、普攻、DoT、地面区域、直接施放）逐位不变。**测试**：`sim/lockedProjectile.test.ts` 33→**39**（新增第 5 组 6 条：对照组 / 飞行中转身 / 飞行中被控 / **反向**（释放瞬间就背对射手 → 转回正面也躲不掉，否则「永远返回 dodge」也会全绿）/ 飞行中开闪避键仍生效 / 快照字段本身）。**变异验证**：让 `rollAvoidance` 忽略快照（= 收口前的行为）→ 红 3 条；让锁定投射物一律不规避（= 把资格整个在释放瞬间掷完）→ 红 4 条。`net/visibility.test.ts` 两处手搭的 homing 字面量因**必填**字段编译不过 → 已补字段：「漏填编译不过」就是这条约束的执行方式，快照面（`ProjectileSnapshot`）不下发它。**⚠️ 给主循环 balance 归因追加一句**：这批不只让猎人的输出迟到 —— 它还把战士/圣骑的 20% 格挡、匕首招架、盗贼闪避对这 4 条技能的**生效时点整体后移**，`nextRandom(target)` 的消耗顺序也跟着变，**差异不全是那 0.36~0.47 秒**。**⚠️ 同批审计的 minor/note 四条，如实入册、本次不修**：④ **新增的两条 W25 伤害用例是靠种子过的** —— `makeRig` 的 foe 恒为 `warrior`（默认 `sword_shield` → `block = 0.2`；rogue/hunter 三项全 0），而 `foe.yaw = caster.yaw + Math.PI` 恰是正面（格挡只在正面生效），于是「瞄准射击 0.4 秒」与「掷锤」这两条走 `School.Physical` 的用例每一发都要真掷一次 20% 格挡，今天绿只是这颗种子没掷出格挡。W23 那批同类用例全用法师、`rollAvoidance` 第一行就 return，完全不碰随机流 —— **这条脆弱性是 W25 新引入的**：以后任何改动靶子实体随机流顺序的提交（新增一次闪避/暴击判定、改 tick 顺序、加一条 DoT）都可能让「箭到了却没结算」红在一个与它毫无关系的地方。修法：给 foe 挂一条把 `dodgeFront`/`parry`/`block` 清零的探针光环（或拿 hunter/rogue 当靶子），或把断言改成先查 damage 事件的 `avoided === undefined` 再比血量。（★ 收口新增的第 5 组**不吃这条**：它用 `dodgeFront: 1` 的探针光环，规避与否是确定的，不靠种子。）⑤ **反制射击：sim 瞬时打断，客户端仍画一条 0.55 秒才抵达的装饰箭** —— `flies()` 四条判据全中（30m、Direct、单体、无 `spawnProjectile`），`boltSpeedOf` 查不到 `lockedProjectile` 回落 `BOLT_SPEED = 55` → 装饰箭寿命 30/55 = 0.545 秒。玩家按下断法箭时对手的读条**当场**断掉、箭半秒后才飞到 —— 正是 W23/W25 这条线要消灭的那个错拍，只是方向反过来（**结算早于表现**）。排除①只写了 sim 侧的理由（「断法迟到 0.4 秒等于没断」），表现侧仍然错拍这件事一个字没写；而 `vfx/lockedProjectileVfx.test.ts` 新增的那条 `counterShot` 用例（断言寿命就是距离/55）把这个状态**正式钉成了不变式**，读起来像在给割裂背书。修法二选一：(a) 给「`flies()` 认、但 sim 侧瞬时结算」的技能一条 hitscan 表现（寿命压到 0.05 秒或换瞬时光束），让表现与结算重新对齐；(b) 明确接受这半秒并把那条用例的注释改成「这里钉的是**回落机制**，不是说这半秒错拍是对的」。同形状还剩 `hunter.piercing_bolt`（32m），但它是碰撞型走真轨迹，不算同一类。⑥ **装饰弹道按 `skillId` 认领快照弹体，同技能两发同时在飞时会吞掉一发的渲染** —— `SpellVfx.ts` 的 `if (this.bolts.some(b => b.skillId === p.skillId)) this.boltCovered.add(p.id)`，认领判据只有 `skillId`，没有施法者/目标/弹体 id 的对应关系：两名猎人在同一 0.47 秒窗口内各射一发瞄准射击时，只要本机为其中一发画了装饰弹道，另一发的快照弹体也会被 `boltCovered` 认领并整个跳过渲染 —— 场上少一支箭、0.4 秒后血条凭空掉。**这是 W23 就有的写法、不是本批引入**，但本批把它的作用面从 21 条扩到 25 条，且新增的 4 条里两条是猎人常用键（瞄准射击 8s、震慑箭 10s CD），并发概率高一档。修法：认领按「(`skillId`, `targetId`) 或 `casterId`」配对，`CastResolved` 消息里这两样都有。⑦ **余账①的措辞该点名「倒挂」** —— 同一张弓射出去的四支箭的最远飞行时间：`aimed_shot` 35/75 = **0.467s**、`concussive_shot` 30/75 = 0.400s、`arcane_shot` 30/55 = **0.545s**、`serpent_sting` 30/55 = 0.545s：**射程更远、更重的那支箭反而最先到**，而猎人按得最频繁的填充键（秘法箭）是全场最慢的箭，比 25 米的火焰冲击还慢。玩家真会看出来的是这一面（「我先按的秘法箭，怎么后按的瞄准射击先炸」），不是「有两种速度」。`hunter.ts:104-113` 的注释同样只说了「两种速度」。⑧ **`sim/tick.test.ts` 约束 6+7 的探针现在唯一命中 `warrior.charge`** —— 掷锤迁走之后（枚举确认：Direct + Enemy + ≥8m 的 28 条里，`charge` 是唯一一条载荷含**顶层** `stun` 且未迁移的），这条「顺序」用例的探针变成了一条会把施法者**传送到目标脸上**、还带 8 米最小射程的位移技能，且靠全表扫描抓：站位（10 米）、最小射程、唯一性三件事任意一件被后续批次动到，红灯就会落在「找不到一个此刻能放的瞬发技能」而不是落在顺序上。修法：技能显式写死（`find(s => s.id === 'warrior.charge')`）+ 旁边加一条前置断言「它必须是瞬发 + 顶层 stun」，站位写成 `charge.range.min + 2` 之类的推导值，别让 10 这个字面量成为第二个会漂的数。**⚠️ 这个文件同时是并行 W24 批在改的（他们动 `tick.ts` 第 2 步与 match），合并时两边都会看到它。** | 猎人/盗贼/战士的远程手感与 W23 之后的其余职业不一致 | ✅ W25（2026-08-11，4 迁 3 不迁 + 箭速分档 + **收口：规避时点按释放瞬间**；**balance 未跑，归因由主循环单独盯** —— 猎人是全场最脆，这批的方向性风险是「主力输出与唯一拉开手牌双双迟到」）。**balance 已归因（主循环 2026-08-11 实跑，多自变量口径照收口批警告）**：极差 69.0pp 持平（paladin 85.7 顶/mage 16.7 底不动）。逐职业：warrior 42.9→**50.0**（掷锤晋级弹道后背身冻结生效）/ hunter 21.4→**26.2**（箭虽迟到，但「转身回防救不了闪避」的收口净赚）/ druid 78.6→81.0 / priest 85.7→78.6 / rogue 28.6→**21.4**（闪避对四条新弹道的生效时点改按释放瞬间——转身闪避流受损）/ dk、mage 不动。方向与三通道（飞行延迟/规避时点/背身冻结）自洽；hunter 上行说明「最脆职业再削」的担心未应验 |
| W26 | **`AuraModifiers` 五个字段「只聚合、没有消费方」**（原 PROGRESS 技术债 §9，2026-08-08 大乱斗派对道具批发现，按 G5 补登总账）：`sim/modifiers.ts` 的 `aggregate()` 把 `maxHealth` / `knockbackTaken` / `castSpeed` / `attackSpeed` / `absorbDone` 乘进 `EffectiveModifiers`，然后**没有任何下游读它们**。现有数据源：熊形态 `maxHealth: 1.2`、机动甲 `knockbackTaken: 1.25` 与抗控甲 `0.6`、6 处 `castSpeed`（法师法杖 1.1 / 魔杖法球 0.88、牧师法杖 1.08 / 权杖圣典 0.85、圣骑权杖圣典 0.85、守护甲 1.08）、守护甲 `attackSpeed: 1.08`、`absorbDone` 零数据源。复现：`grep -rn 'castSpeed' packages/shared/src/sim` —— 除 `modifiers.ts` 自己外，`casting.ts` 与 `autoAttack.ts` 里一次都没出现。**这类缺陷不报错、不变红，只有 `advantage` / `description` 变成谎话** —— 10.8 / 验收 #32 承诺的护甲横向取舍因此有一部分只写在文案里 | **已交付（Wave4，2026-08-11）。五条接线各一处落点，全部走既有唯一出口：** ① `castSpeed` → `casting.castTimeOf()`，在 `beginCast` 算 `endsAt` 时乘一次。**取值时刻 = 读条起手那一瞬、之后锁死**（WoW 口径；且 `endsAt` 是绝对时刻，HUD/`castVfx` 按 `(now-startedAt)/(endsAt-startedAt)` 插值，每 tick 重算会让掉 buff 表现成读条条跳一下）。**刻意只缩读条段、不缩 `channelDuration`**：引导时长是效果本身的时长（暴风雪引导 4 秒 == `spawnGroundArea.duration` 4 秒，`tickWorld` 的打断分支靠这个等式掐区域），只缩 `channelEndsAt` 会得到「引导条走完了雪还在下」，缩区域则是在改技能强度=配平。schema 原文也只说「**读条**时间乘算」。倍率**由调用方注入**（`CastTimeScaleFn`）而不是 casting.ts 自己算 —— 那个文件的纪律是零数据注册表依赖，而聚合要读武器/护甲；`tickWorld` 里一个闭包同时喂三个施法入口（`beginCast` / `beginCastOrQueue` / `tickCastQueue`），分头各传各的会出现「直接放吃代价、排队放不吃」。② `attackSpeed` → 新增 `autoAttack.swingIntervalOf()`，`tickSwings` 与**两处登记侧**（`MatchLoop.syncSwings` / `CombatDirector.syncBotSwings`）共用 —— 不共用的话第一刀不吃护甲代价。方向按 schema 原文「攻击**间隔**乘算」：> 1 = 更慢。③ `knockbackTaken` → `effects/displacement.ts` 的 `knockback` 处理器（读**被推者**的聚合值）。★ 那一行上方原本就写着「抗控型护甲降低击退距离（10.8），这里读聚合后的修正」，而下面一行没读任何修正 —— 注释描述的是一件不发生的事。**刻意不动 `pullTarget`**：拉拽落点由施法者位置决定，按承受方抵抗缩放会把人拽到半路悬着。④ `maxHealth` → `CombatEntity` 新增 `baseMaxHealth`（职业 `baseHealth`），`tickWorld` 第 7 步与 `deriveStatusFlags` 同址调 `entity.applyMaxHealthMultiplier()`。**幂等**（`base × 倍率`，不是 `当前 × 倍率` —— 后者 20Hz 下 3 秒把熊撑到 1500 倍且无断言会红）+ **血量百分比守恒**（§9 原文警告的「写回时机错了会让变身瞬间掉血」）。位置必须在全部光环变动之后：放前面变熊要等下一 tick 才涨血，那 50ms 里挨的一刀读作「变熊没加血还被偷一下」。⑤ `absorbDone` → `aura.applyAura` 新增 `absorbScale`，由 `applyAura` 处理器喂施加者的聚合值，**只在这枚光环真的带 absorb 时才算**（省掉每次上 buff 的一次聚合）。⚠️ **零数据源 = 零行为变化**，接它的理由是「不能继续半死着」；`absorbInitial` 记的是缩放后的真实盾量，14.3 的强度衰减与 16.1 的吸收记账都不受影响（降级为「删字段」的条件未触发）。**快照必修（对抗校验查出）**：P11 波3 把 `maxHealth` 搬进 `EntityMeta` 的「首见即发」静态块，理由写着**「一局内不变」—— 熊形态接线之后这句话不成立**。不补发的后果：服务器 1260、客户端还是开局的 1050，而快照里的 `health` 是对的，于是熊满血画成 120% 血条，**没有任何一层会报错**。补发走与装备完全同一套「指纹变了才发」的路（`snapAccounts` 加 `hpFp`），不是把字段塞回每 tick 的实体段（P11 实测 306KB/s/客户端，变身是稀有事件不值那个价）；客户端**零改动** —— `SnapshotHydrator.setMeta` 本来就是「带 statics 就覆盖」。**教学必修**：`TutorialDirector` 毕业环直接写 `d.maxHealth = 300`，写回接上之后 50ms 就被打回职业原值（毕业环变成打不完的满血局，而教学验收只看「击杀三个」不看血池）—— 改成写 `baseMaxHealth`。**测试**：`sim/auraModifiers.test.ts` 39 条（五条接线各「生效 + 边界」、变身血量守恒 8 条含满血/80%/残血 1 点/上限边缘/死人不动/回合重置写法、集成「§9 表格每一处现有数据源逐个亮灯」）+ `server/MatchLoop.maxHealth.test.ts` 2 条（补发 / 不变身不补发）。**五条接线逐个做了变异验证**（把乘算改回原样 → 分别红 5/3/2/8/2 条；快照补发变异 → 红 1 条）。`sim/party.test.ts` 的「不使用死修正字段」清单随之清空，但**断言本身刻意保留**作为下一个死字段的挂点。**如实记的三条余账**：① `healCastSpeed` 是**第六个**同族死字段（零数据源、零消费方），本批未接 —— 判「是不是治疗技能」要扫效果树，而 W23 已经证明顶层扫描会漏 `onHit` 下沉的载荷，值得单独一批；② 客户端挥砍动画倍速 `weaponAnim.swingTimeScaleFor()` 读的是**武器原始** `swingInterval`，不吃 `attackSpeed` —— 守护甲下动画比间隔快 8%，表现为刀与刀之间多一点空档，不是错拍（联网客户端手里没有聚合所需的装备/光环全量，plumb 过去要新开通道）；③ `CombatHud` 技能提示里的 `${skill.cast.time}s` 是**基础**读条时间，不跟护甲/武器走（施法条本身跟，因为服务器发的是 `endsAt - startedAt`）。★ GCD **不吃** `castSpeed`（schema 原文限定「读条时间」），与 WoW 的急速同时压 GCD 是有意差异。**——— 收口批（2026-08-11 对抗校验）逐条处置 ———** **已修 3 条 major**：① **BOSS 狂暴 `attackSpeed 0.75 → 0.8`** —— 该字段乘的是**间隔**，0.75 = 攻速 +33.3%，而 `boss.ts` 的 description 与 docs/06 的 BOSS 表都写「+25%」。W26 之前这是一句**死的**谎话（字段没消费方，BOSS 挥多快都一样），接线之后它变成**活的**超发 8.3pp —— 正是本批立项要消灭的缺陷形态的镜像。选「改数值兑现文案」而不是「改文案保数值」：这样 boss.ts 与 docs/06 两处**同时**为真，且 BOSS 是 PvE 内容配平影响可控（熔岩巨锤 2.6s 间隔：1.95s → 2.08s）。`data/data.test.ts` 新增一组断言**从 description 里抠百分比反推 modifiers**（不重抄 40/25，含「句式还认得出来」的兜底与「狂暴必须让间隔变短」的方向兜底），单改一边就红；已做变异验证（改回 0.75 → 红 1 条，报错文字直接给出「应写 0.8」）。`auraModifiers.test.ts` 里「0.75 那一档」的用例同步改成 0.8，并去掉对 BOSS 数据的转述（引擎方向验在那边，文案对账验在 data.test.ts，两处各管一件事）。② 方向预判作废重写，见状态列。③ `skillModifiers` 整族另立新账 **W27**。**未修、如实记档的 minor 5 条**：① `scripts/balance-report.ts:212-213` 是 `swingIntervalOf` 漏掉的**第三个登记侧**（直读 `getWeapon()?.swingInterval`）—— 今天无害（该脚本永不换护甲），但它正是 `autoAttack.ts` 那条「两处各写一遍迟早漂移成开打第一刀不吃护甲代价」注释所防的事的现成实例，而且它是 balance 归因的**基准脚本**：将来任何给**默认**装备加 `attackSpeed` 的改动会在这里静默失真、报告却看起来一切正常。修法是换成 `swingIntervalOf(auras, a, world.time)`，并把注释里的「两处」改成「三处（含 `scripts/balance-report.ts`）」—— 注释里数错登记侧数量正是下一次漏改的种子。② `client/src/hud/LoadoutPanel.ts` 的换装数值对比**不列**本批刚变真的四个字段：`compareWeapons`（:116-124）无 `castSpeed`，`compareArmors`（:155-158）无 `attackSpeed`/`castSpeed`/`knockbackTaken` 且末尾 `slice(0, 4)` 会切掉第 5 项 —— 守护甲现在正好有 5 项非零差异，玩家读到的是「只花 7% 移速买 15% 减伤」，本批刚兑现的两条真代价一条都不显示；sim 兑现了、展示层反而把它退回文案（三者都是 `lowerIsBetter`，`slice` 要放宽到 6）。③ **死亡不清光环**：`sim/death.ts` 全文无 `clearAuras`（全仓只有 `boss.ts` 摘尸与 `arena.ts` 回合重置清），熊形态光环 `duration: FORM_AURA_DURATION = 3600` 跨越死亡 —— 实测置死后走 2 tick，`aurasOf` 仍是 `['druid.bear_form']`、`maxHealth` 仍 1260，随后波次复活按 `respawn.ts` 的 `e.health = e.maxHealth` 直接给满 **1260/1260**。W26 之前这条只泄漏隐形的 `damageTaken 0.85`，现在连血池一起泄漏且血条上直接读得出来；属**既有**缺陷、W26 只是让它第一次可见，值得**单独立账**（修法：`AuraDef` 加 `clearOnDeath` 默认 true，出生保护/复活护盾显式 false；在此之前至少在 `death.ts` 头部写一句「光环刻意不清 —— 形态类 3600 秒光环会跨越死亡」）。④ `arena.resetRound` 的**顺序**问题：`arena.ts:241` 先按旧上限 `e.health = e.maxHealth`、`:250` 才 `clearAuras`，熊形态德鲁伊在回合切换时有**一 tick** 的 1260/1260 人形（下一 tick 才被百分比守恒收回 1050/1050）—— 数值最终正确，但那一帧广播的 `maxHealth` 指纹变了会触发一次多余的 statics 补发、客户端血条最大值闪一下；修法是把 `clearAuras` 提到回血之前。⑤ 两处新增聚合里 `syncSwings` 那次是**纯浪费**：`beginSwing` 第一行就是 `if (store.has(id)) return`，而参数急求值，绝大多数 tick 白算一次 `effectiveModifiersOf`（改成 `if (!swings.has(e.id))` 守卫即可，语义逐位相同）；同址 `tick.ts` 的「零开销」注释只对**写回**成立，聚合本身每 tick 无条件付（实测 4 枚光环约 0.25µs/次，12v12 20Hz 两处合计约 0.24ms/s/房间 + 约 960 个 `EffectiveModifiers` 对象/秒/房间的分配，与 P11「24 人 = 48 数组/tick」的口径不一致）。**note 两条**：① **守护甲对法师是 1.188 的复利，不是统一 +8%** —— `modifiers.ts` 的装备池对 `castSpeed` 一律相乘、不参与「取最强」保护：法师默认法杖 1.1 × 守护甲 1.08 = 1.188（霜矢 2.0s → 2.376s），牧师 1.08 × 1.08 = 1.1664，而战士/盗贼默认武器无 `castSpeed` 只吃 1.08。归因时按「职业 × 装备」而不是「装备」记这一条；若将来出现「法师穿守护甲直接不能玩」，源头是这个乘积不是 1.08 本身（收窄的方式是给 `aggregateModifiers` 的 `castSpeed` 加一条「装备池内 > 1 的部分取最强」，但那是配平改动、该单独一批）。② **hard 档 `KICK_WORTH_CAST_SECONDS = 1.2` 判据从此随装备浮动** —— bot 打断读的是**实际**读条时长（`botController.ts` 用 `foeCast.endsAt - foeCast.startedAt` 而不是 `skill.cast.time`）：1.2s 读条配 0.85 权杖变 1.02s，hard bot 从此**不踢**它；1.1s 读条配法师法杖 1.1 变 1.21s，hard bot 从此**开始踢**。设计上正确（同址注释自己说「bot 看到的和真人看到的是同一根条」），但它是一条会影响 **hard 档**回归基线的行为变化，归因时需要知道。normal 档不受影响（反应 0.35s + `remaining > 0.1` 的可踢下界 0.45s，全仓最短读条 0.4s 缩放前后都踢不到）。**对抗校验已核实、下一轮可省掉重查**：真 `tickWorld` 跑 100 tick 熊形态 `maxHealth` 恒 1260 / `health` 恒 1008（80% 守恒），1 HP 变熊 → 1.2 HP、变回 → 1 HP 无取整归零，死人 pct=0 原样留 0；`castSpeed` 只在 `beginCast` 取一次值，读条中途掉 buff / 被驱散 / 换装 `endsAt` **不回弹**（与 7.x WoW 口径一致），`CastStarted.duration` 与客户端 `castStateFromStarted` / `SpellVfx` 全跟随实际时长、无协议改动无 VFX 错拍；三个施法入口确实共用同一闭包（`tick.ts` 定义处 → 两个喂入点），排队放与直接放吃同一份代价；击退实测默认 14m / 抗控甲 8.4m / 机动甲 17.5m，方向与两套护甲的文案一致，`clampDisplacement` 仍是唯一出口、进墙出界钳制未被绕过，`pullTarget` 确实没吃；快照补发无洞（`equipChanged && !hpChanged` 分支写入值与已记录值相等，不会吞掉 statics；隐身进出与 BOSS 无 loadout 两条边角都能正确补发）；教学修复完整（`TutorialDirector` 两处各三行，含 `Math.min(d.health, d.maxHealth)` 钳制，不会被 `entity.ts` 的早退永久卡住）；`phases.ts` 的 `seconds: skill.cast.time` 无害（`phasesFor` 只被自己引用，VFX 运行时走实际 `endsAt`） | 文案承诺了一件不会发生的事（本仓最难查的一类）；护甲横向取舍缺一半 | ✅ **Wave4（2026-08-11）**。**balance 已归因（主循环实跑，--matrix 前后对比）**：极差 76.2→**69.0pp**（本轮开工时 81.0）。逐职业：priest 90.5→85.7 / paladin 85.7→85.7 / druid 71.4→**78.6** / warrior 42.9 / dk 42.9→40.5 / rogue 35.7→28.6 / hunter 14.3→**21.4** / mage 16.7→16.7。**预登记的可证伪断言通过**：不含 priest/mage/druid 的 10 个对局对逐位不变 —— 全部变动流经已识别的两条入口。对局对级归因：druid 从 priest 手里抢 3 场（priest-druid 6/6→3/6，熊形态坦度）；hunter-mage 3/6→6/6（法师默认法杖 1.1 读条变长被猎人吃满）；mage-rogue 3/6→6/6 对冲（mage 总分不动）；priest-dk 3/6→2/6。rogue −7.1 全部来自与三个受影响职业的对局。**原方向预判已作废，换成下面这条可证伪的**（收口批对抗校验推翻了它的两条前提）：本批在 balance 口径（`scripts/balance-report.ts`：`ArenaPreset.Classic`、全程默认装备、无 BOSS）下的真实入口只有**两个**而不是五个 —— ① 两件**默认**法杖的 `castSpeed`（`mage.staff` 1.1 与 `priest.two_hand_staff` 1.08，两者分别是各自的 `defaultWeaponId`），**纯削弱**；② 德鲁伊熊形态的 `maxHealth 1.2`。**另三条逐位为零，且可离线证伪、不必跑 balance**：`attackSpeed` 的唯一数据源是守护甲，而八个职业的 `defaultArmorId` 全指向 `makeArmorSet` 的 Baseline 档、`modifiers` 恒为 `{}`（全仓无人传 `defaultModifiers`），脚本也从不换装（全文无 `armorId`/`weaponId` 赋值，`arsenal.enabled` 只对 `ArenaPreset.Armed` 为真）；`knockbackTaken` —— `grep -rn "kind: 'knockback'" packages/shared/src/data/classes/` 只命中 `boss.ts`，八职业零个击退效果；`absorbDone` 零数据源。**★ 更正一：**「bot 从不变熊，因为七个分类器都排除 shapeshift」**不成立**。分类器（`isSelfDefenseSkill` / `isSpeedBurstSkill`）确实都排除，但最终出招 argmax 有一条 fallback —— `botController.ts` 的 `pool = damaging.length > 0 ? damaging : offensive.filter(castableNow)`，**fallback 不筛 `hasDamage`**，而 `reduce` 在全员 `burstDamageOf` 为 0 时返回 `pool[0]`；`validateCast` 只拒 `TargetFilter.Enemy` 与 `Ally` 两种错位，`TargetFilter.Self` 的熊形态拿敌人当 target 照样验得过，于是它进得了 `offensive`。德鲁伊技能表里 `bear_form` 在下标 7，前面 6 个可用位（moonfire / entangling_roots / cyclone / skull_bash / barkskin，另两个是治疗被 `isHealSkill` 摘掉）全不可用时它就是 `pool[0]`。**对抗构造实测**：把除熊形态外的全部德鲁伊技能置入冷却 → `decideBotAction` 在 3m 与 25m 都返回 `druid.bear_form`。所以 druid 那一列**可能动**，且熊形态血池变大还会推迟 bot 的 35% 保命线（二阶行为变化）。**★ 更正二：列与列不独立。**报告是 round-robin 的**每职业总胜率**，priest/mage 读条变长会改掉所有与它们对局的那 7 列 —— 任何形如「其余六列逐位不变」的断言都是错的。**可证伪的断言要按对（matchup）写**：`--matrix` 下**不含 priest / mage / druid 的那 10 对**（warrior/paladin/deathknight/rogue/hunter 两两组合，C(5,2)）**必须逐位不变**；任何一对动了，说明还有一条没被识别的路径 —— 那才是真发现。方向：priest（90.5）与 mage（16.7）同向下行，mage 有跌破 hunter（14.3）成为新下界的风险，**极差大概率恶化而非收窄**。按 M14 规矩先归因再谈数值 |
| W27 | **`WeaponDef.skillModifiers` 整族 8 个字段零消费方**（与 W26 同族、规模更大：18 处数据源，且 docs/05 已把它们当装备规格逐行公示）：`data/schema.ts` 的 `SkillModifier` 定义了 `damageMultiplier` / `healingMultiplier` / `cooldownMultiplier` / `castTimeMultiplier` / `radiusMultiplier` / `durationMultiplier` / `rangeMultiplier` / `behindBonusDelta` 八个字段，挂在 `WeaponDef.skillModifiers`（同处另有 `WeaponDef.rangeMultiplier`）。全仓**唯一**读到 `skillModifiers` 的地方是 `data/index.ts:249-251` 的校验 —— 它只查键名指向的技能是否存在，**一个乘算值都不读**。**复现**：`grep -rn "damageMultiplier" packages/shared/src/sim` 零命中（`cooldownMultiplier` / `castTimeMultiplier` / `behindBonusDelta` 同样零命中，`data/classes/` 之外全仓无人读）。数据源 18 处：`warrior.ts:514`、`deathknight.ts:591/637`、`rogue.ts:707/730/750`、`hunter.ts:498/518`、`mage.ts:609/632/654`、`priest.ts:480`、`druid.ts:611/630/647`。而 `docs/05-equipment-system.md`（:27/41/43/49/50/51/58/65/67/81）把这些乘算当作装备规格逐行写进了文档（例：「rogue.backstab: damageMultiplier=1.15」） | ★ **`castTimeMultiplier` 的自然落点就是 W26 刚刚改过的那一行** —— `sim/casting.ts` 的 `castTimeOf()`，公式变成 `skill.cast.time × castSpeed × castTimeMultiplier`；`cooldownMultiplier` 落在 `casting.ts` 的 `caster.cooldowns.set`，`damageMultiplier` 落在 `effects/combat.ts` 的 `attackerMods.damageDealt` 同址 —— 三条都不必新开出口。★ **这条债自带一层自我印证**：`classes/priest.ts:472-474` 的降级注释写着「skillModifiers 也只支持 damageMultiplier / cooldownMultiplier，无法按技能改读条时间」—— 作者以为那两个是**活的**，实际八个全死；同一段注释还是 W26 `castSpeed` 那条降级的出处。接线前须先决定 `radiusMultiplier` / `durationMultiplier` / `rangeMultiplier` 是接还是删 —— **删字段同样是还债**（口径见 W26 对 `absorbDone` 的取舍：零数据源时要么接、要么删，不许继续半死着） | 18 处装备数值 + docs/05 整章规格是一句不生效的承诺：玩家换了武器，技能强度什么都没变，而 `advantage` 文案说变了 | **已交付 —— 八个字段全部接上**（2026-08-11，用户拍板「对体验友好就接」）。**① 唯一入口**：`sim/modifiers.skillModifierOf(entity, skillId)`，八处消费点一律走它（六处各查一遍 `getWeapon()?.skillModifiers?.[id]` 迟早漂移成「换了武器伤害变了、冷却没变」）；武器级 `WeaponDef.rangeMultiplier` 由派生的 `skillRangeMultiplierOf()` 与技能级相乘，**只作用于超出最长近战触及（3.8 米）的技能** —— 不加这道闸的话法刃法师的元素之刃（2.8 米近战）会跟着缩到 2.24 米，而 cost 文案写的是「**远程**技能」。**② 落点**：`castTimeMultiplier` → `castTimeOf()`（公式 = `cast.time × castSpeed × castTimeMultiplier`，两根轴相乘而不是取较强 —— 17.1 管的是多职业往同一人身上堆，这两个都只来自本人的一件装备）；`cooldownMultiplier` → 新抽的 `enterCooldown()`，**`finishCast` 与引导结算两处共用**（X10 追加轮之前两处各写各的，整批引导技能免费无冷却；再分家一次就是「暴风雪不吃武器的冷却缩减」）；`damageMultiplier` / `healingMultiplier` → `dealDamage` / `dealHeal` 的输出乘算同址；`behindBonusDelta` → 背刺加成，**与 W25 快照正交**（快照冻结「是否背身」这个事实管 if，delta 管乘数），只对本来就有 `behindBonus` 的技能生效并夹到 ≥ 0；`durationMultiplier` → applyAura 时长、**递减之前**（放到之后会得到「第三次控制比第一次长」）；`radiusMultiplier` → `resolveCastTargets` 经 `aiming.scaleShape`（缩外沿：circle.radius / ring 内外径一起 / cone.range / line.length / chain.jumpRange；角度与宽度不动）；`rangeMultiplier` → `validateCast` 的四处距离检查（`maxRangeOf()`，**只缩上界**，`range.min` 是冲锋「太近不能用」的机制门槛）。**③ 分层**：`casting.ts` 的零数据注册表纪律**保住了** —— 新增 `SkillModifierAccess` 注入接口（`modifierOf` + `rangeScaleOf`），生产路径只有 `tick.ts` 的 `weaponSkills` 一处，四个入口（beginCast / beginCastOrQueue / tickCasting / tickCastQueue）共用同一个对象。⚠️ **如实记：客户端的两处施法预检**（`CombatDirector` 的 `describeCastBlockers`/`canReach`、`SnapshotCombatView` 的距离判定）**不传这个对象，因此 HUD 变灰不跟 rangeMultiplier** —— 今天零数据源所以零分歧，一旦有武器填了这个字段就会出现「HUD 说够得着、按下去说超出距离」，必须同步（本批禁区是 client，X26 并行占用）。**④ W25 预警逐条核过**：`ctx.skillId` 在投射物（碰撞/锁定/延迟落点，`tick.ts` 传 `hit.projectile.skillId`）与地面区域逐跳（传 `g.skillId`，法杖「强化暴风雪」正是靠它落到每一跳雪上）**都在**；普通攻击传的是字符串 `'autoAttack'`（查不到，且应该查不到 —— 白字的武器影响写在 `swingPercent` 上）；⚠️ **光环周期跳（DoT/HoT）传的是 `t.aura.def.id` 而不是技能 id**，所以 DoT 每一跳不吃武器加成、只有落刀那一下吃 —— 取舍不是遗漏（光环贴上就脱离了施法者的装备，按「跳的那一刻手里拿什么」缩放会把「上完 DoT 立刻换武器」变成一条操作技巧）。**⑤ 25 条数据源逐个亮灯**：18 条 damageMultiplier + 7 条 cooldownMultiplier，集成断言逐条驱动真实路径（damage 走「同一把武器、只换 skillId」的对照组；cooldown 走完整 `tickWorld` 并记下写入时刻）。**⚠️ 四条如实登记的空转项**（原因在数据不在接线，`damageMultiplier` 挂在效果树里根本没有伤害的技能上）：`druid.healing_touch`（纯治疗，作者想写的是 `healingMultiplier`）、`druid.bear_form` / `druid.cat_form`（形态切换零伤害，「动物形态伤害 +15%」要的是形态光环上的 `damageDealt`，`skillModifiers` 结构上表达不出来）、`mage.frost_nova`（只有 `root`，法刃「瞬发技能 +15%」逐个列举时没人检查它打不打伤害）。四条写死在 `skillModifiers.test.ts` 的 `KNOWN_INERT` 里，修好了断言会主动变红。**⑥ 零数据源的六个字段按 W26 `absorbDone` 口径「接上但零行为变化」** —— 不删：`docs/05` 的 cost 文案（法刃「远程距离 -20%」、法杖「范围能力强」）与 `classes/priest.ts` 的降级注释都在等这几个字段，删掉等于把承诺一起删掉。生效那一半的单测用 `withWeaponMod()` 临时挂表跑完还原，走完整生产路径。**⑦ 三段陈旧注释已更正**：`priest.ts:472`（原文「skillModifiers 也只支持 damageMultiplier / cooldownMultiplier」—— 它以为那两个活着，实际八个全死）、`mage.ts:585`（原文说 schema 缺 rangeMultiplier / radiusMultiplier / durationMultiplier，缺的其实是消费方）、`rogue.ts:745`（原文说 schema 没有 behindBonus 那一档修正）。**⑧ 本批只接线不调数**（`data/classes` 一个数字没动）。**⚠️ balance 必动、按主循环归因**：默认武器下**盗贼**（背刺/毒刃 +15%）、**死骑**（湮灭 +15%）、**猎人**（奥术射击冷却 ×0.75）、**法师**（暴风雪/陨星 +15%，且暴风雪每一跳都吃）净上升，**战士 / 圣骑 / 牧师**默认武器无改写、相对下降，德鲁伊仅纠缠根须冷却 -10% 微弱上升；非默认方案里削得最重的是**匕首+格挡短刃盗贼**（肾击 ×0.7、刺骨/背刺 ×0.85）。43 条新单测、typecheck / lint 零告警、`skillSmoke` 不掉。**✅✅ W27 收口（2026-08-11，独立复现后修复）**：**① major — `radiusMultiplier` 够不到嵌套载荷（铁律⑦第 9 处翻车）**：接线时只落在 `skill.shape` 上，可**真正决定 AoE 大小的半径写在嵌套 effect 自己身上** —— 暴风雪 `spawnGroundArea.radius: 6`、陨星 `delayedGroundImpact.radius: 5`、冰冻陷阱 `spawnTrap.triggerRadius`，而这三个处理器**根本不读 `targets`**，所以把形状吹大只是把一批没人用的目标算了一遍（实测挂 `radiusMultiplier: 2` 后区域半径**仍为 6**、落点半径**仍为 5**）。上批那条端到端用例挑的是 `druid.stampeding_roar`（SelfCenter + 直接吃 shape 的圆），正好是能过的那一类 —— 与前八次同族翻车逐字同形。修法取「同一个乘算」：`effects/displacement.ts` 新增 `radiusScaleOf(ctx)`（内部就是 `combat.weaponSkillModOf`，与 `scaleShape` 读的是**同一个数**、同一个唯一查表入口），三个生成类处理器各乘一次；无改写时 `× 1` 逐位不变。护栏：暴风雪/陨星各一条端到端（`tickWorld` 全程，点名不用群体咆哮）+ 一条**从数据现算**的清单断言，全仓 **9 个**嵌套半径逐个「无改写逐位不变 + 挂上乘算就跟着走」，新加一个自动进清单（第 10 个 `ffa.bouncy_mine` 挂在**消耗品**上，不在 `SKILL_BY_ID` 里 —— 消耗品不是技能，武器改写表结构上命不中它）。已用变异验证：还原任一处乘算，两条断言当场红。**② major — bot 估值 `burstDamageOf` 不吃 `skillModifiers`**：`botController.sumDamage` 全程只调 `magnitudeOf`，实测拔掉 `hunter.long_bow.skillModifiers` 前后估值**差值 0**，而同一发箭的实伤确实 ×1.15。后果落在选招 argmax 上 —— bot **用接线前的数字选招、按接线后的数字结算**：匕首圆盾的肾击被高估 43%（实伤 ×0.7）、双持剑战士的致死打击与死骑湮灭同族，反向低估的是法刃火焰爆与长弓瞄准射击。而 `scripts/balance-report.ts` 跑的正是 bot 对战，不同步会让 W27 的极差位移里混进「bot 选错招」的分量，与「数值真的变了」再也分不开。修法：`totalDamageOf` 把 `skillModifierOf(self, sk.id)?.damageMultiplier ?? 1` 透传进 `sumDamage`，逐条对齐结算侧那张核过的清单 —— 直伤 / 碰撞弹 / 锁定弹（递归带下去）/ 落点 / 地面区域逐跳**都乘**，⚠️ **唯独光环周期跳（DoT）不乘**（`tick.ts` 第 4 步传的是 `t.aura.def.id`，DoT 每一跳实伤本来就不吃武器加成，跟着乘只是把偏差换个方向）。治疗侧**不需要同样处理**：bot 选治疗只按 `isHealSkill` + 血量最低者，从不比较治疗量，没有 argmax 会被 `healingMultiplier` 带偏。护栏 5 条（加强/削弱/嵌套载荷各一条比值、无改写逐位不变、纯 DoT 技能挂上乘算后估值**逐位不变**）。**③ ⚠️ balance 归因口径**：本条修复**改变了 bot 的选招**，所以 W27 的 balance 位移必须在**修复之后**跑；修复前跑出来的数字含「bot 选错招」的分量，不可用于调配平。**🔵 未做（如实登记，minor/note 不修）**：ⓐ **`durationMultiplier` 同样够不到嵌套载荷** —— 它只落在 `combat.ts` 的 applyAura 上，`spawnGroundArea.duration` / `spawnTrap` 存续 / `delayedGroundImpact.delay` 全部直读 effect 字段，「暴风雪的雪多下 2 秒」「冰霜陷阱多摆 5 秒」今天写上去**无效**（零数据源、纯潜伏；已在 `displacement.radiusScaleOf` 的 ⚠️ 与 `mage.ts` 文件头写明「别以为半径接上了时长也接上了」）；ⓑ **`rangeMultiplier` 的距离口径客户端三处没跟上**：`casting.ts:118` 那句「距离检查有四处」说的是 **sim 四处**，客户端还有 `SnapshotCombatView.positionBlocker`（联网技能栏预检）、`CombatDirector.hudCastContext`（不传 `weapon`，试验场技能栏按未缩放距离判）、指示器三处（`directionIndicator.show(skill.shape,…)` 圈的大小不吃 `radiusMultiplier`）——今天全部潜伏（两个字段零数据源，`skillModifiers.test.ts` 有断言守着），冷却侧**没有**这个问题（联网技能栏读快照里的权威冷却，无本地乐观预测）。填数那一批必须连客户端一起接；ⓒ **`skillModifiers.test.ts` 的「18 条逐个亮灯」用的是合成载荷** —— `hitFor()` resolve 的是一条手写的 `{kind:'damage'}`，只换 `skillId`，证明的是「`skillModifierOf` + `dealDamage` 这一段接对了」，**不是**「这个技能自己的载荷在结算那一刻 `ctx.skillId` 还是它」（18 条里瞄准射击/火焰爆/陨星/暴风雪的伤害全在 onHit / onImpact / onTick 里）。已用一次性端到端变异测试逐条核过、**结论是这一族没问题**（长弓瞄准射击 1.150、法刃火焰爆 1.150、魔杖火焰爆冷却 5.6s=8×0.7、自然法杖纠缠根须 ×0.9、权杖驱散魔法 ×0.75、符文剑湮灭 1.150），所以这是**断言强度**问题不是行为缺陷 —— 换成端到端是下一批的活。**balance 已归因（主循环 2026-08-11 实跑，含 bot 估值修复后口径，--matrix 对比）**：极差 69.0→71.4pp，**deathknight 40.5→71.4（+30.9，本轮最大单批位移）** —— 默认双手符文刃的「湮灭 ×1.15」活了 + bot 估值第一次会优先按它，对 druid 0/6→6/6、对 rogue 0/6→6/6 整对翻盘；priest 78.6→66.7（默认法杖 castSpeed 叠加武器面）；rogue 21.4→14.3 新下界。⚠️ 这 18 处数值是**盲写的**（字段死了十几个里程碑，作者从没见过它们生效）—— 现在活了，数值本身从未被配平过。按 M14 规矩：调数不混在接线批里，湮灭 1.15 是死骑那列的第一旋钮，归 F5 真人轮或专门配平批 |

## X. 表现与内容打磨

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| X1 | **夺旗图 0 件装饰** —— 唯一没有 `decor` 字段的图（速赢清单「下一铲」） | `makeCtfDecor()`：30 件，地图常量的确定性函数、红蓝中心对称；否定式约束测试钉住「不挡中路/地道口/旗房/墓地」 | 灰盒观感 | ✅ 批次二 2.10（2026-08-05，速赢清单销账） |
| X2 | 教学图装饰仅 6 件「够用不够看」；走位环无专用地形 | `maps/tutorial.ts`；`PROGRESS.md` 教学分家章节 | 第一印象 | ⛔ |
| X3 | **死亡回顾无技能名**：协议 `Damage` 只带 school，需加 `skillId`（与 S7 的泄露口径一起拍板） | `Damage` 加 `skillId?`（`CombatEvent` damage 加 `skillId`，`dealDamage` 三处带 `ctx.skillId`）；死亡回顾用 `getSkill(skillId).name`（`autoAttack`→「普通攻击」，查不到退学派）。**泄露口径**：来源不可见时 `redactFor` 连 skillId 一起抹 | 协议债 | ✅ 批次四（2026-08-05，m13 #24 回顾显示技能名 + m10 #1e 抹除断言） |
| X4 | 结算面板无夺旗专属列（携旗距离/护送时长 sim 里都有）；连杀播报无「终结连杀」提示（数据够 UI 没做） | 夺旗列已清：`MatchStatsRow` 补夺旗/归还/截旗三列，面板按模式开关（15.4 否定式反向断言）。**余账**：携旗距离/护送时长等深度列、「终结连杀」提示 | 夺旗列 ✅；终结连杀仍欠 | 🔧 上半批次三 3.5（2026-08-05） |
| X5 | 装备满槽无「选哪件换掉」对比 UI（10.5 后半，M16 已知不足） | `hud/ArsenalHud.ts` | 武装竞技场体验 | ⛔ |
| X6 | 引导条 HUD 仍是读条口径（4 秒满格）——3D 法阵已按引导独立时间轴走，HUD 没跟 | 引导条按剩余时间**向左缩**（WoW 口径）+「引导」标记，玩家/目标/姓名板三处同源；普通读条逐字节不变。⚠️ 实现靠 index.html 的 `@keyframes cast-fill` 保形（测试只锁名字，锁不住内容 —— 改那条关键帧前先看 CombatHud 引导段注释） | 引导技能读条骗人 | ✅ P3（2026-08-07，hud 单测钉方向） |
| X7 | 盾自然过期无收束动作（过期不是破裂，是对的；但壳直接消失没有淡出） | 自然过期 0.3s 收束淡出（破裂仍是破裂动作，语义分开） | 小 | ✅ P3（2026-08-07） |
| X8 | 音效无技能级分化：91 技能共用 7 组学派音；盘里 `cast_chain_heal`/`cast_lightning_bolt` 等专用音零使用 | P3 签名系统：`av/skillSignature.ts` 双层（推导散列 + 八职业手写表 100% 覆盖），playCastFor/playImpactFor 换文件/变速/叠层；磁盘断链逐键测试 + 全局唯一性零例外门禁 + main.ts 入口源码锁。⚠️ 数值全是占位判断，听感校准归 **X23** | 大招没有专属声音签名 | ✅ P3（2026-08-07，11 agent） |
| X9 | 粒子次级动作（同爆发内各层错开 40-80ms）未做；池饱和度断言未补（三期已知不足） | `vfx/ParticleBurst.ts`；`SpellVfx.ts`（≈:464）注释 | 观感上限 | ⛔ |
| X10 | **真 GPU 上从未验证观感与帧率**：全部验收跑在 swiftshader 软渲染（空闲 4 FPS）下，「够不够炫」「掉不掉帧」都没有真机数据 | **首轮真机数据已入册**（2026-08-09，用户机器 Intel UHD 核显 + MX250 双显卡本，Chrome）：24 实体满编、镜头 18m 拉满 —— 核显高画质 **15fps / p95 94ms**（Chrome 默认挑它）、低画质也只 23fps/p95 75ms；强制 MX250 高画质 **33fps / p95 42.5ms**、低画质 26fps/p95 72ms。三个结论：① `powerPreference: 'high-performance'` 缺失一行值一倍帧率（已修，SceneShell）；② **低画质绘制调用砍 60%（651→265）帧率反而更差 ⇒ 瓶颈偏每实体 CPU 开销**（骨骼/粒子/姓名板），P9 自动降档在此类机器上救不动，P4 的钱该花在 CPU 剖析；③ 二分（17/11/5 实体）无明显拐点，5 实体 avg 就有 17.6ms，地板偏高。**观感（够不够炫）的肉眼判定仍缺**。**观感半边首批已改**（用户拍板批 2026-08-10，原话「目前体感上陨星和霜矢区分不大（霜矢应该类似一个会飞的短冰矛），陨星应该是很大的视觉差别的」）：① 霜矢改短冰矛 —— `boltVfx.ts` 新增 `BoltForm`/`SKILL_BOLT_FORM` 技能级弹体形态覆盖（与 `SKILL_WEATHER` 同套路），核拉成八面梭形（长径比 8.6，球恒为 1）、前方四棱冰锥、亮核挪到矛尖、两个圆 Sprite 压到三成半（它们正是「怎么看都是球」的元凶）、绕轴三根偏心冰晶 + 自旋 6rad/s、彗尾条收窄拉长；② 陨星 —— `groundVfx.ts` 新增 `FallPlan`/`ImpactPlan`，延迟落点此前**天上什么都没有、落地也不补**（`syncProjectiles` 的 delayedImpact 分支到 continue 就结束），现在 26 米高 1.15 米半径的翻滚岩块自由落体砸下（火尾走**零池占用**的彗尾条，细流池 48 格预算一格不动）＋ 落地错峰三层（主爆 / 掀尘 / 腾烟）＋ 判定半径冲击波 ＋ 3.2 秒焦土。通用档逐字段等于改动前，未登记的技能一像素不变。**真机肉眼终验仍缺** | 12v12 帧率已知：入门独显≈30fps 可玩、核显不可玩 | 🔧 帧率数据✅（2026-08-09 首轮）／观感：霜矢+陨星✅（2026-08-10）、其余技能肉眼轮 + 更强 GPU 数据点仍缺 |
| X11 | 多语言未做（HUD/大厅/教学全中文硬编码，无 i18n 层） | M13/M15 已知不足 | 发布范围决定 | ⛔ |
| X12 | 武器无背后收纳（`ui_weapon_sheathe` 音效已备好） | M12 已知不足 | 小 | ⛔ |
| X13 | **法师没有任何群体减速/脱身手段**（用户实测：「被一堆近战追着打很快就嘎了」）：只有霜矢的单体减速 + 5 米霜爆新星（18s）+ 瞬闪（15s）。WoW 的冰锥术（锥形群体减速）在规格 9.x 的法师技能表里**本来就没有** —— 是**设计缺口不是 bug**，加不加需要拍板（同类问题可能也存在于其他远程职业） ⇒ **群体减速半边 P3b 已补**（冰锥术，锥形 90°×10m / 50% / 5s）。**控制扩展半边**（用户拍板 2026-08-10 原话）：「法师没有群体控制减速脱身，我理解应该是很多职业的技能带有减速效果，尤其是**法师的冰系减速技能应该有一些持续减速的效果**，**火系应该有一些击晕的效果**，这些没有」。**先审计后动手**，八职业 × 控制类型现状表（改动前）：战士 断腿斩 40%／突进 0.75s 昏迷／掷锤 2s 昏迷／破胆怒吼 4s 恐惧；盗贼 毒刃 50%（毒）／昏击 0.6s×连击点（5 点 3s）／偷袭 2s／致盲 4s；猎人 震慑箭 40%／断筋 50%／寒霜陷阱 3s 迷惑；死骑 寒缚链 60%→0 衰减／凛冬号叫 40% 5s／凛冬领域 30% + 四次命中 1.5s 昏迷／扼喉 2s 昏迷；法师 霜矢 30% 3s／冰锥术 50% 5s／暴风雪 30%（区域内每 0.5s 刷新）／霜爆新星 2s 定身／化形术 4s 迷惑；**圣骑士 一条减速都没有**（只有裁决之锤 2.5s 昏迷）；牧师 恐惧 2.5s + 沉默 3s，**零减速**；德鲁伊 缠根 3s 定身 + 旋风迷惑，**零减速**。⇒ 三条落地：**① 冰系「寒冷」族**：霜矢的 chill 从 30%/3s 提到 **40%/4s**，并让**冰枪术**（瞬发零冷却，被追击时真正一直在按的那个键，此前一点减速都不带）施加**同一枚**光环 `mage.frostbolt.chill` —— 「不持续」的真正病根是**接不上**而不是数值不够；共用一个 id 是刻意的：拆成两枚时减速取最强数值等价，但**一次驱散只清得掉一枚**，反制链会断（WoW 参照：寒冰箭的 Chilled 族，40%/4s 取正式服口径；冰枪本体在正式服不减速，靠「碎冰」吃冻结加成，本仓库无此机制故并入寒冷族）。**② 火系击晕**：**陨星**落地 **1.5 秒昏迷**（`DrCategory.Stun` 全套递减/免疫/抗控/战斗意志可解；WoW 参照 火系天赋 **Impact**「火焰法术有几率昏迷 2 秒」，本仓库把「低几率×高频率」换算成**「必定×长冷却」**—— 几率触发的硬控在 PVP 里不可预读、写不出反制方式，与附录A#3 冲突）。**③ 圣骑士裁决**补 **30%/4s 减速**（WoW 参照 **正义审判/Judgement of Justice**，作用正是「让目标逃不掉」；量级取全场最弱一档，因为它是 10s 冷却的常驻键）。**明确不补的**：牧师（WoW 的牧师减速长在**精神鞭笞**上，本仓库没有这个技能；已有恐惧+沉默两条硬控）、德鲁伊（WoW 的德鲁伊减速是**感染伤口/飓风术/寒冰之环**，宿主技能一个都不在本仓库；已有定身+旋风）—— 两者同时是当前基线胜率最高的两档，「零减速」在它们身上是设计决定不是遗漏。**明确不做的**：烈焰爆（8s 冷却）加击晕 —— 8 秒一次的硬控等于读条职业永远读不完法术，还会反复吃满自己的昏迷递减窗口把队友的裁决之锤一起变半衰，那是压制性连控不是「一个击晕点」 | `data/classes/mage.ts` 技能表；docs/00 §9.x；`data/classes/mage.ts`（`FROST_CHILL` 常量 + 霜矢/冰枪/陨星）；`data/classes/paladin.ts`（`paladin.judgement.slow`）；测试 `sim/chillAndStun.test.ts` 25 条；**收口批**：`sim/projectile.ts`（落点阵营过滤 `matchesFilter`）、`sim/effects/displacement.ts`（`targetFilter` 接线）、`sim/effects/combat.ts` + `sim/aura.ts`（免疫新减速 / `impairsMovement`） | 远程职业面对多近战时无解 | ✅ **已落地**（2026-08-10 Wave3）。⚠️ **两笔如实记的余账**：① **陨星的击晕在 bot 对局里永远不会出现** —— `botController` 的 `usableOn` 直接把 `targeting === 'ground'` 的技能排除在选招池外（AI 不选落点），所以 balance 报告看不到这个击晕；`ccCategoryOf` 也**刻意没有**下探 `delayedGroundImpact.onImpact`（下探了也是一条永不执行的死分支 —— 本仓库不写那种代码）。真人手里它是生效的，测试从生产管线驱到了落地昏迷。② **圣骑士（85.7%）与法师（16.7%）分处基线两端，这一批同时加强了两者** —— 减速在当前 bot 模型里近乎惰性（P1b 的风筝已回滚，bot 只前压不拉扯），但如果下一轮 balance 里圣骑士继续上行，`paladin.judgement.slow` 是**第一个该回滚的嫌疑对象**，回滚成本是删一个 `applyAura`。**⇒ 同日对抗校验收口（2 blocker/major 修 + 1 major 记账处置 + 3 条记账不修）**：**①【blocker 已修】延迟落点落地时没有任何阵营判据** —— `tickProjectiles` 的 delayedImpact 分支在落地那一刻**重新圈人**（施法期 `aiming.ts` 的 `TargetFilter` 是 1.5 秒前的事），此前只看距离。伤害误伤是 M4 以来的既有缺口，X13 把一条**硬控**放上了这条路：最典型的用法「被贴脸时把陨星丢在自己脚下」变成**自己被晕 1.5 秒**、队友一起晕，而 counters 一个字都没提。修法：`DelayedImpact` 带 `targetFilter`（由 `delayedGroundImpact` 处理器从 `SkillDef.targetFilter` 带过来，缺省 `Enemy`），落地按与施法期**同一套语义**过滤（`matchesFilter` 分支逐条对齐 `aiming.ts`）；顺带修好大乱斗星火倾泻的同一条误伤。⚠️ bot 永不选地面技能 ⇒ balance 报告**永远抓不到这条**，只有 `chillAndStun.test.ts` 的两条集成用例守着。**②【major 已修】「免疫新的减速与定身」对减速从未生效过** —— `immuneMovementImpair`（自由庇佑）/ `immuneSlowAndRoot`（剑刃风暴）此前**只在 `applyControl` 的 root 分支**被读过，而减速是一枚普通光环、走 `applyAura` 处理器，于是全仓库每一条减速（寒冷/断腿斩/毒刃/寒缚链/凛冬号叫……）都挡不住 —— 与寒缚链 decay、疾行步 moveSpeedFloor 同族的静默失效。X13 把寒冷改成瞬发零冷却每 GCD 刷新之后这条洞变成决定性的：法师侧与圣骑士侧**本批新写的 counters** 都把自由祝福当反制手段卖给玩家。修法：`applyAura` 处理器补一条免疫判定，判据复用 `aura.ts` 新导出的 `impairsMovement`（减速 ∪ 定身）—— 与自由庇佑自己那半边 `dispel(impairs:'movement')` **同一条谓词**（「解得掉的」与「挡得住的」必须是同一个集合）；`applyControl` 里两条重复的 root 判断合并成一条，**只对 root 生效不是遗漏**（昏迷/恐惧/迷惑不属于「移动限制」，8.3 的通用解控是战斗意志）。**③【major 记账处置 (b)】裁决的两枚魔法减益不对称** —— 裁决在同一次命中里挂 `paladin.judgement`（易伤，casterScoped）+ `paladin.judgement.slow` 两枚 `DispelType.Magic`，而全花名册唯一的「清友方魔法减益」（牧师净化术）是 `count: 1`、`dispel()` 取 `eligible.slice(0, limit)` 按入表顺序 ⇒ **一次驱散清掉的永远是易伤，减速原样留着**，正是本批为寒冷写下的那条禁忌（法师侧遵守了，圣骑士侧因 `casterScoped` 硬约束**做不到**合并 —— 合进易伤那枚，减速会因为 `moveSpeedMultiplierOf` 不传 attackerId 而永远不生效）。三条出路都称过：**(a)** 逐字段 casterScoped —— 需要给 `AuraModifiers` 定义一张「哪些字段与攻击者相关」的分类表，而 11-contributing §4 的「三次即入 schema」这里**才第一次**，为一枚光环引进模糊分类正是本仓库反复吃亏的抽象；**(c)** 给 `dispel()` 引优先级（同类别先清减速）—— `control.stun` / `control.fear` 也是 magic 减益，「先清减速」等于让一次驱散去解减速而不是解昏迷，是明确退步；**⇒ 取 (b)：保持两枚，把口径改成如实的** —— counters 改写为「一次驱散魔法只带得走一枚（先清掉的是易伤），想解减速要么再驱散一次，要么用自由庇佑这类专解移动限制的手段」，并由 `chillAndStun.test.ts` 的圣骑士侧三条用例钉住这条不对称（哪天 (a) 落地，那几条会红，提醒把文案改回去）。**④【minor 记账不修】陨星的图标核语义 `damage → stun`** —— `skillIcon.ts` 的 `CORE_KINDS`「顺序即优先级」里 stun 排在 damage 前，`flattenEffects` 早已下探 `onImpact`，所以加了落地击晕之后陨星的主形状从「向上尖角（伤害）」变成「断裂环（昏迷）」。交付摘要里「表现层零改动即可」的说法**不成立**：程序化 SVG 不是只在素材缺失时才画（素材开关默认 false），每局开局那段时间里法师最大的单发伤害键在动作条上读起来是一个控制键。要么接受（14.3/17.2 的图标语义规则的正常结果），要么给「既伤害又控制」的技能在 `coreKindOf` 里加一条更细的规则 —— 本轮只记账。**⑤【minor 记账不修】`mage.ice_lance` 的 counters 仍说「冰霜学派锁定」** —— P10 补丁（commit 6e32507）已把 counters 划进玩家可见 UI、口径统一为「系」，而这一行是本批亲手重写的；同文件另有 6 处 P10 没扫干净的**既有**残留，一并留给下一轮文案批（本收口批按「minor 不修」的分工只记账）。**⑥【note 记账】「共用一枚 ⇒ 一次驱散清干净」只在单个法师时成立** —— `applyAura` 的刷新判据是「同 id **同来源**」（`aura.ts:87`，同函数注释写明「不同来源的同 id 光环各存一份」），两个法师同点冰枪时目标身上仍是两枚 `mage.frostbolt.chill`，一次 `priest.dispel_magic` 照样只清得掉一枚。数值上无害（减速取最强），但 `FROST_CHILL` 的注释与 `chillAndStun.test.ts` 的驱散用例把它写成了**普遍结论**、没有限定「同一个施法者」—— 别拿这条结论去反对某个多来源场景的改动。**balance 归因（主循环 2026-08-10，三轮隔离实验）**：X13 初值（裁决 30% 减速）使 圣骑 85.7→92.9 与 牧师 92.9→85.7 对调、登顶；禁用裁决减速单独一项即逐位还原（自由庇佑修复对 bot 数字零贡献）—— 预登记回滚判据触发。处置取**降档而非回滚**（用户拍板的题意是「职业都要有贴题减速」，圣骑是唯一零减速职业）：30%→**20%**/4s 实测不再对调（priest 90.5 / paladin 85.7），且极差收窄到 **76.2pp（全程最低）**，死骑顺带 40.5→42.9。终表：priest 90.5 / paladin 85.7 / druid 71.4 / warrior 42.9 / dk 42.9 / rogue 35.7 / mage 16.7 / hunter 14.3。陨星击晕与寒冷族对 bot 数字结构性无贡献（bot 不选地面落点、不风筝），真实价值在真人手里 —— 归 F5 |
| X14 | **阵营区分只做了姓名板**（P3a）：规格 00 §777 要求「姓名板、**脚下标记**、**轮廓**和 UI」四个通道，本轮补了姓名板（阵营色 + ▲/◆ 字形）。P10 补了 **UI 通道**（目标框敌我：语义色 + ▲/◆ + 「友方/敌方」标签 + 血条随阵营变色）。**第三、四通道已做**（用户拍板批 2026-08-10 Wave1.5）：新建 `vfx/FactionRing.ts` 的 `FactionRings` —— ① **全体脚下阵营标记**：友方连续实环（0.30–0.40）／敌对八段虚线环（0.30–0.44），17.2 双通道是「颜色 + 实虚线 + 外沿尺寸」三条，色盲色板换完色相仍分得出；几何体与材质**按阵营各一份**、逐实体只 new 一个 Mesh（24 人局 48 Mesh / 3 几何体 / 4 材质，P8 合批教训，单测钉死）；不 import 画质过滤（与 TargetRing/StatusMarkers 同一把锁），纯程序化零贴图故 `?art=off` 可构造；与 TargetRing 的层级分工是「半径不重叠 + 静止 vs 自转 + 低不透明度」，选中环仍最醒目。② **轮廓走便宜路**：放大一圈的背面胶囊壳（BackSide + 加法混合 + **保留** depthTest，角色自己挡掉中间那块，剩下探出剪影的一圈就是轮廓）—— 不上后处理 outline，理由是 X10 实测这类机器 CPU/GPU 都紧。⚠️ **诚实的局限（仍开）**：胶囊是**近似**剪影，武器/张开的手臂探出壳外那一段没有描边；`rim` 开关默认开，真机肉眼看过再定强度（占位 opacity 0.13）。**场景接线已做**（Wave1.5 收口）：两场景各挂一个 `FactionRings`（**不受 `art` 门禁** —— 与目标环同一条理由：「谁是敌人」是可读性信息不是美术层），名单走 `scenes/sceneViews.ts` 的 `factionRingViewsOf`。`hidden` 的判定**必须**在投影里做而不是在 `FactionRings` 里（那个类读不到死亡/第一人称，硬猜只会猜错，而漏判比不画更糟 —— 人看不见但脚下有环等于给对手报点）；潜行**不必**再判一次：两场景喂进来的名单本身已经过滤过（联网侧不可见的实体压根不进快照，试验场走 `visibleEntities()` 的 `isSelectableBy`）。阵营口径是「和我同队才是友方」，于是大乱斗（人手一个队号）自然全场敌色、BOSS（`TEAM_NEUTRAL`）也自然是敌色，一个分支都不用写。位置与目标环**同源**（联网 `renderedPositionOf`、试验场自己走渲染插值后的 `rendered` —— 顺手修掉了「自己脚下的环按 sim 固定步长跳」这个新引入的隐患）。两场景的自检对象各加 `factionRings: { count, rim }`（`FactionRings` 刻意不读画质，「低画质下没被裁掉」只有端到端验得出来）。测试 `scenes/sceneViews.test.ts` 10 条。**余账**：`verify-m12.mjs` 的 #48 类断言（低画质下 `factionRings.count === 实体数`）还没补 —— 那是浏览器脚本，不在本批的可跑范围 | `hud/CombatHud.ts` `renderNameplates`/`renderUnitFrame`（已做）；`vfx/FactionRing.ts`（新，全体标记 + rim 轮廓）；`vfx/TargetRing.ts`（仍只服务当前目标，两者刻意分层） | 12v12 可读性 | 🔧 姓名板✅（P3a）+ 目标框✅（P10）+ 脚下标记✅/轮廓✅便宜路 + 两场景接线✅（2026-08-10 Wave1.5）／**真机肉眼（轮廓够不够、rim 强度、与护盾壳叠加糊不糊）仍开** |
| X15 | **无指针锁定**：右键拖转身被窗口宽度封顶（1366px 窗实测拖 1200px 只转出 149°，光标顶到屏幕边缘 movementX 归零），贴身缠斗转半圈就得松手重来。全仓零 `requestPointerLock` | P10 审计（真机量化）。⚠️ **刻意没在 P10 修**：verify 脚本靠合成鼠标事件驱动镜头，指针锁定改吃 movementX 后合成事件口径要整体重定 —— 动 verify 基建的事单独一批做，不混进 60 文件的 UX 批 | 镜头手感的地基缺口 | ⛔ |
| X16 | **镜头/输入参数全部写死且不可设置**：鼠标灵敏度（0.0045）、转身速率（3.2）、FOV（60）、Y 轴反转、自动跟随全无 setter，设置面板零暴露；「全部按键可重绑」（17.2）也没做到 —— 移动六键/Home/K/F1/F3/F4 都不在可重绑表里（左手党/AZERTY 最需要的恰是移动键） | `CameraController.ts` CAMERA 常量；`InputManager.ts:122`；`SettingsPanel.ts` ACTION_LABELS | 无障碍与手感个性化 | ⛔ |
| X17 | **目标框/自身框无光环行**：DoT 剩几秒、有没有吸收盾、我的减益掉没掉 —— 全靠战斗日志一条通道（P10 已把日志改成显示名，但这仍是日志不是面板）。`HudUnit` 没有 auras 投影，`controlBadges` 只出 5 个控制旗标 | `hud/CombatView.ts` HudUnit（新增可选 `auras`）；新建 `hud/auraRow.ts`；`CombatHud.renderUnitFrame` / `renderSelfAuras`；index.html `.aura-*` / `#self-auras` | 目标制 PVP 的核心信息盲区 | ✅ **HUD 面已做**（用户拍板批 2026-08-10 Wave1.5）。落地：`HudUnit.auras?: readonly HudAura[]`（**可选**，理由与合同 C1 的 `gcdRemaining` 一字不差 —— 两个生产方任一没跟上时「没填」必须是合法状态；空行渲染成空串 ⇒ 未接线时目标框 HTML 逐字节不变）；`HudAura` 只带 HUD 真读的六项，剩余秒**由 `expiresAt` 与 `CombatView.now` 现算**（发事实不发 remaining，与 P11 给 `AuraSnapshot.expiresAt` 同一条：快照被插值重读时 remaining 越读越旧）。目标框/焦点框/自身三处共用 `auraRowModel + auraRowHtml`（口径分叉的话同一枚光环会在两个控件上显示不同秒数）。**S7 掩码中性显示**：`HIDDEN_AURA_ID` 一律 `kind:'unknown'` + 中性灰 + 不查图标 —— 给了 `school` 也不许用，那是把服务器刚掩掉的东西从旁边漏回去（单测钉死）。17.2 三通道：边框色（增益友方色/减益敌方色）+ 角标字形（＋/－/·，与 `BLOCKER_GLYPH`/`CONTROL_VISUALS`/姓名板 ▲◆ 逐张比对过不撞）+ class。上限 8 枚 + `+N` 溢出计数（截断不是悄悄丢掉）；14.3 护盾带一条随剩余吸收量退下去的条；图标沿用 `skillIconUrl`（光环 id 去掉最后一段再查一次，落不到就色块 —— 猜错一张图比没有图更糟），素材没探测通过时**一律色块**（否则每秒几十个注定 404 的 `<img>`）。测试 `hud/auraRow.test.ts` 16 条。**两个生产方已接**（Wave1.5 收口）：① 联网侧 `SnapshotCombatView.toHudAura` 从 `AuraSnapshot` 投影；② 试验场侧新建 `scenes/LocalCombatView.ts`（`CombatDirector` 外面薄薄一层，只把 player/target/focus 三个单位换成「浅拷贝 + auras」）—— **不直接改 director** 的理由：`CombatDirector.player` 是 `readonly` **字段**、类内上百处直接用它，要给它挂光环得先把字段改成 getter，那是一次与光环行毫无关系的大改；姓名板不读光环，所以 `visibleUnits()` 原样返回实体本身，12v12 里不会每帧多出 24 个拷贝。测试 `net/snapshotAuras.test.ts` 7 条 + `scenes/localCombatView.test.ts` 6 条（后者一路驱到真的套上霜甲护盾，断言自身框与目标框走同一条投影 —— 三个 getter 漏一个是静默的）。**新余账（联网侧的诚实降级）**：快照**不带** buff/debuff 向也不带名字，所以联网路径上除了 `control.*`（那个 id 是 sim 自己拼的、同处写死 `kind:'debuff'`，是结构事实不是猜测）之外，`kind` 一律 `'unknown'`、`name` 一律不填 —— 光环行因此在联网局里是一排中性灰格子（有图标、有秒数、有层数、有护盾条，但读不出增益/减益）。两条出路：给协议加 1 bit 的 kind，或按「技能数据表里的 `applyAura` 效果」在客户端建一张 `auraId → AuraDef` 注册表（`net/visibility.ts` 说的「仓库里没有这张表」到今天仍然成立）。哪条都不该顺手在接线批里定 |
| X18 | **姓名板三件小账**：互相重叠无避让（12v12 实测 3 对重叠、血条压别人名字上）；底部锚定导致读条出现/消失时整板上跳 6px（点击热区在光标下移动）；4px 血条无数字/低血变色（「谁进斩杀线」看不出） | P10 审计（`CombatHud.renderNameplates`；index.html `.np-hp`）；新建 `hud/nameplateLayout.ts` | 混战可读性 | ✅ **三件全做**（用户拍板批 2026-08-10 Wave1.5）。① **避让**：`stackNameplates()` 按屏幕落点把压在一起的板**向上**推刚好够的量（板高 + 2px 缝，不按固定档位跳出大空隙），横向距离先剪枝、抬高封顶 96px（抬到天上的板比重叠更难认、还会被上沿剔掉）。**迟滞是非对称的**：抬高只要压人就立刻抬，往回降要够本（>8px）才降 —— 没有它两块板会在「刚好挨着」的边界上每 50ms 换一次高度，看起来是名字在抽搐；排序键把 y 量化到 8px 再按 id 兜底，镜头微动不换名次。成本纪律照 X10 的点名办：**只在 20Hz 那一拍算**，且 `needsRelayout()` 判定集合没变 + 位移均 ≤5px 时连算都不算，位置仍每帧跟镜头（错位量最多滞后 50ms，肉眼无感）。② **上跳**：`np-cast` 改**常驻节点**，无施法时只加 `.idle`（CSS `visibility:hidden` —— 保留占位正是它与 `display:none` 的区别）。这条修的不只是难看：姓名板是 5.2 的点击选中面，热区在光标下跳意味着「点了没选中」。③ **低血**：<35%（与 bot `TACTICS.SURVIVAL_HEALTH` 同数，跨包源码断言钉住）变色 + 冒出百分比小字（17.2 第二通道），百分比**绝对定位**不占布局高度（否则等于换个地方再犯一次 ② 的错）。⚠️ 低血色取 `neutral` **不取 `danger`** —— 四套色板里 `danger === hostile`，而敌人姓名板本来就是那个颜色，用它等于一点都看不出来（这条判据连同理由写在 `lowHealthColor` 上，单测遍历四套色板）。测试 `hud/nameplateLayout.test.ts` 15 条 |
| X19 | **走进另一个角色被彻底顶住**，与代码自称的「软推开…可以穿过」相反：`SEPARATION_STRENGTH: 8` > `MOVE.BASE_SPEED: 7`，高重叠时分离推力恒压过自走速度（实测按 W 三秒只前进 2.9m 后卡死）。⚠️ **平衡敏感刻意未动**：分离度改动会波及所有近战 bot 缠斗，必须走「单独一步 + balance 归因 + 恶化回滚」的流程，不混 UX 批 | `sim/movement.ts:36`；修法候选：降到 3–4，或去掉玩家 wish 方向分量只留侧滑 | 近战贴身手感 | ⛔ **需单独归因批** |
| X20 | **新手教学固定法师**：教学分支不吃 `?class=`，选了战士进教学还是法师（P10 已在按钮文案标注） | `main.ts` 教学分支 | 教学与所选职业脱节 | ⛔ |
| X21 | **排队窗过期无反馈**：0.4s 排队窗（P10）过期后按键仍是静默消失 —— 迟到 0.4 秒的失败提示可能比沉默更误导，**要不要提示、提示什么需拍板**，不是漏做 | `sim/casting.ts` 排队消费处注释 | 「按了没反应」残留的最后一角 | ✅ **拍板了：要提示，但换一路说，不复用失败提示**（用户拍板批 2026-08-10，任务 D）。**五层全通**（sim + 协议 + 服务器 → HUD 面 → 两场景接线，Wave1.5 收口）。落地：`CastEvents` 新增 `onQueueExpired(caster, skill, { queued, waited })`，`tickCastQueue` 过期分支调它（`onFailed` 仍然不发 —— 迟到的 `onGlobalCooldown` 才是误导源，换通道不是加噪音）；新协议私信 `CastQueueExpired { skillId, waited }`（照 M10 全套：union + `ALL_SERVER_MESSAGE_KINDS` + `referencedEntities` 归私信一列返回 `[]`，零实体引用；刻意不给 `CastFailure` 加枚举值 —— 客户端 `FAIL_TEXT: Record<CastFailure,string>` 会因此编译不过，那是跨包破坏）；`MatchLoop` 的 cast sink 接上私发。bot 永不排队的三道保险一字未动（另加一条副作用测试钉住）。测试：`sim/castQueue.test.ts` +3、`MatchLoop.castQueue.test.ts` +2。**HUD 面已备好**（2026-08-10 Wave1.5）：`CombatHud.flashQueueExpired(skillId: string, waited: number)` —— 对应技能格短促红闪 + 「没赶上 0.4s」小字，非模态、约 1.1 秒自动消失（比中部提示的 1600ms 短，它只是回执不是要求改变操作的信息）。三条实现纪律：① **措辞说「没赶上」不说任何一种「不能放」** —— 迟到的失败提示正是 sim 侧刻意不发 `onFailed` 的原因，从这条通道请回来等于白拍板；② **两个静态相位（hot 260ms → fade → 消失）而不是 CSS animation** —— 技能栏每 50ms 整块重建 innerHTML，挂在格子上的 animation 会跟着重播，一次短促红闪就变成每秒 20 次频闪（`sk-press` 能用动画是因为它只播一次且发生在两次重建之间）；③ 小字**绝对定位、盖在格子自己身上**不进流，否则技能栏整体长高一行再缩回去 —— 那就是 X18② 刚修掉的那个毛病换个地方再犯；也不往格子外面飘（上方那条带子归 X17 的 `#self-auras`）。与 `.sk-block` 的四类阻碍长相刻意不同（那是「不能放」，这是「按早了」），两者可同时出现。测试 `hud/hud.test.ts` +6。**场景接线**（Wave1.5）：两条通道共用 `scenes/sceneViews.ts` 的 `queueExpiredFlash(msg, hud)`（它负责 `SkillId → string` —— HUD 内部用 `String(skill.id)` 比对，不转就永远匹配不上，而那种失配**是静默的**，闪不出来和「没人接线」长得一模一样）。联网侧 `NetworkScene` 新增 `case 'CastQueueExpired'`；试验场侧 `CombatDirector` 新增可选钩子 `onQueueExpired(caster, skill, waited)`（在 tick 的 cast 事件里**只转发、一个字都不写日志** —— 从这条通道请回一条 fail 日志就等于把刚拍完的板拆掉），`TestbedScene` 接上并按 `caster.id === player.id` 过滤（技能栏画的是玩家的九格，闪假人的排队纯误导）。测试 `combat/queueExpired.test.ts` 5 条（读条中再按一格 → 0.4s 后响一次且只响一次、caster 传得出来、不写「无法释放」、不接钩子时一切照旧）+ `scenes/sceneViews.test.ts` 2 条 |
| X22 | **命中爆发的视觉签名未穿线**：P3 的 tint/scale/form 生效在释放爆发/弹道/落地三处，**命中**爆发退回纯学派色 —— `SpellVfxEvent.damage` 不带 skillId（协议 Damage 与 sim damage 事件里都有，X3 起），穿线即可；⚠️ 来源不可见时协议抹 skillId，回落学派是正确行为（音频侧同口径已实现，照抄 HitFeedback 的写法） | `vfx/SpellVfx.ts` damage 分支；喂数据两场景各一处 | 命中一瞬的技能识别度 | ⛔ |
| X23 | **P3 签名的听感校准轮从未有人耳听过**：全部 castRate/impactRate/音色选择都是按文件名语义推的占位判断（各表已逐条标注）。最高风险三类：mob_* 吼叫的原始时长（死骑凛冬领域/凛冬号叫/缚魂拽三条 bark 压速后可能拖尾）、ui_* 取材的出戏风险（猎人 ui_weapon_unsheathe 绞盘、德鲁伊 ui_sheep 风笼、盗贼 ui_craft_* 两条）、foley 音量基准（德鲁伊 move_land 形态落地可能被战斗混音压没）。与 X10 真机轮合并跑最省 —— 一边看帧率一边听签名 | 八张 `av/signatures/*.ts` 的 ⚠️ 注释就是校准清单 | 签名批的真正验收 | **拆半**：语义校准轮 ✅（用户拍板批 2026-08-10）／真机人耳终验 ⛔ 仍开 |
| X23a | **语义校准轮**（✅ 已销）：用户口径「冰系要有结冰声、火系要有火焰燃烧声，其他职业也一样，声音要贴合技能」。八表逐条过，**16 技能 / 18 个音效键**改动（死骑 5 / 法师 4 / 猎人 2 / 盗贼 2 / 德鲁伊 1 / 战士 1 / 圣骑 1；牧师与大乱斗派对表复核后零改动）。定下选音优先级写进 `skillSignature.ts` 文件头：学派素材 → 动作素材 → 借小怪音要过三关 → 唯一性靠门禁不靠妥协语义。三类高风险的处理：① mob_* 吼叫**实测**（ffprobe）—— X23 点名的死骑三条压速后是 0.98/1.11/1.64 秒，均短于学派基线 cast_fire 2.30 秒，**拖尾担心证伪**；真正拖尾的是没在名单上的圣骑义愤 `ui_gather_legendary` ÷0.8 = **4.11 秒**（全批最长），已换 cast_holy。借小怪音改判为**误报风险**（场上真有小怪）与**跨职业撞脸**（战士破胆怒吼与牧师惊惧尖啸同文件同倍速）而换。② ui_* 四条**全换**：猎人绞盘→`lockpick_advanced_1`、德鲁伊风笼→`proj_nature`、盗贼致盲→`melee_swing_light_7`+`foot_dirt_1`、盗贼烟雾弹→`proj_shadow`；并定「羊叫只留给真有羊的地方」（法师变形术），猎人寒霜陷阱/盗贼致盲的 `ui_sheep` 同批退场。③ foley 音量基准**实测证伪**（ffmpeg volumedetect）：素材库按峰值归一，move_land_1~4 峰值 −7.2/−6.3/−6.2/−5.7 dB，不低于 impact_fire/cast_frost 的 −7.2 dB，无需调整；签名层也无音量轴，唯一旋钮 `AudioManager.IMPACT_LAYER_VOLUME` 留作真机退路。启用了盘上从未用过的 `foot_snow_*`（唯一的碎冰质感）承担「结冰声」 | 八张 `av/signatures/*.ts` 的 X23 校准注释逐条写明改前改后与理由 | 冰系终于像冰、火系本来就对 | ✅ **用户拍板批（2026-08-10）** |
| X23b | **真机人耳终验仍开**（⛔）：X23a 校准的是**语义**不是听感 —— 全批 117 条签名仍然没有人耳听过，castRate/impactRate 的具体数值、叠层配比、以及几处「就地取材」的替代是否成立（`lockpick_advanced_1` 会不会偏 UI 提示音而非实体机构音、`foot_snow_*` 踏雪能否读作碎冰、`proj_nature` 能否读作风、义愤换 cast_holy 后丢掉的「上扬」轮廓可不可接受）都只有戴上耳机才能回答。每条替代的退路已写在对应注释里，省得下一轮重推。与 X10 真机轮合并跑最省 —— 一边看帧率一边听签名 | 同上；`skillSignature.ts` 文件头的选音优先级是复听时的判据 | 签名批的真正验收 | ⛔ **需真机 + 人耳** |
| X24 | **联网 CastResolved 不响施法音**：试验场 resolved 时响一声签名施法音、联网只播近战 swing —— 两场景不对称（P3 之前就存在，P3 只穿线没改行为）。补的话在 NetworkScene CastResolved 分支用 playCastFor | `scenes/NetworkScene.ts` CastResolved | 联网瞬发技能比试验场「哑」 | ⛔ |
| X25 | **法阵技能级签名的真机肉眼轮仍开**（Wave1.5 法阵换色批的余账）：色相预算已到 14.2 的 ±0.08 上限（117 技能实测去重 90/108），继续拉开只能靠非颜色通道 —— 新加的「外圈断齿数」（4–10 档）正常镜头距离下数不数得清没有人眼验过；纹章反向偏移的「暖芯冷边」对比、规模轴衰减 0.25 后大招法阵是否「看得出更大」，都只有真机能回答。⚠️ 弯路已写在 `vfx/castTint.ts` 文件头（「乘 2 再钳」把 90/108 压成 52/108），下一轮别再推一遍。与 X10 观感轮 / X23b 人耳轮合并跑最省 | `vfx/castTint.ts` 文件头 | 法阵批的真正验收 | ⛔ **需真机人眼** |
| X26 | **联网光环行读不出增益/减益**（Wave1.5 接线批的诚实降级）：`AuraSnapshot` 不带 buff/debuff 向也不带名字，客户端也没有 auraId→AuraDef 注册表 —— 联网局除 `control.*`（id 由 sim 拼出、结构上写死 debuff）外一律 kind:unknown + 中性灰、name 退回 id。图标/秒数/层数/护盾条都在，唯独「好事还是坏事」读不出。两条出路：① 协议给 AuraSnapshot 加 1 bit kind（最省，要过 P11 字节预算与 codec）；② 客户端按技能数据表 applyAura 建 auraId→AuraDef 注册表（顺带拿 name/school，还能替掉 visualForAuraId 的「id 去段猜技能」启发式）。运行时拼出的 id（control.*、party 增益）注册表里没有，仍需 unknown 兜底 | `net/SnapshotCombatView.ts` `toHudAura`；`net/visibility.ts` 注释 | 目标制 PVP 的光环可读性半格 | ✅ **已销（2026-08-11，走②）**。用户拍板原话「**体验上没区别就尽量减轻服务端负担**」—— 两条出路玩家看到的东西一模一样，而①每帧每实体每枚光环都要多发一位、还要过 P11 字节预算与 codec 改动，②则是**零协议改动、零带宽、零服务端 CPU**（技能数据两端同源，客户端手里本来就有全套）。**新建 `client/src/data/auraRegistry.ts`**：`auraId → {AuraDef, 施加它的技能}`，**63 枚**，懒建一次之后是纯 Map 查询。★ 三个来源缺一不可：`SKILL_BY_ID`（八职业 + BOSS + 大乱斗派对技能）、**消耗品两池**（`CONSUMABLES` + `PARTY_CONSUMABLES` —— 它们不是技能，`SKILL_BY_ID` 一枚都覆盖不到，而巨人化/战斗药剂正是玩家最常在自身框上看见的增益）、`BOSS_ENRAGE_AURA`（由 `sim/boss.ts` 在 30% 血线**直接**施加，不经任何 applyAura）。武器/护甲**结构上没有**光环（它们带的是 `AuraModifiers` 数值修正，不是 `AuraDef`）—— 不是漏了。★★ **铁律⑦：不写 effect kind 的 case 表，无差别深走整棵数据树**（任何带 `dispelType`+`id`+`kind` 的对象就是一枚 AuraDef）——光环藏在 `lockedProjectile.onHit`（寒冷、毒蛇钉刺）、`delayedGroundImpact.onImpact`（星尘）、`spawnGroundArea.onTick`（冰霜风暴、凛冬彻骨）、`spawnTrap.onTrigger`、`onNthHit.effects`、`spendComboPoints.base`、光环自己的 `periodic.effects` 里，case 表的失败模式是「新加一种嵌套 ⇒ 里面的光环静默查不到」，没有任何测试会红（已发生 8 处同族翻车）。**消费面四处接线**：① `toHudAura` 的 kind/name/school —— 联网光环行终于有红绿边框 + ＋/－ 角标 + 玩家可见名（「寒冷」而不是 `mage.frostbolt.chill`，也不是施加它的技能名「霜矢」）；② `auraIconUrl` 三级台阶（原样查 → 注册表 → 去段启发式），**`warrior.mortal_wounds` / `deathknight.winter_domain_chill` / `ffa.greasy` / `ffa.stardust` 这四条此前是光秃秃的色块**（它们 id 的前两段不是技能 id，旧启发式全部落空）；③ `visualAttributeForAuraId` 改成「注册表优先、启发式兜底」，同四条从中性灰变成正确的学派色（实测全表逐枚对比：只有这四条变化，其余 59 枚与旧启发式**逐枚一致**，零回归）；④ 试验场 `CombatDirector.auraDisplayName` 插进第 2 级 —— 光环已经掉了（`dispelled`/`auraRemoved`）时说得出**光环**名而不是退回技能名，两场景从此说同一个词。**旧启发式一条都没删**：运行时拼出来的 id（`control.*`、将来 sim 现造的）注册表里永远没有，unknown 兜底也全部保留。**S7 红线一处都没放宽**：`HIDDEN_AURA_ID` 在 `auraEntryById` 第一行、`toHudAura`、`visualAttributeForAuraId`、`auraIconUrl` 四处各拦一次（掩码光环**连注册表都不许查** —— 查了等于从旁边漏），并有一条**源码门禁**锁住「拦截写在查表之前」这个顺序。**新增 `client/src/data/auraRegistry.test.ts`（14 条）**，三条门禁：① **覆盖率** —— 直接扫 `shared/src/data/**` 的源码文本（`dispelType: DispelType.` 是 AuraDef 的指纹，往上找最近的 `id:`）抠出 63 枚字面量 id，与注册表**双向**逐枚对照；判据故意用**另一种方法**得出而不是把实现的深走再写一遍，两种方法都说 63 才叫真的对上；② **撞 id** —— 同一 id 两处定义成不同 def 的清单恒为空（用**键序无关的深比较**：`rogue.stealth` 潜行/消失共用、`mage.frostbolt.chill` 霜矢/冰枪术共用，两种「一枚多用」都合法，用 JSON.stringify 比会把键序算进去而误报）；③ **S7** 三条含源码顺序门禁。另在 `snapshotAuras.test.ts` / `auraRow.test.ts` / `combatDirector.test.ts` 补了正面用例（kind 从 unknown 变准确、四条图标从色块变真图、掉了的光环仍报得出光环名）。🔵 **未做（留账）**：ⓐ **本地试验场的光环行至今是死的** —— `HudUnit.auras` 在联网侧由 `toHudAura` 填，而本地侧 `CombatDirector` 直接把 `CombatEntity` 当 `HudUnit` 用（结构上满足接口），`CombatEntity` **根本没有 auras 字段**，所以 `dir.player.auras` 恒为 undefined、`auraRowHtml` 恒返回空串 —— 试验场的目标框/自身框从 X17 起就没有过光环行，且因为字段可选，没有任何类型错误或测试会红。这不是 X26 引入的，但注册表就位后修它只差一层投影（`aurasOf(this.auras, id)` → `HudAura`），建议随下一批 HUD 批一起做；ⓑ 注册表对「一枚多用」按**先来先得**记施加技能（`rogue.stealth` 记的可能是消失），所以 `auraIconUrl` 把「原样查」放在第一级 —— 名字对得上的那张图永远最准；真要更准得让 AuraDef 自带图标键，属数据侧改动，不值当。**✅ X26 收口（2026-08-11）**：**major — `school` 的判据从一处变成了两处**：X26 给 `toHudAura` 的 school 加了第三级回落 `entry?.skill?.school`，而本地路径 `LocalCombatView.hudAurasOf` 仍然只填 `a.def.school` —— 实测遍历整张注册表，**63 枚里有 53 枚**满足「`def.school` 缺席但推得出学派」（`warrior.mortal_wounds`/`hamstring`/`bladestorm`→physical、`paladin.judgement`→holy…）。这些 school 唯一的去处是 chip 边色（`auraRow.ts` 的 `SCHOOL_COLOR[…] ?? AURA_NEUTRAL_COLOR`），于是**同一枚断筋试验场是中性灰 `#9aa3b6`、联网局是钢铁色 `#d8cbb4`** —— 正是同批 `vfx/debuffAura.ts` 点名要避免的那条（「两条路各写一遍迟早会漂，玩家只会发现『单机是冰蓝的、联机是灰的』」），只是方向反了；同时上批 leftovers 里「本批全是纯数据判据、**无渲染改动**」与实测不符（53 个格子的边色变了）。修法：注册表新增 `auraSchoolById(id)`（内含唯一那条 `def.school ?? skill.school` 回落），两条投影都调它，优先级统一成**实例/快照 > 注册表**（`control.*` 的学派是 sim 施加时算出来写在实例上的，注册表查不到它，所以实例那份排前面）；本地侧手里明明有 `AuraDef` **仍然走同一张表**（`debuffAura.ts` 那条纪律）。`LocalCombatView` 文件头那段「联网侧只能报 unknown 的诚实降级」已过时，一并更正。护栏两条：① `snapshotAuras.test.ts` **全注册表逐枚**比「本地判据 == 联网判据」（单边用例 109 条全绿也照不出两条投影之间的分歧）+ 一条正面断言「53 枚确实推得出来」（防止退化成「两边同样一片灰」也绿）；② `localCombatView.test.ts` 走**真的** `hudAurasOf`，断言霜甲护盾（def 自己不写 school）拿得到 frost。已变异验证：还原本地那一行，②当场红。**🔵 未做（如实登记，minor 不修）**：ⓐ **毒感知只在 3D 壳层生效** —— `rogue.poisoned_blade`（`dispelType: Poison`、光环自身无 school）在角色身上走 `visualAttributeForAuraId` 得到 `poison` 的黄绿 `0xb6d442`，而 HUD 格子走 school 推导得到 `physical` 的钢铁色：同一枚毒刃**角色是黄绿壳、目标框是钢铁格**。要么 chip 取色改走 `visualAttributeForAuraId`，要么给 `SCHOOL_COLOR` 补一档 poison —— 关键是别让「什么颜色」有两个答案；ⓑ **S7 纵深防御缺口**：`toHudAura` 对掩码光环仍原样透传 `a.school`（`masked` 只挡住了注册表那两级），而同批 kind/name 都做了双重拦截、文件头写的是「这条红线一处都不许放宽」。今天**不是活漏洞**（`visibility.ts` 保证掩码光环不带 school，且 `auraRow.ts` 对 hidden 强制中性色），但应对齐成 `masked ? undefined : (…)` 并补一条「服务器万一发了 school，客户端也不许收」的对抗用例（现有用例只喂不带 school 的掩码光环，恒绿） |
| X27 | **玩家文案「学派」残留清扫**（P10 补丁没扫干净的尾巴，X13 校验顺带清点）：mage.ts 等职业数据的 description/counters 仍有约 6 处「××学派锁定」对玩家可见（P10 口径应为「××系被封锁」，FAIL_TEXT 已是）。X13 亲手重写的那一行已顺手改掉（2026-08-10），其余属存量。已清（主循环 2026-08-11）：八职业 25 处玩家可见字段全换 P10 口径（「X学派锁定」→「X系被封锁」、「不产生学派锁定」→「不封锁任何系」等），gen-docs 重生成，data 测试 277 绿。字符串字面量内「学派」清零（代码注释保留——注释不是玩家语言） | `data/classes/*.ts` 玩家可见字段 | 文案一致性（15.2） | ✅ 主循环（2026-08-11） |
| X28 | **角色被 AABB 竖棱吸住，彻底停住**（P5 地图批对抗校验发现的既有缺陷，非新图引入）：`moveAndSlide` 逐轴解算，圆柱贴上凸角后 x/z 两轴都判相交 → 永久静止；bot 无寻路（B1）无恢复手段。全仓直线可达率实测：已发布图最差 `arena_3v3` **11.46%** 永久静止率（柱环正好落在 45° 上）、testbed 6.82%、ffa 6.47%；本批四张新图靠「中央前最后一段只留可跨几何」压到 1.37~3.85% 并立 6% 机检兜底，**机制本身没修**。两条路需拍板：① moveAndSlide 对竖棱沿切线滑一次（13.5「斜向碰撞沿墙滑动」的本义，但改所有人的移动手感 + balance 必动）；② bot whisker 绕障（突破 B1「不做寻路」口径）。全仓测量表见 docs/17 | `sim/movement.ts` `moveAndSlide`；探针 `themed.test.ts` 直线可达率机检 | 近战追击/bot 可用性的地基缺口 | ⛔ **需拍板** |
| X29 | **变形游走的三笔余账**（Wave4.5「受击状态表现」批，sim 半边落地）：用户口径 2026-08-11「被变形宠物了也应该是在一个小范围内走来走去的」。已做的是**位置真的移动**（`sim/wander.ts`：中招点为锚、2.5 米半径、40% 基速、0.8~1.2 秒换一个目标点，转向由 `(entityId + 中招点, 段序号)` 纯散列驱动（掺中招点是为了不让同一个人每次被变形都走出同一条能背下来的路；**刻意不掺 `world.seed`** —— 那个东西不进快照，掺了会把余账二堵死） —— 不用 `Math.random`，也**刻意不碰** `world.ts` 的 `nextRandom` 实体流，否则游走每 tick 掷一次骰子会把全场的闪避/暴击序列整体错开）。⚠️ **余账一：balance 必动、本批未归因** —— 被变形的目标会走出近战贴脸范围，bot 局里近战的输出窗口和追击路径都会变；本批不跑 balance（主循环归因）。⚠️ **余账二：客户端不预测自己的游走** —— `SelfMovementSnapshot` 不带锚与段，`Predictor` 按 `lock:'full'` 预测成「站着不动」，位置全靠快照纠正。**不是橡皮筋**（预测与权威没有朝相反方向用力：预测位移恒为 0，`reconcile` 每次直接落到权威位置，`smoothing` 只是把这 100ms 的位移补画出来），如实的说法是**自己的小鸡比权威位置滞后约一个快照间隔**（SNAPSHOT_RATE=10 → ≤0.28 米，`CORRECTION` 平滑档内）。化形期间玩家零操控，这一拍无感；要消掉它得给 `SelfMovementSnapshot` 补锚与段 + `Predictor.stepOpts` 消费 —— **没有消费方之前不许先加字段**（死字段是本仓库的老病）。⚠️ **余账三：恐惧（fear）没做** —— 同属 Incapacitate 递减链，但 WoW 的恐惧是「朝远离施法者的方向跑开一大段」，与「原地踱步」是两种语义（半径、速度、朝向规则全不同），混进同一条判据等于给恐惧偷加一个「跑不远」的削弱。本批只认 `drCategory=Incapacitate ∧ flags.stunned`。⚠️ **口径更正（2026-08-11 收口批）**：这里原本写的是「与客户端换小动物模型的判据**同一条**」，**当时并不成立** —— `TestbedScene.isMorphed` 确实按递减类别判，`NetworkScene` 却按**单一光环 id**（`'control.incapacitate'`）判，于是**自带 id** 的气旋囚笼（`druid.cyclone`，同为 Incapacitate + stunned）在联网局里不换模型：sim 按类别判「该走」→ 他在走，摇头的否决又只否决小鸡 → 他在晃，画面上是「一个无法行动、不能被打也不能被治的人形角色边走边晃头」，同一发在试验场则是小鸡。**已修**：判据本体收到 sim 的 `isMorphedFormAura`，两个场景各自反查后落到同一个函数 —— 联网侧走新增的 `client/src/entity/morphForm.ts`（`control.*` 查 shared 新导出的 `CONTROL_AURA_FORMS`，它从 `CONTROL_SPECS` 派生；其余光环查客户端 `auraDefById` 索引），有「索引里每一枚光环两条路答案相同」的逐枚一致性用例 + 两条源码接线锁。⚠️ **反向的坑，别踩**：表现层**不要**改调 `isWanderIncapacitated` —— 两者刻意不等价（后者多一条「另一个硬控在场就由它说了算」），照那条改会让「被变形又被裁决之锤钉住」的目标从小鸡变回人形、昏迷一结束又变回小鸡（一次纯由第二个控制引起的模型闪回，已实测）；`wander.ts` 的注释已按「同一个前置、不同的后置」如实改写。⚠️ **余账四（同批已修）：游走会把人走下台面** —— 目标点此前只看「离锚点多远 + 圆是凸的」，而 `moveAndSlide` 只拒绝**水平**碰撞，走出台沿之后接管的是重力。实测 6×6 台面、离台沿 0.6 米处被变形 30 秒 → 走下去掉 12 米（`MOVEMENT.FALL_DAMAGE_HEIGHT`=8，这一跤还吃坠落伤害），而「被控期间被游戏本身挪下台」与 13.2「所见即所中」是同一个信任等级。已加**地面连续性闸，两道**：① 目标点脚下要有地（`findGroundY` 从抬高一个台阶处往下扫两个台阶，判据与 `tryStepUp` 的落回检查同族）；② **这一步踩下去的地方**也要有地（沿走向探 `GROUND_PROBE`=0.35 米）—— 第二道不是保险丝而是必需：站得住的区域**不一定是凸的**，L 形走道/两墙拐角上起点与终点都站得住、中间那条直线却切出去（全地图网格体检抓到过一例，实测掉 8 米；`wander.test.ts` 里那条 L 形用例就是它的最小复现，去掉第二道闸当场红）。`driveWander` 第六参**可选**，不传 = 不设闸、无参默认路径逐位不变；「被挤出圈外往锚点走回去」那一段刻意不设闸（人可能正被挤到台下，拦住回家比走回去更糟）。此前这一族**整族没有覆盖**：其余用例全跑在 400×400 的无限平地上，那张地板把「脚下没有地」从头到尾遮住了。**收口体检**：20 张地图逐张网格采样（共 800+ 个落脚点，每点 20 秒游走）—— 修后**零掉落、零站死**，最小位移 1.24 米（说明闸没把小鸡冻在原地）。🔵 **顺带的口径**：气旋囚笼同样命中这条判据 → 它也会走，且现在**两个场景都显示小鸡**（WoW 里旋风是把人吹上天，本仓库没有浮空表现，走比钉住更贴近「被控住的活物」——这是如实的近似，不是漏网）。定身/真昏迷/冰封庇护在场时不游走（更强的那个说了算）。🔵 **这一笔已随 W24 清（2026-08-11）**：**死亡不拔锚** —— `tick.ts` 第 2 步那句「取不到实体或 `!e.alive` 就 continue」排在 `stopWander` 之前，游走中被打死的实体 `MovementState.wander` 原样留在仓里（实测死后再跑 20 tick 锚与段号一字未变）。生产路径今天无害（复活走 `teleportTo`，它会置空 wander），但 `arena.ts` 的 `resetRound` 清光环/递减/地面区域/弹体**唯独不碰 movement**（`ArenaDeps` 里压根没有 movement 字段），W24 多回合接线的那天第二回合就会带着上一回合的旧锚开局 —— 正是 `teleportTo` 注释里点名要防的「小鸡穿过半张地图往中招点走回去」。修法一行（`!alive` 分支里先 `stopWander` 再 continue）+ 给 `ArenaDeps` 补 movement —— **两处都已落地**（Wave5/W24 同批）。`ArenaDeps.movement` 取**必填**而不是可选（与 `projectiles` 同一条理由：漏传的代价是「第二回合带着旧锚开局」，必填让它在编译期变红，可选会让它在多回合赛的第二回合才现形）—— 三处夹具（`arena.test.ts` / `lockedProjectile.test.ts` / `verify-m5.ts`）随之补齐。测试各 1 条：`wander.test.ts` 的「游走中被打死 → 锚当场拔掉 + 水平动量抹零」、`arena.test.ts` 的「`resetRound` 后 wander 为 undefined」 | `packages/shared/src/sim/wander.ts`（新）；`sim/movement.ts` `WanderState` / `MovementLock` 注释 / `teleportTo` 拔锚；`sim/tick.ts` 第 2 步；**收口批新增**：`sim/wander.ts` `isMorphedFormAura` + `WanderGround` 地面闸、`sim/effects/combat.ts` `CONTROL_AURA_FORMS`（从 `CONTROL_SPECS` 派生）、`client/src/entity/morphForm.ts`（新）+ `morphForm.test.ts`（9 条，含逐枚一致性与两条接线锁）、两个场景的 `setMorphed` 判据；测试 `sim/wander.test.ts` 19 条（含 5 组变异验证：松锁、不认 rooted、不认第二个硬控、传送不拔锚、打断不抹动量；+ 台面闸一条，已验证「去掉闸就红」） | 被变形的目标不再是钉在地上的雕像；近战贴脸窗口变化未量化 | 🔧 sim 半边✅（2026-08-11）／判据统一 + 地面闸✅（2026-08-11 收口）／**balance 已归因**（主循环 2026-08-11 实跑：**胜率八职业逐位不变，69.0pp 不动**，仅二阶指标微动 rogue 场均 17.1→16.8s / hunter 14.6→14.3s —— 预判「必动」在胜率粒度未应验：40% 速 2.5m 圈跑不出 bot 追击半径，对局收束方式不变；地面闸同批入基线）、客户端预测⛔、恐惧⛔、**死亡拔锚 + resetRound 清锚✅（2026-08-11，随 W24）** |
| X30 | **中招在角色身上没有任何表现 + 击晕只是站着**（Wave4.5「受击状态表现」批的 client 半边）：用户 2026-08-11 拍板原话「中了 debuff 身上要能看见效果 —— 冰系减速身上有一层冰蓝色、火系击晕身上有一层火焰，其他法术一样」「被击晕应该有摇头晃脑的动作而不是简单停在那里」。① 此前身上**唯一**能读出「我中了什么」的通道是 14.3 的控制标记（脚下冰棱/头顶星星/横杠/波纹）—— **减速与 DoT 在模型上零表现**（被寒冰箭咬住和满血站着逐像素相同），八属性视觉语言（14.2）只活在法术侧，受击方那一侧只有 P3 的一记 0.12 秒闪白；② 昏迷播的是 `Hit_A` 的 0.55 倍速循环，人是站直的 | **两件都已交付**。**① debuff 学派色壳层** —— `vfx/debuffAura.ts`（纯判据）+ `StatusMarkers.setDebuffShell`（渲染，与护盾壳/复活保护同一个类持有，生命周期免费）。壳 = 内层正面色膜（胶囊 ×1.05、普通混合、0.20）+ 外缘背面加法（×1.11、0.26），**分层表写在 `SHELL_LAYERS` 的注释里**并有门禁测试：半径必须留在 X14 阵营 rim（×1.16、完全静止、0.13）以内，护盾球壳（R×1.85/2.02）在最外面 —— 四层在几何/混合/不透明度/运动四维上两两错开。优先级口径 **控制 > 减速 > DoT > 其他减益 > S7 掩码**，同类别取 `expiresAt` **最大**者（★ 不是「最新一枚」：协议没有 appliedAt，P11 只发 expiresAt；而且壳是持续表现，选剩得最久那枚颜色才不会来回跳），并列取先出现的；**增益一枚都不上壳**（用户说的是中招）。17.2 的非颜色通道由**八套运动档案**兜（火 rate 6.2 上窜 / 霜 1.6 下沉 / 毒 1.2 最缓…，八项 rate 两两不同；竖向漂移做了半波偏置，所以正负号真的是一条通道而不只是相位）。**S7 红线**：`HIDDEN_AURA_ID` 只走中性灰 + 中性运动，`shellAttributeOf` 第一行就返回 undefined —— 哪怕调用方塞了 school 也不看（有对抗性用例钉着），中性运动的 rate 也与八套学派全不同（免得颜色藏住了、动法漏出去）。**零粒子**：24 人局每人每 0.2 秒撒一簇 = 每秒 120 次 emit，48 格细流池 0.4 秒被自己刷空（`ParticleBurst` 的 ⚠️ 记的实测），所以学派微动全做在材质脉动 + 竖向漂移上，一格池子都不申请（源码门禁锁住 import/new）。**顺带解开联网侧的死结**：快照除 `control.*` 外不带 buff/debuff 向（X26），于是建了一张**只服务壳层**的客户端 `auraId → {AuraDef, 施加技能}` 索引（结构化遍历八种嵌套 effect，54 枚，有「不许缩水」门禁）—— 记住**施加技能**是关键增值：四条光环 id 的前两段不是技能 id（`warrior.mortal_wounds` / `deathknight.winter_domain_chill` / 大乱斗两条道具减益），靠 id 反查会全退成灰。⚠️ 这**不销 X26**（HUD 光环行的 kind 与玩家可见名仍未接，两条出路仍待拍板）。**② 击晕摇头晃脑** —— 先把八个玩家 GLB 的 JSON 块 dump 出来核对（A14 之鉴）：22 个片段里**没有任何 Dizzy/Stunned/Daze 类片段**，但每个骨架都有且只有一根 `head` 骨、**没有 neck**。于是程序化（P6 闪避侧闪的先例）：`entity/stunWobble.ts` 纯算术 + `CharacterView.applyStunWobble` 在 `mixer.update()` **之后**拧头骨（yaw 与 roll 差 90° = 头在画圈而不是左右摇；pitch 走半频 + 半波偏置 = 永远低头、不会仰头）；找不到 head 的骨架回落整模小幅摇摆（只动 x/y，z 留给 P6 侧闪），胶囊兜底阶段什么都不做（头顶星星那条信息通道仍在）。⚠️★ 逐帧**还原干净姿势**是正确性前提而不是优化：当前片段可能压根没有 head 轨道（真素材的 `Hit_A` 就只动手臂），不还原就是每帧再乘一次偏移 = 指数级累积，头会越拧越歪 —— 有 400 帧回归用例钉着。恐惧**不晃**（7.3 里恐惧也置 `stunned`，而 14.3 要求两者视觉不能混；判据 `stunWobbleActive` 两场景共用），死亡/变形一票否决。**顺手修**：`NetworkScene.updateMarkersFor` 此前把 `SIM.TICK_DT`（固定 0.05）当 dt 喂给 `StatusMarkers` 的三条**秒**计时（承伤 0.15 / 破裂 0.4 / X7 过期收束 0.3），60fps 下时间流速 3 倍、三段演出全都只剩名义时长的三分之一，而画面上只是「碎得有点快」没人会报；改传 `realDt`，与试验场口径一致。新增 `vfx/debuffShell.test.ts`（37）+ `entity/stunWobble.test.ts`（18→19），含两场景**各一条接线锁**（场景要 WebGL 才构造得出，「写完了没人接线」在这里既不是类型错误也不是运行时错误，只能锁源码）。⚠️ **收口批（2026-08-11）补的一条：联网侧的「死亡一票否决」此前是死代码** —— `NetworkScene` 两处 `anim.update({...})` 里**既没传 `dead` 也没传 `stunned`**，而 `AnimState.Death` 是 `AnimationController` 从这两个字段派生的，于是联网局里的角色永远进不了 Death：`applyStunWobble` 的死亡否决判的正是 `AnimState.Death`，被控期间死掉的人**尸体一直摇头**，直到那枚光环自然到期（光环不随死亡清除，`deriveStatusFlags` 对死人照跑 —— 实测尸体 `flags.stunned` 仍为 true）。已按试验场口径补齐两个字段（顺带修掉「远端角色死了不倒地、被控不播踉跄」这条既有的双场景不对称），并加一条源码接线锁；原来那条「★★ 死亡不晃」用例之所以绿，是因为它直接 `setAnimState(AnimState.Death)`，绕过了真实场景里唯一的赋值路径。🔵 **未修（留账，2026-08-11 收口批如实记）**：ⓐ **壳没有死亡否决** —— `setDebuffShell` 只接一个 ShellPick、`updateMarkersFor` 对 `interp.sample()` 的每一份快照都调用且不筛 `alive`，所以「被 DoT 咬着或被减速时死掉」的尸体会带着一层按学派脉动 + 竖向漂移的色膜，直到光环到期（毒/流血最长一档好几秒）；控制标记那层同形状，但壳把它从脚下一圈图标放大成了整个身体的色膜，在尸体上更扎眼。修法：调用方对 `!snap.alive` 喂 `setDebuffShell(undefined)`（走既有的 0.28 秒淡出，观感是「死了之后 debuff 的光散掉」），两场景同调 —— 判据放调用方，与 `setStunned` 的分工一致。ⓑ **`updateMarkersFor` 由 `SIM.TICK_DT` 改喂 `realDt` 这件事没有任何断言钉住**（护盾三条秒计时的修正方向没错，但它改的是**护盾表现**、不属于「受击状态表现」这一批，源码门禁只锁了 `debuffShellOf` 的调用、没锁 dt 参数，下一个人改回去不会有测试红）；且 `realDt` 全链路无上限钳制，切标签页回来那一帧的巨大 realDt 会把一整段淡入淡出在一帧内走完（最坏是「啪」地出现/消失，不会 NaN）—— 建议补一条 dt 参数门禁 + 在 `advanceDebuffShell` / `wobbleWeightStep` 入口钳一次（按秒除的只有这两处） | 中招与被控在模型上读不出来，是用户连续两轮点名的观感缺口 | 🔧 代码与测试✅（2026-08-11）。**浏览器实跑已验**（Playwright + `?testbed`，swiftshader 软渲染 4fps，零 page error）：① 玩家中战士断筋 → 壳 `slow`/物理沙色 `d8cbb4`，中法师霜矢 → `slow`/冰蓝 `8fd4ff` 且竖向漂移恒为负（霜下沉）；② 霜爆新星定住假人 → 壳升为 `control`/冰蓝，与「控制盖减速」的口径一致；③ 战士局里玩家被击晕那一帧 `stunned=true / wobbleWeight=1 / hasModel=true`，解控后回 0（「解除即停」）；④ 截图肉眼看过一张：角色身上确实罩着一层贴身的冰蓝，模型本身仍然读得清。**真 GPU 上的终验仍缺** —— 壳的浓度/脉动速率、晃头的幅度与频率**全是占位值**（4fps 下判断不了脉动快慢），等真机调；两条已知局限如实记：ⓐ 壳按标准碰撞盒建，BOSS（视觉 2.2 倍）与巨人化药水（1.6 倍）身上会缩在体内（与 X14 阵营 rim 同一条既有局限，两者一起修才划算）；ⓑ 胶囊是近似剪影，张开的手臂/长杖会探出壳外 |

## P. 性能

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| P1 | **`listEntities()` 每次 spread 新数组且被放进嵌套循环**：tick 软推开对每个移动实体再 spread 全体（O(n²) + 24 人 48 数组/tick）；groundArea 三层嵌套；projectile 每子步 3 次数组分配。`tick.ts` 内层调用是**循环不变量，提出去一行即消** | 三处循环内调用已 hoist（各步顶层的现取**保留** —— 第 3–5 步的效果可能生成实体） | 20Hz × 所有房间的基础成本 | ✅ 批次一 1.8（2026-08-04，balance 逐位不变） |
| P2 | **人机会话拿完整快照 + 裁剪 + `JSON.stringify` 后在 socket 层丢弃** → 满人机房开销 = 满人房。`BotDriver` 注释自认欠账但低估了成本（stringify 也跑了） | `BotSocket.isBot` 标记 + `broadcastSnapshots` 跳过（跳的是浪费不是裁剪语义，M16b 红线原样） | 补位与接管都走这条路 | ✅ 批次一 1.8（2026-08-04，m16 29/29） |
| P3 | **快照中与视角无关的部分逐接收者重建**：projectiles/grounds/flags/score/armories/suddenDeathBlips 每人重算；每实体 `Object.fromEntries(resources)`×2 → 24 人 ≈ 2.3 万对象/秒 | **大头已被 P11 波3 冲销**（2026-08-09 核对时销账）：共享段按队构建 + JSON 序列化每队一次全队复用（`MatchLoop` sharedByTeam），24 次/tick 降到 2 次；`Object.fromEntries`×2 已被 q1Record + 位掩码取代。**残余**：每队仍各构建一次（可见性是队伍级的，属正确成本）；`buildSelfState` 里剩一处 fromEntries | GC 压力主源→已大幅缓解 | 🔧 P11 波3 基本清；残余低优 |
| P4 | `assertNoHiddenEntities` 在生产环境把 O(sessions×entities) 可见性再跑一遍（成本 ×2）。🔵 「宁可掉线不能透视」的决定**保留**，但成本应可度量，可改采样/轮转校验 | `MatchLoop.ts`（≈:697-700） | 有意但未度量 | 🔵/⛔ |
| P5 | **广播对同一消息 stringify N 次**（RoomState/MatchEnd/统计等全量广播）。修法零风险：stringify 一次 + `sendRaw(string)` | `Session.sendRaw()` + `broadcast()`/`broadcastStats()` 共享编码 | 白算 | ✅ 批次一 1.8（2026-08-04） |
| P6 | **全员掉线的对局照跑 20Hz 到终局**：started 分支不调 `dropIfEmpty`，且人机会话占着 `sessions` 让判空恒 false → 无人房间跑完整局（夺旗半小时量级），叠加 P2 | `disconnect()` 判**零真人 session**（`humanSessionCount`，人机 `isBot` 不算）→ `scheduleAbandon` 排 30s 宽限计时器 → 窗口过完仍零人则 `abandonMatch`（停循环+遣散人机+回收房间）。★ 宽限而非立刻拆：集体闪断（verify:m13 §4b 一次掐断双方）要留重连的路，`onReconnect` 里 `cancelAbandon` 撤销 | 可被外部触发的资源占用 | ✅ 批次三 3.7（2026-08-05，RoomServer 单测 ×3：回收/单人不误杀/宽限内重连救回；m13 19/19 不掉） |
| P7 | 重连令牌查找 O(rooms) 线性扫（`[...rooms.values()].find(...)`），应建 token→room 索引 | `room/RoomServer.ts`（≈:580） | 小 | ⛔ |
| P8 | 无 instancing（全仓 0 处 `InstancedMesh`）；模型材质逐 mesh `.clone()`（为受击闪白）破坏合批 —— 可改顶点色/uniform | `entity/ModelLibrary.ts`（≈:143-145）；`arsenal/ArsenalView.ts` 等 | draw call 随掉落物线性涨 | ⛔ |
| P9 | **无自动画质降档**：两场景默认 High（2048 阴影 + 2x 像素比），FPS 已量但无反馈回路，低端机只能自己按 F2 | `render/QualityController.ts`；`GameLoop.ts` fps | 低端机第一印象 | ⛔ |
| P10 | 每帧小额浪费：`NetworkScene` 多处线性 `find/some`（有 Map 不用）；记分板可见时每帧重建对象数组 | `NetworkScene.ts`（≈:1101,1173,1218-1225,1310） | 低 | ⛔ |
| P11 | **快照下行带宽 ~306 KB/s/客户端（≈2.4 Mbps）—— 比同类网游肥 10~30 倍**，是部署容量的第一约束。实测（2026-08-07 容量探针，真 ws 客户端 + 20Hz 输入）：12v12 单房下行合计 **60 Mbps**（≈27 GB/小时）；CPU 反而便宜（13.6% 桌面核/房，线性，零慢 tick）；RSS 88MB 基底 + 7MB/房。根因链 = 20Hz × 全量 JSON 快照 × 逐接收者重建（P3 是它的 GC 面）：无 delta 编码、无二进制、无 permessage-deflate。**这也是弱网真人玩家的问题**（手机/弱 WiFi 扛不住 2.4 Mbps 常驻流）。修的杠杆按性价比：① ws 开 permessage-deflate（JSON 重复键可压 5~10×，代价 CPU）② 非自身实体降到 10Hz ③ delta 快照（典型 10~50×）④ 二进制编码。⚠️ 修哪个都要过 m10 的裁剪红线（压缩/差分不得绕过 redactFor） | 容量探针数据（PROGRESS P3 后问答记录）；`shared/net/visibility.ts` 快照构建；`Session.sendRaw` | 12v12 部署与弱网体验的第一瓶颈 **波1 已落地（2026-08-08，perf/p11-wave1，四提交）**：schema 瘦身（flags 位掩码 + 静态块首见即发 + 装备 EntityLoadouts 指纹通道 + 量化）+ sim 障碍物空间索引（128 AABB 线性扫描是 76% tickWorld / 83% bot 决策的根因，O(n²) separation 只占 6% —— 当初猜错方向）+ 事件广播跳 bot/共享编码。实测：12v12 快照 17.4KB→4.6KB，下行 313→67 KB/s/客户端（4.65×）；3v3 81→20 KB/s。m10 check1/2 重定向到 EntityLoadouts 流（不重定向会恒真）。剩余杠杆按序：按队伍共享构建（~9×快照 CPU）、10Hz 快照率（SIM.SNAPSHOT_RATE 已声明零读者；需先把 INTERP_DELAY 0.1→0.15 并接通 Welcome.interpDelay，否则插值窗静默破）、字段级 delta、二进制 **波2 已落地（2026-08-08，同分支）**：permessage-deflate（L1+上下文接管+wb13，量过参数，事件 <1KB 不压）+ 快照 10Hz（SNAPSHOT_RATE 接线；INTERP_DELAY 0.1→0.15 配套；teleported 跨 tick 粘滞防 13.4 破）。**合计实测：12v12 线上 313→4.1 KB/s/客户端（76×），单房 60→0.8 Mbps；3v3 81→1.3 KB/s（62×）；10 房 12v12 全真人 240 人单核满 10Hz 零丢 tick；40 房 bot 满编 0.82 核**。m10/m13 全绿。带宽不再是第一约束 —— 剩余瓶颈是 CPU：按队伍共享构建（~9×快照 CPU）与 sim 进一步降本是下一波；delta/二进制在 deflate 接管已吃掉跨帧冗余后收益大减，暂缓 **波3 已落地（2026-08-08，同分支）**：按队伍共享构建 —— 实体段是 (世界,队伍) 的函数（新增「同队逐字节相同」测试钉住支点），每队 build+stringify 一次全队拼接复用；逐人的恰好 6 个字段收进 Snapshot.self（buildSelfState）；静态块与装备走 EntityMeta 每会话通道；drops.pickable→self.pickableDropIds。m10 check2/3、m16 check6/8/11b 已重定向。**实测：10 房 12v12 全真人 240 人 1.0→0.61 核；16 房 384 真人 0.83 核零丢 tick（≈19 房/核，生产跑编译 dist 再 +10~15%）**。终态账：1c1g ≈ 12~15 房 12v12 全真人（~300-360 人）或 ~35 房 bot 满编；100 房 24 人需 ~2 核 | 🔶 波1-3已落地 |

## G. 工程与流程

| ID | 债 | 证据 | 影响 | 状态 |
|---|---|---|---|---|
| G1 | **无 CI**：1113 条单测 + 15 支验收全靠手跑，没有任何东西保证提交时它们被跑过 —— **所有其他债的放大器** | 仓库无 `.github/` | 回归网全靠自觉 | ✅ 批次三 3.1（2026-08-05，两 job：typecheck+单测 / 六支非浏览器验收+配平冒烟；浏览器验收上 CI 单独立项） |
| G2 | **无 linter/formatter**（eslint/biome/prettier 全无） | 各包无配置 | 风格与低级错误无门禁 | ✅ 批次三 3.2（2026-08-05，eslint flat config 零告警基线进 CI；formatter 未引入——55k 行统一格式化是独立决定，暂不混入） |
| G3 | **573MB 素材直接进 git、无 LFS**（`.git` 已 482MB、3861 个素材文件被跟踪）；其中**约 150MB 可证明死重**：`_2k.hdr` 126.7MB 代码从不加载（只拼 `_1k` 路径）+ 11 个未引用 `_1k` preset ≈22MB；`ui/` 1451 文件仅 ~93 个被引用 | `.gitattributes` 无 `filter=lfs`；`render/Environment.ts`（≈:86 硬编码 `_1k`、:24-31 仅 5 preset） | clone 成本；历史永久膨胀，转 LFS 越拖越贵 | ✅/🔵 批次三 3.3（2026-08-05）：**160MB 死重已删**（16 张 `_2k.hdr` + 11 个未引用 `_1k`，assets 572→412MB）；`ui/`+`music/` 盘点完（G3a）；**LFS 已拍板（🔵 用户决定）**：部分克隆过渡（README 改 `git clone --filter=blob:none`，零改写零额度），LFS/出库的最终方案与发布前 F2 素材投递一并定 —— 届时先删完 G3a 死重再迁移 |
| G3a | **盘点结果（3.3 中步产出，删除前需逐项过引用方式）**：`ui/` 1451 文件仅 skills 表 ~88 + 光标 3 被引用，**~27MB 死**（daily-rewards/deeds/dungeons/mobs/professions/ranks/store 等整目录是上游包搬来的无关功能；skills/ 未引用图标是未来选图的调色板，**不删**）；`music/` 987 文件按文件名反查仅 91 个基名被引用，**死重上限 ~147MB**（⚠️ 音频常见 `${base}_${n}` 变体拼名，删除前必须核对 sfx 表的拼接方式）；另发现零引用小件：`*_backdrop*.webp` ×6、`space_galaxy.jpg`（CREDITS 里登记过用途但代码从未接） | 本轮全仓 grep（2026-08-05）；引用面：`skillIconMap.ts`、`index.html` 光标、`AudioManager.ts` `/music/${kind}/${name}.mp3` | 死重继续膨胀历史 | ⛔ |
| G4 | **试验场与联网场景是 2779 行平行实现**：~24 个同名字段、`syncWeapon()` 逐字节重复（含同一句注释）、控制标记优先级逻辑写了两遍（联网侧注释声称「不会两条路各写一遍」但确实写了）。已有 `CombatView`/`loadoutViewFromSnapshot` 证明抽共享层可行 —— 这是「护盾判据分叉」类 bug 的持续温床 | `TestbedScene.ts`（1263 行）vs `NetworkScene.ts`（1516 行）；`:619-623` vs `:1455-1459`；`:755-766` vs `:1414-1425` | 每加一个表现都要写两遍、漏一边 | 🔧 批次三 3.4 第一铲（2026-08-05）：`SceneShell` 收走 renderer/画质/环境/镜头/输入/resize 构造 + 画质应用链（四处逐字重复→一份）+ NDC 换算（四处→一份），场景侧 getter 转发、壳唯一持有；八支浏览器验收全绿证零行为变化。**第二铲余账**：实体渲染循环（marker/护盾/化形 5 行×2）、`syncWeapon()`、控制标记优先级 —— 灯光两边数值刻意不同，**不属于**重复面。P10 又添一处：**射线拾取/悬停光标**两场景各写一份（联网侧补「点 3D 模型选中」时按任务范围本地实现，统一成可拾取注册表归本铲） |
| G5 | **债务不可 grep**：~150 处欠账以中文散文注释存在，无机器标记 → 无法统计、无法收敛。**本表即解法**；新债照登记规矩挂 `// DEBT(ID):` | 全仓 0 处 TODO/FIXME/HACK | 判断不了「还欠多少」 | 🔧 本表建立即开始清偿 |
| G6 | verify 脚本靠硬编码 sleep：14 支脚本静态累计 126.8 秒固定等待（m15 一支 27.4s/38 处）。**实测数据点**（P3 收口，2026-08-07）：m4 的控制递减段在 17 支连跑的高负载下偶发失败（递减窗口 15s 与固定 sleep 序列 ~14.8s 只差 0.2s 余量），单独跑 5/6 绿 —— 修的时候从这支下手 | `scripts/verify-*.{mjs,ts}`；`verify-m4.mjs` 递减段 | 手跑成本高、偶发脆弱 | 🔧 单测侧一角已收（用户拍板批 2026-08-10，任务 D）：`RoomServer.test.ts` P13「掉线即接管」全量并发下 5 秒超时（单跑 1.6s 绿）—— 给显式 `{ timeout: 20_000 }` 预算（它要推 1830+300 个同步 tick，慢是真的慢不是挂了），并把接管后的 `setTimeout(200)` 换成 `waitFor('PeerDisconnected')` 条件轮询。**verify 脚本侧 126.8 秒固定等待与 m4 递减段仍欠** |
| G7 | `vite build` 无 `manualChunks`（three ~600KB 进首 chunk）；动态 import 只有 2 处（NetworkScene/LobbyShell），TestbedScene 静态打包 | `packages/client/vite.config.ts` 无 build 配置 | 首屏体积 | ⛔ |
| G8 | 小项：`exactOptionalPropertyTypes` 关闭（代码里大量 `...(x!==undefined?{x}:{})` 说明本可以开）。~~`ws` 版本两处不一致~~（已统一 ^8.21.1，本行 2026-08-09 核对时发现已修未销） | `tsconfig.base.json` | 低 | ⛔（只剩 exactOptionalPropertyTypes） |

## F. 发布阻塞（对外发布的门禁，`docs/09` §7.2 同源）

| ID | 债 | 证据 | 状态 |
|---|---|---|---|
| F1 | **SkillId 与技能名原创化**：91 技能 85 个可辨识借用 WoW；**id 是承重的**（光环 id、vfx key、武器 grants/removes 全引用）→ 跨数据层重命名 + 创作决定（起名评审），规格书 18.3。「不要拖到发布前」 | PROGRESS 技术债 §7；`docs/09` §3 | ⛔ |
| F2 | **素材投递方案**：`vite build` 产物不含 assets（dev 靠中间件流式读），发布怎么带 573MB 未定（打包/CDN/按需加载） | `packages/client/vite.config.ts` 文件头注释；`docs/09` §7.2 | ⛔ |
| F3 | **公网硬化包全绿**（= S1–S6 + TLS 终结 + 客户端 URL 协议自适应） | 见 S 组 | ⛔ |
| F4 | **默认页翻成大厅**：面向玩家那半 P6 已翻（无参 = 主菜单/大厅入口页，验收载体退到 `?testbed` 前缀 —— 本行「现默认试验场」的旧描述 2026-08-09 核对时更正）。**剩发布形态那半**：构建开关/独立入口未落地，`vite.config.ts` 无 build 配置 | `main.ts` 路由（P6）；vite.config.ts | 🔧 玩家侧✅（P6）／发布构建开关⛔ |
| F5 | **真人平衡实测**：`pnpm balance` 是回归工具不是平衡真相（bot 不会假读条/绕柱/风筝；种子间波动 ±6pp）——发布前需要真人对局轮 | `scripts/balance-report.ts` 自述；M14b 章节 | ⛔ |
| F6 | **真 GPU 帧率/观感验证轮**（= X10，发布门禁重列）。首轮帧率数据已入册（见 X10 行，2026-08-09）；发布门禁还差观感肉眼轮 + 至少一台中端独显机器的数据点 | X10 行 | 🔧 |

## B. 平衡与 AI

| ID | 债 | 证据 | 状态 |
|---|---|---|---|
| B1 | `decideBotAction` 只支持单目标（选敌/换目标/保队友没做）；不会用地面技能（`CastIntent` 不产落点，「如实少一类」）；无寻路（撞死角就停）—— 3v3 以上人机可用性的前置 | **P1a 已清一半**（docs/17）：难度三档反应时间、看读条就打断（假读条 <0.35s 骗不出 normal 的踢）、normal/hard 按单发威力出招（冷却感知 + 自身中心 AOE 距离门 + 五种伤害形状估值）。**余账（P1b/P1c）**：**躲地面 AOE 已清**（P1b）；风筝需「先控再退」组合逻辑（纯后退实测恶化 33pp 已回滚）、连击点终结循环（两版尝试逐位同致盗贼 0%，回滚待诊断）、地面落点、换目标、寻路。**难度进大厅已清**（P5，2026-08-06：全链 + verify:p5，另修 canStart 不认识补位致单人开不了局；hard 留踢 + 战斗意志三调用方全接）。**P3b 又清一条**：零冷却 DoT 无限重挂已修（目标身上还在跳的 DoT 不再计周期伤害，`auras` 进 `BotPerception`；牧师基线 0.0%→71.4%）。★ P3b 顺带给「连击点」这条余账添了一个**精确证据**：盗贼割裂的 25 能量能让它基线从 21.4% 掉到 0.0%（改成 0 分毫不差回到 21.4%）—— 因为 bot 不会用终结技，这 25 点能量换来的流血永远兑不成击杀。修 bot 而不是修数值。**P4 大清账**（2026-08-06）：四类死键接通 —— 保命（血 35% 开减伤/吸收/免疫，不叠盾；`cannotAttack` 交易键只在对面读条时开）、控制（DR ≥50% 才出手 + 不叠控 + 免疫不空放；替补打断/peel/锁杀/自保四时机）、驱散（`dispelEligible` 只读资格判定，与结算共用一份规则，无目标不按）、位移（后撤跃/加速/冲锋/背刺传送 + **先控再退**条件版风筝：对手被硬控且拿近战武器且 <12m 才退，窗口内只放瞬发）。基线 90.5→71.4pp、盗贼 0→28.6%、无职业为 0，分步归因九行见 PROGRESS。**P8 再清两条**（2026-08-06）：**换目标已清**（hard 集火 pickFoe：血量评分+粘性，easy/normal 保持最近目标）、**知进退已清**（hard 苟住：弹尽粮绝转身满速跑，七道门）。**B1 团队协作再清两条**（2026-08-08，用户反馈「BOT 都是单独行动，团队 PK 没有配合逻辑」）：**队友治疗已清** —— `allies` 进 `BotPerception`（可选，不传 = 只有我，1v1 与老调用方逐位不变），`healTargets` 按血量百分比排序（平手按实体 id，否则回放分叉），决策链逐个走**既有** `usableOn`/`validateCast` 通道验（P3 的「拿 foe 验治疗致 HPS 恒 0」不许复发），奶不到就退到下一个；**队伍集火呼叫已清** —— `BotDriver.tick` 按队算一次 `callFocusTarget`（全队候选集的 `foeScore` 最优者 + 队伍层 `SWITCH_HYSTERESIS` 防整队横跳），个体拿它当 **-25 分偏置**而不是命令（25 > 迟滞 20 才推得动平手，但拽不走已经在打的残血目标）；`isFoeCandidate` 一寸未动 —— 呼叫不扩大任何人的候选集，队友看得见的潜行者对我依然隐身（A4）。easy 两条都不参与（与不打断/不躲圈同一条难度门）。**顺带修一个静默两年的联网 bug**：`BotDriver` 翻译 `CastRequest` 时硬写 `targetId: foe.id`，把决策层挑好的目标丢掉 —— 自身增益走 `usesNoTarget` 分支没事，而单体治疗/护盾/净化（`Direct`+`Ally`）被当成对敌施放，`validateCast` 静默判 `InvalidTarget`：**联网人机的治疗职业从来没奶中过一次**。**仍欠**：形态轮换、隐匿开场、挡援（`chargeToAlly`）、给队友驱散/上盾、瞬闪逃脱、连击点终结技、绕柱寻路 | 🔧 P1a（2026-08-05，botController 单测 ×12）· P3b（2026-08-06，DoT 重挂）· P4（2026-08-06，四类死键，行为单测 ×21）· P5（2026-08-06，难度进大厅 + 留踢 + 战斗意志）· P8（2026-08-06，集火 + 苟住）· B1 团队协作（2026-08-08，单测 +30，两档基线逐位不变） |
| B2 | 配平测量边界：每对 3 场的离散度（种子 2 极差 40.5pp）；盗贼规避组合被 bot 高估 —— 已如实记录，扩大样本量是可选项 | M14 章节 | ⛔ |

---

## 🔵 已拍板的决定（不是欠账 —— 防止后人当成待办去「修」）

| 决定 | 理由出处 |
|---|---|
| custom handler 剩 2 处（`priest.leapOfFaithLandingGuard`、`druid.wild_charge`）**保留** | PROGRESS 技术债 §1：语义确实无法数据化 |
| 试验场**不建**军械箱 | 141 项验收的初始条件不能污染（M16 已知不足，牧师套盾打掉 M4a 之鉴） |
| 教学课程以法师栏位写死 | 教学载体是试验场，玩家就是法师（M15） |
| 联网技能栏可用性**保守判断**（只看冷却与资源） | 真门禁要 World，宁可多一次被拒的请求也不误报不可用（M10） |
| 音频用线性距离衰减，**不用** `PositionalAudio` | 方位音在镜头旋转时飘忽（`AudioManager.ts` 注释） |
| 不引入 rapier，物理手写确定性圆柱体 | 跨平台确定性是回放/配平前提（`movement.ts` 文件头） |
| `assertNoHiddenEntities` 生产环境开启 | 宁可掉线不能透视（docs/13）—— 成本问题走 P4，不关校验 |
| playwright 固定版本（唯一不带 `^` 的依赖） | 浏览器验收对版本敏感，可复现优先 |
| 人机照常收快照（不绕开裁剪） | 绕开 = 给人机透视；成本问题走 P2 的「按 session 类型跳过序列化」，**不是**跳过裁剪语义（M16b） |
| 大厅不是默认路径 | 无参路径是验收载体红线；发布时走 F4，不动这条 | 

---

## 归并说明（旧账去向）

| 旧出处 | 去向 |
|---|---|
| PROGRESS 技术债 §1（custom 收敛） | ✅ 已清至 2 处保留 → 🔵 决定表 |
| §2 数值占位 / §2b 普攻 | ✅ M14 / M11 已清（历史记录留在 PROGRESS） |
| §3 施法失败提示优先级 | → **A16** |
| §5 抗法护甲 schema | ✅ M11 已清 |
| §6 消耗品 | ✅ M11-6 + M16 已清 |
| §7 技能名/SkillId | → **F1** |
| §8 近战 inRange 负边距 | → **A10** |
| docs/14「继承的技术债」三条 | → 🔵（custom）/ **F2**（打包）/ **F1**（SkillId） |
| docs/14 速赢清单剩余三项 | → **W13**（BGM）/ **W15**（昼夜）/ **X1**（夺旗装饰） |
| 各里程碑章节「已知不足」仍开放的 | → A9/A11/W2-W8/W12/W16-W18/X2-X12/B1（逐条见上） |
| docs/10 已知偏差 #6/#7/#8/#9/#10/#11/#12 | **留在偏差表**（规格偏离，不是欠账）；#14 的实现缺口拆成 A3/A4/S8 |
| docs/10 待设计确认 Q1–Q15 | **留在 Q 表**（设计留白）；与本表交叉的只有 S7/X3 的泄露口径拍板 |
